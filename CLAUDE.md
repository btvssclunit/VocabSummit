# CLAUDE.md — 词山学海 Vocab Summit

**这份文件描述的是「今天什么是真的」。** 读它就够了。
Last updated: 2026-08-28（**`20260828c`：注册完就选头像**——owner：「direct new
users to immediately choose profile pic after selecting nickname and identity」。
换头像 本来藏在 我的档案 → 一个链接 → 一张网格后面，而大多数学生从不打开 我的档案
（和 §18al 把 班级 搬到注册页是同一条）；现在按下 确认 之后**直接铺开头像**，
选完（或按 以后再说）才把画面交回宿主。同批修掉一条既有缺陷：**确认页交给 onDone
的一直是那个字面量，里面从来没有 avatarId**，所以落地页 换昵称 之后名牌会掉回 👤。
见 §18ay。
前一批 **`20260828b`：山上的昵称也有英文了**——把 `20260826a`
漏掉的五处从 `nickname.js` 逐字节搬进 `cs.js`（两张 EN 表 · `nickEn()` · 词块英文 ·
确认页那一行 · 那句 `Let me type my own`）。10,370 种组合全部出得了英文。
⚠️ **只补了一半**：山上两个辅助默认关，而首次弹窗够不着顶栏那对药丸，
所以全新的山上学生仍然看不见它，修法与理由记在 §18aw 末，**待 owner 拍板**。
前一批 **`20260828`：排序搬进 ▼ 菜单**——owner：「let me sort
active date in ascending and decending too like excel」。排序**一直都在**（点列名），
但屏幕上唯一长得像控件的是那颗 ▼，而它只管筛选；现在 升序／降序 坐在同一个菜单最上面，
措辞按列型分（最早在前／小→大／A→Z）。同批：**空值两个方向都沉底**（`—` 在
localeCompare 里排在数字前面，升序会先甩出一屏从没活跃过的档案）· 日期列首击改降序。
见 §18au。
前一批 `20260828b` 之外，同批**补写了 `20260825a` · `20260825b` · `20260826a` 三节**（它们当初只推了版本号，
没写进这份文件）：**道具终于能用**（§18au）· **房间不吃道具的闸门挪到 takeItems()**（§18av）·
**昵称有英文了**（§18aw）。⚠️ 补写时核出一个**今天仍在的缺陷**：
`20260826a` **只落地在 `nickname.js`，四座山那份 picker 至今没有英文昵称，
而且 owner 亲自报错的那句 `Let me type my own` 原样还在**（§18aw 末）。
前一批 **`20260824e`：手机版落地页三处**——owner：「the footer notes
are smacked in the middle of the screen instead of responding dynamically to screen
proportions」。页脚用 `margin-top:auto` 贴底 · 昵称的省略号**本来就没生效**（flex 容器里的
匿名文字吃不到 text-overflow）· 名牌在竖屏换行，昵称独占一行。见 §18at。
前一批 **`20260824b`：落地页那颗按钮改成「开始旅程」，圆角发光**——
owner：「make it rounded and glowing」。⚠️ 这是**刻意推翻**原来的 16-bit 造型
（原注释写着 hard edges / no border-radius / stepped shadow），别当成回归改回去。见 §18as。
前一批 **`20260823x`：词库取一次不够，加重试**——owner 在 HCL 上
撞到 **HTTP 503**，而一次抖动就**永久**毁掉那一屏（无重试、无按钮，学生只会得出
「高级华文今天打不开」）。三次尝试 + 重试按钮 + 说对话的错误信息，山上与码头各一份。
⚠️ 当场实测：同一分钟从**同一个新加坡边缘节点**取五次全 200、Pages 构建全绿，
两分钟后 owner 自己也说「somehow loading again」——**确实是抖动**，
但抖动的代价不该是一整科打不开。见 §18ar。
前一批 **`20260823u`：填空句泄答案一、二级 35 句全部改写**——
接 T5 全量清点，「艰苦的训练__了他的意志」这类**答案就贴在空格旁边**的题一次改完，
四个母表 61 行；**改完重跑清点，一、二级归零**。同批 **码头补回 中/EN 标签**
（它是界面上唯一告诉看不懂中文的人这里能换语言的东西）。见 §18aq。
前一批 **`20260823r`：进度码收进折叠区**（恢复码在每条轴上都更好，**除了要联网**——
那一条没有替代品，所以是收起来不是删）。见 §18ap。
前一批 **`20260823p`：图片瘦身 3.8 倍**——owner：「anything that can
be done about slow loading of images?」。首页美术 **7.9 MB → 1.6 MB**：不透明底图转 JPEG
（档名不动，与 landing_hero_bg.png 同一套做法）· 带透明的量化成 256 色 ·
里程碑徽章 1092px → 512px（最大只用到 150px）· **攀山快答的 3 MB 游戏美术不再开机就下载**。
见 §18ao。
前一批 **`20260823n`：🚑 保险丝自己在抹页面**——看门狗用 `offsetHeight===0` 判空白，
而这套版面下**健康页面本来就是 0**，于是每一页在 6 秒后自毁。同批：年度试炼改填空 ·
顶峰说清解锁条件。见 §18an 末与 §18ao。
前一批 **`20260823k`：🚑 白屏事故**——`.py-ans` 同时是 body 的状态类与元素闸门类，
HCL 对所有人全白。见 §18an。
前一批 **`20260823c`：班级下拉菜单 + 注册就问 + SEO**——owner：「I want
to make it a drop down and make it easy for students to select their class. So that it's
sorted properly in the back end … its a very long list so probably good to help students
to choose their levels (S1/2/3/4) first?」，随后追加「ask students but dropdown class only
applies to bukit view students」。百德的 **30 个班级**进了 `profile.js`，
**先点年级（中一…中四）再从 7–8 个班里选**；别校学生照旧自己填。
**注册流程现在也问班级**（选填），我的档案不再是唯一入口。见 §18al。
同批 **SEO**：Search Console 验证档、每页 description／canonical／OG／Twitter、
`sitemap.xml`、首页 JSON-LD、社交卡 `art/og-card.jpg`，后台与 `tools/` 一律 noindex。见 §18am。
⚠️ **`20260823`（②两扇门）与 `20260823b` 从未被提交**——2026-08-23 清点时它们还躺在工作区里，
owner 硬刷新看不到门，原因就是这个。见 §18am 末。
前一批 **`20260823`：② 从切换改成两扇门**——owner：「the games
button, they expect to click and be directed to choose game mode, but the game mode is
the section below and they miss it … they should be presented with 2 doors on a new clean
page with the current background persisting」。**四座山与出发码头一起改**（owner 当场裁定）。
同批还有四件都是 owner 看着截图报的：**顶栏名牌在手机上被压成一根竖条**（32px 宽、394px 高，
**这条是既有缺陷，不是这次改出来的**）· **码头名牌上「The Beginner's Pier」出现两次**，
整条副标删掉 · **山上的副标不再印「词山学海 Vocab Summit」，只留 CPDD 学段标签**，
并给四座山的名字配上拼音与英文（**Adventure of the Vocab Star · Arena of the Vocab Champ ·
Forge of the Vocab King · Court of the Vocab Sage**，owner 定名）· **门与活动卡的图标按视口缩放**。
见 §18ak。
前一批 **`20260822e`：年级峰 → 关卡**——owner：「年级峰 is a
misnomer as there aren't sub mountains on each mountain, it should be more like a game
stage」。山上只有一个峰，就是 顶峰；那四个标记描述的是**画里没有的地形**。
`关卡` 是「一关」的大白话，两个字都常用，而且 ② 那一栏本来就叫 闯关——
`关` = 一关，学生碰两次而不是一次。同批把 **README 与 GitHub About 一起更新**（见 §3）。
前一批 **`20260822d`：顶栏那条玻璃板整个拿掉**——
owner：「like the seamap doesn't have a top bar … we just have to ensure that the labels
stay visible」。**面板没了，内容还在**：返回、名牌、四颗控件各自带玻璃浮在画面上；
学段名改挂在**自己的小名牌**上（海图能把名字挂在岛上，一局测验没有画可以挂）。
滚动时才浮出一层**没有边缘**的柔光，静止时一点 chrome 都没有。
连带修掉 **地标 hover 时名字出现两次**（原生 title 与 .mtn2-name 各画一次）。见 §18ai。
前一批 **`20260822c`：术语改名 + 后端文件对齐**——
八个学生读不懂的名字换掉了（`HANDOFF_术语改名_20260822.md`，owner 已批）：
**攀山快答 · 沙滩快跑 · 词海钓鱼 · 组词成句 · 出发码头 · 结伴出海 · 学习（原 学行）· 学海起步**，
外加 owner 当场签核的 **组字成词（山上原 组词挑战，与码头统一）**。
⚠️ **只动标签**：模式 id、store key、Firestore 路径、美术档名一个字节都没动。
同批把后端档名的中英混用理顺：**`js/app.js` → `js/cs.js`、`css/app.css` → `css/cs.css`**
（海＝xh，山＝cs；共享档案不站队）。见 §18ah。
⚠️ **`docs/` 底下的旧交接文一律不改**，它们印的是 2026-08-22 之前的名字。
前一批 **`20260822b`：我的词山 只剩五个地标**——
owner 看了 `20260822` 之后裁定 **单元圆点整排撤掉**：它们全都画出来了、也全都点得开，
但 23 个圆点摊在约 330px 山路上就是一条糊掉的链子，**数不出来**，
所以「中四只有四个单元」在修好之后看起来仍然是真的。
现在山上只有 **⛺你在这里 + 四座关卡 + 顶峰**；
**单元卡没有消失，搬进了关卡**（带单元名、主题、掌握数，点进去还是同一张卡）。
营地同时改成 **「你在这里」地图针**，气泡站在路面之上、尾巴指着海拔点，**不再盖住任何东西**。见 §18ag。
前一批 **`20260822`：我的词山 六处**——
**每一级的最后一个单元本来就画不出来**（被 关卡 压在同一个海拔上，四座山各藏四个，
中四 因此只剩 单元一–单元四）· **营地会走过还没通过的 年度试炼**，现在停在那里、峰上加锁 ·
**营地那颗 64px 圆盘一次吞掉五个圆点的点击**，现在缩小、让到路边、且圆点画在它上面 ·
**单元卡的词按 板块 分组**（生活空间／核心／…） · **知道了 换成 返回／学习／挑战**，
后两颗把①复习范围设成那个单元再回首页 · **拼音闸门在这张卡上从来没接过线**，
现在卡上文字与每个词都跟着开关走。连带 **G1 那句「凡事不愿＿＿」改成「完全不愿＿＿」**
（四个学段一起，母表已同步）。见 §18af。
前一批（第十七批）：**教师后台 学生档案卡**——点任一行打开那名学生的
一整页（历练值／灵露／段位／徽章／纪录／出发码头），并**在后台重建学生的进度码**；
连带 **`ws_xh` 第一次有了云端备份**（此前码头一份都没有），以及**日期筛选的结束日在
Windows 上被裁掉**的修复；见 §18z。**`20260819b`：恢复码 `claims/{code}` 全部落地
——十个字符换回全部进度**含灵露与贝壳**，靠全新账号闸门堵住重复漏洞；VS4 长码同批落地。**同日还有 **`segPy` 回填——组词成句的拼块终于有拼音**（§18aa）·
**教师后台 学习支援 一栏**（§18ab：拼音开关看得见了，提示趋势 因为测错了东西被整段删除）·
**榜单自己那一行发光、不再挂「你」**（§18ac）· **进度码去尾零，轻度学生 213→37 字符**（§18ad）·
以及**建设中的恢复码 `claims/{code}`**——十个字符换回全部进度**含货币**，
owner 明令推翻 VS3 的「码里不带货币」（§18ae））。
前一批（第十六批）：**房间 一场只有一个结束条件**——
限时 或 限题，二选一，词雨钉死在限时；**另把房间四块屏幕上与代码相反的旧文案改对**；见 §18y。第十五批是 **结伴出海 上线**——教师后台现在开得了码头房间，
科目行多一格 码头，范围按 组别 选。第十四批是 **码头 同伴挑战**）。
前一批（第十三批）：**航海徽章明细卡**——
第十二批是 **教师后台 结伴登峰出题设置照学生端重做**——
按年级折叠的单元卡 · 两栏 · 模式带说明 · 题数与时长改滑杆）。
前一批（第十一批）：**看句选词 三个缺陷**——音效额度被朗读烧光 ·
`.xh-hint` 一直是透明的 · 选项 class 名对不上 CSS。
前一批（第八批）：排行榜 UID 截断 · 档案头像放大 · 营地去掉自由试炼 ·
码头 学习 收成两张卡 · 顶栏液态玻璃 · 走进社区。

> **历史在别处。** 「当初为什么这么做」「什么试过但失败了」在
> `docs/ARCHIVE_工程日志_2026-08.md`（80 节，2026-08-10 → 08-16，带目录）。
> ⚠️ **不要从归档推断当前行为**——中间有多次推翻，一段话读起来言之凿凿，
> 第三节可能就把它整个废掉了。推断一律以本文件为准；
> 改动一条既有决定之前，去归档里搜关键词、读**最靠后**的那一条。

---

## 1. 这是什么

百德中学 Bukit View Secondary School（BVSS）母语部华文组的词汇自主学习平台。
Owner：郑凯欣（Kai Xin），HOD/MTL。**她不是程序员**——解释改动要用平白的话，
优先给小而可验证的补丁，不要大改写。
Live: <https://btvssclunit.github.io/VocabSummit/>（GitHub Pages，unit 账号 btvssclunit@gmail.com）

名取「书山有路勤为径，学海无涯苦作舟」。对联在落地页上，**必须保留**。

### 五个入口
| 入口 | 页面 | 内容 |
|---|---|---|
| 词星大冒险 | `G1_index.html` | G1 基础华文 |
| 词将竞技场 | `G2_index.html` | G2 华文 |
| 词王淬炼坊 | `G3_index.html` | G3 快捷华文 |
| 词圣鸿文苑 | `HCL_index.html` | 高级华文 |
| 出发码头 | `XH_index.html` | 学海起步 · 看图学词（pre-G1） |

`teacher.html` 是独立的教师后台（邮箱登录 + HOD 审批）。

⚠️ **另一个同名但无关的项目**：旧的 VocabKing（也叫 词王淬炼坊，
<https://for.edu.sg/g3cl-vocabking>）是另一套单文件代码，带 Firebase Auth、硬编码词表、
service worker。它的文件会在全局搜索里冒出来、长得很像。**它的约定在这里一概不适用**，
不要在两者之间抄代码或抄设计假设。

---

## 2. 仓库结构与路径规则

    /            index.html · G1/G2/G3/HCL_index.html · XH_index.html   ← 已发布的网址，必须留在根目录
                 teacher.html（独立，不加载任何共享 JS）
                 guide.html（使用与设计指南，同样独立；见 §18o）
                 firestore.rules · README.md · CLAUDE.md
    js/          cs.js（四个学段的引擎）· arena.js（房间，刻意与 cs.js 隔离）
                 profile.js（我的档案／头像／进度码／意见反馈／船只；ws2_profile 的唯一所有者）
                 nickname.js（落地页 · 昵称选择器 · 海图与航行）
                 search.js（五站通用搜索，六个页面共用）
                 tts.js（多音字送读校正表；cs.js 与 xh.js 都读它）
                 xh.js（出发码头，从不加载 cs.js）
                 firebase-init.js（匿名登录 + Firestore 助手）
    css/         cs.css · xh.css
    tools/       voices.html（TTS 音色诊断）· sound.html（音效诊断）
    data/        g1/g2/g3/hcl.json · id_registry.json
                 xh_v3.json（码头 149 词）· xh_phrases.json（生活空间 90 句）
                 search_index.json（五站搜索索引，生成物）
    art/         bg/ badge/ avatar/ camp/ mountain/ seamap/ item/ sprite/ sprite/avatar/
                 xh/（码头 sprite · 场景图 scene_*.png · badges/）
    docs/        HANDOFF_* · PATCH_* · SPEC_* · BRIEF_* · DECISIONS_*.csv · ARCHIVE_工程日志
    archived_art/ 退役但保留的美术

### ⚠️ 两条最容易踩的路径规则
- **CSS 里的 `url()` 相对样式表解析**：`css/*.css` 要写 `../art/…`。
- **JS 里拼出来的路径相对页面解析**，而页面都在根目录：JS 里写 `art/…`、`data/…`，不加 `../`。
- 三个文件读 `document.currentScript.src`，但**只取 `?v=` 查询串**，不取目录——
  移动脚本不影响缓存机制。

---

## 3. ⚠️ 部署仪式（每次部署必读）

**每次部署把版本号推进七处**：`index.html` + 四个学段页 + `XH_index.html` 的 `?v=`，
以及 `teacher.html` 里的 `ASSET_V` 字面量。当前：**`20260828c`**。

⚠️ **同一天部署第二次就加后缀**：`20260816` → `20260816b` → `20260816c` → … → `20260816z`
→ **`20260816AA` → `20260816Ab` → `20260816Ac`**（单字母用完之后，owner 2026-08-16）。
⚠️ **日期永远保持真实**：换天就换成当天的日期并从无后缀重新开始，
**不要**为了继续排序而把日期写成假的——这个字符串同时是「这份构建是哪天出的」的唯一记录。
纯日期字符串在同日两次部署之间不会变，第二次会被当成旧的送出去——
这正是这套机制存在的原因，而且上线第一天就踩过。

**为什么需要**：GitHub Pages 给每个文件 `cache-control: max-age=600`，
十分钟内浏览器**根本不问**有没有新版本，而且**每个文件各自老化**。
于是设备上会出现**新的 cs.css 配旧的 cs.js**。owner 真实遇到过：
新徽章瓦片有 hover（新 CSS）但点了没反应（旧 JS 没有处理函数）。

- 版本化的是六个本地 JS/CSS；gstatic 的 Firebase SDK 已在上游版本化，不动。
- **数据 JSON 自动继承版本**：`cs.js`/`arena.js`/`xh.js` 从自己的 `<script src>` 读 `?v=`
  再接到 `fetch("data/…json")` 上，不必单独记。
- `teacher.html` 是例外（独立、不加载本地 JS），它的 `ASSET_V` 要手动改。
- 忘了改不算 regression，只是退回没有这套机制之前的行为。
- 这**不是** build step，也**不是** service worker，两者都仍被排除。

### ⚠️ 部署清单（每次 push 之前逐条过）

上面那段讲的是**为什么**，这一段是**照着做**。分开写是因为 2026-08-25 发现
「当前：」那一行停在 `20260823` 熬过了 `20260824b` 和 `20260824e` 两次部署——
一段没人逐条勾的散文，读过和照做是两回事。

1. **`?v=` 推进八处**（七个页面 ＋ `teacher.html` 的 `ASSET_V`）。
   一条 `sed` 全改，因为 `teacher.html` 那两处用的是同一个串：
   ```
   sed -i '' 's/?v=20260828c/?v=新值/g' *.html
   ```
   改完核一遍，应当只剩两行——新值，加 `guide.html` 那个空的 `?v=`
   （guide 不在名单上，见 §18o）：
   ```
   grep -oh '?v=[0-9a-zA-Z]*' *.html | sort -u
   ```
2. **把「当前：」那一行改成新值**（就在本节开头）。
   ⚠️ 这一条最容易漏，因为漏了**当次部署一点症状都没有**——受害的是下一个人：
   他照着这份文件读到的是三次部署之前的串，`sed` 的「旧值」就写错了，
   于是第 1 条静默地什么都没改。它和 `README.md` 是同一类失误（§不在任何 QA 回路里）。
3. **三份门面**——见紧接着的下一节：`README.md` · GitHub About · 本文件。

⚠️ 只改 `CLAUDE.md` / `README.md` / `docs/` 这类**浏览器根本不加载**的文件时，
第 1、2 条自然是空操作——不要为了「走完清单」去推一个没人会取的版本号，
那只会把「当前：」和真正上线的构建对不上。

⚠️ 漏掉第 1 条**不是**「学生收不到这次改动」：`max-age=600` 十分钟后照样会去取新的。
真正的代价是那十分钟里**每个文件各自老化**，设备上出现新 CSS 配旧 JS——
也就是上面记的那次「新瓦片有 hover、点了没反应」。别把它说得比实际严重，
也别因此就不做：那十分钟正好是一节课的开头。

### ⚠️ 每次改动后要一起更新的三份门面（owner 2026-08-22 立的规矩）
1. **`CLAUDE.md`** —— 这份文件（本来就在做）。
2. **`README.md`** —— ⚠️ **最容易漏的一份**。它是 GitHub 上第一眼看到的东西，
   而且**不在任何 QA 回路里**：改错了没有任何症状。2026-08-22 清点时它同时挂着
   三个已退役的名字（启航码头／攀山竞速／看图学词）和三个对不上的数字
   （码头 150 词、句库 92 句、「plus 100 at the pier」）——那些都是几周前就改掉的。
   ⚠️ 改模式名、改词数、改玩法清单，**同一次会话里就要落进 README**。
3. **GitHub 的 About**（描述 · 网址 · 主题）—— 仓库页右上角那一段。
   ⚠️ **它不在仓库里，`git push` 带不上去**，所以它是三份里唯一一个「提交了也没更新」的。
   跑 `local-admin/update_github_about.sh`（`--dry` 只印不写）。
   ⚠️ **`gh` 装在 `~/.local/bin/gh`，不是 Homebrew 装的**（这台机器没有 Homebrew，
   而装 Homebrew 要密码）。它是 GitHub 官方 release 的 macOS arm64 版，
   下载后核对过官方 SHA-256 才解压；`~/.zshrc` 里加了一行把 `~/.local/bin` 放进 PATH。
   ⚠️ **登录只需一次**（token 存 macOS 钥匙串）：`gh auth login` →
   GitHub.com → HTTPS → Login with a web browser，用 unit 账号。
   ⚠️⚠️ **这台机器上 `gh` 现在登着两个账号，而默认活跃的是 `kaixinbuilds`，不是
   `btvssclunit`**（2026-08-28 撞到）。症状很误导：`gh repo view` **照常读得到**
   （公开仓库谁都读得到），只有写入那一步回 **`HTTP 404: Not Found`**——
   GitHub 对没有权限的写入回 404 而不是 403，免得泄露仓库存不存在，
   **所以它看起来像「仓库找不到」，其实是「你不是这个仓库的人」**。
   跑之前先切、跑完切回去：
   ```
   gh auth switch --user btvssclunit && zsh ../local-admin/update_github_about.sh; gh auth switch --user kaixinbuilds
   ```
   ⚠️ **脚本是 `#!/bin/zsh`，用 `bash` 跑会在 `${(s:,:)TOPICS}` 那行报
   `bad substitution`**（那是 zsh 的分隔符展开）。⚠️ **`local-admin/` 不在这个仓库里，
   它是 `repo-clone` 的同级目录**（§3 正文写的相对路径要从 repo-clone 里当 `../local-admin/` 读）。
   ⚠️ **描述上限 350 字符，中文一个字算一个**；脚本会印出当前长度。
   改描述时顺手核对 README 开头那段说的是同一件事——**那两处最容易只改一边**。

**部署流程**：本地 `git commit` → push 到 `main` → GitHub Pages 一两分钟后生效。
（Repo Settings → Pages → Source: `main` / root。）

---

## 4. ⚠️ 水线：这个项目最重要的不变量

平台有**两套互不相通的经济**，中间那条线叫**水线**。

| | 四座山 | 出发码头 |
|---|---|---|
| 存储 | `ws2_{stream}` | `ws_xh` |
| 掌握度 | 海拔（掌握词数） | 航程（认得词数） |
| 努力值 | 历练值 | 航海值 |
| 货币 | 灵露 | 贝壳 |

**结构性保证**：`cs.js` 从不读 `ws_xh`，`xh.js` 从不读写 `ws2_{stream}`。
不是靠人记住，是靠代码里根本没有那条路径。

### 允许跨线的只有四样
1. **身份**：`ws2_profile`（昵称、头像、班级、学校）。码头也加载 `profile.js`——
   在码头是另一个人，比任何重复代码都糟。
2. **搜索**（`js/search.js`）：**只读**。不写任何存储、不发任何货币、
   **结果不可跳转进任何活动**。一旦某个结果变成一扇门，码头学生就能走进山上的计分系统。
3. **船只**：拥有记录在全局 `ws2_profile`，**贝壳或灵露都能买**。
   ⚠️ 这是对水线的**收窄，不是推翻**：水线禁止的是**兑换**，两个独立标价不构成兑换
   （没有回售，价值过不去）。理由是船会出现在**落地海图**上，而多数华文学生根本不进码头。
   **不要推广成汇率，也不要在没有同样论证的情况下再加第二件双币商品。**
4. **进度码**（`profile.js` 的 VS3，2026-08-17）：一段码同时装着四座山与码头的进度。
   ⚠️ 这同样是**收窄不是推翻**，而且理由与船只是同一条：**水线禁止的是兑换，这是搬运**。
   恢复时码头的数字回 `ws_xh`、山上的回 `ws2_{stream}`，没有任何换算，不暗示任何汇率。
   ⚠️ **结构性保证一个字节都没变**：cs.js 仍然从不读 `ws_xh`，xh.js 仍然从不碰
   `ws2_*`；跨线的工作全在 profile.js（身份层，本来就在第 1 条里）。
   ⚠️ **「码里没有任何货币」这条已于 2026-08-19 被 owner 推翻**（见 §18ae）：
   现在货币会被恢复。**但它当初的理由没有被推翻**——花掉再恢复旧码等于东西和钱都在——
   只是不再靠「不带货币」来兑现，改靠**全新账号闸门**：单调字段（掌握／认得／纪录／拥有）
   照旧按并集合并，**快照字段（灵露／贝壳／items／equip／berth）只写进一个全新的账号**。
   ⚠️ **快照必须整批原子写入**：恢复钱包却把「拥有」按并集合并，重复漏洞立刻回来。

### ⚠️ 三个数字永不相加、永不互换、永不合成一个排名
**海拔**（广度，单调不减，1 词 = 1 米）· **历练值**（深度，可重复，随难度与连对放大）·
**灵露**（努力货币，只在营地商店花）。码头同理：航程 · 航海值 · 贝壳。
排行榜分开排，永远不出现合计分。

---

## 5. 数据与流水线

### 四座山
- Excel 母表是**唯一真相来源**（不在仓库内，和 `local-admin/` 放在一起）。
- 正常流程：改母表 → 跑 `local-admin/generate_vocab_json.py --write` →
  **`data/*.json` 与 `id_registry.json` 一起提交**。改脚本后跑 `--verify`（应逐字节一致）。
- 直接改 JSON **只允许作为同步对**：同一次会话里把同样的改动落进母表并核对。
- 词数（2026-08-09）：g1 426 · g2 814 · g3 1069 · hcl 1432 = **3,741**。
- 词 ID（`G3-0001` 式）由 `stream|年级|单元|板块|词语` 决定，**永久**。
  词换了单元/板块就是新 id（设计如此）。
- ⚠️ **新词一律追加在所属板块末尾，永不中间插入**——进度码按词序编码，插入会让旧码解错。
- ⚠️ **跨学段一致**：同一个词在每个母表里的 拼音/词性/中文释义 必须相同。
  唯一白名单例外：**绷**（G3 bēng 拉紧义 / HCL běng 板脸义，刻意教两个义项）。
- ⚠️ **JSON 载入器只复制显式字段清单**（`WORDS.push({...})`）。新增词条字段必须同时加在那里，
  否则字段永远到不了渲染层。
- 板块集合**按 stream 不同**（G1 三个 / G2·G3 四个 / HCL 五个）：**渲染数据里有什么，不要硬编码**。

### 出发码头
- `data/xh_v3.json`：**156 词，八个组别**（食物 37 · 动物 28 · **交通 18** · 日常用品 17 ·
  地点 15 · 学校 14 · 天气与自然 14 · **数字 13**）。
  ⚠️ 交通 10 → 18 是 2026-08-22 那批（§18aj），**十八个词整组都有 `子类`**：
  陆路交通 11 · 水上交通 3 · 交通设施 3 · 空中交通 1。
  ⚠️ **整鸡 已并入 鸡肉**（owner 2026-08-16 晚：「一个意思一个词，整个码头统一」）。
  美术退役到 `archived_art/`，它唯一的那句 `scene_market-4` 改写成
  「我想要买一些鸡肉。」。150 → 149 是这么来的，不是漏词。
- ⚠️ **数字组没有 `图档`**，这是刻意的：数字本身就是符号，画三个苹果教「三」教的是苹果。
  ⚠️ **没有图档的词渲染成阿拉伯数字，不是空框**（owner 2026-08-16 深夜）：
  `img()` 在 `!hasPic(w)` 时吐一个 `.xh-num` span。此前 连线板 / 词海钓鱼的渔获 /
  回合末复习列表 都在向 `art/xh/` 要一张不存在的图，学生看到的是**五个空白卡**。
  （词语卡 另有自己的 96px `.xh-sprite.numeral`，不走这一条。）
  ⚠️ 看图识词 / 听音识图 仍然整组排除：**数字瓦片本身就是答案**，那正是它们排除数字的理由。
- ⚠️ **「两」已删除，数字组只留「二」**（owner 2026-08-16 深夜：「只留二，免得混淆」）。
  两者的 `数码` 都是 `2`，在 连线 板上会变成两张一模一样的瓦片。
  ⚠️ **「二」的 `注记` 仍然写着「数东西要用「两」」，这是对的**：删的是**词条**
  （不再当答案、不再当干扰项），注记是解释语法差异的地方，而这一条英文说不清楚。
  ⚠️ 四句句子里出现的「两」不受影响：它们的 `ask` 不是「两」。
- `data/xh_phrases.json`：**90 句**（83 可出题 + 7 display-only + 2 `pic` 覆盖），21 条 `insight_en`。
  2026-08-16 由 92 句改为 90：`scene_aquarium-6`（圣淘沙海豚表演已停办）与 `scene_mall-5`
  （内容与 购物商场 无关）整条删除，另有 7 条改写（量词 颗→棵 · 台→支 · 座→个、
  猪→牛、有买→这是、公园→同学上学、吃完后→吃完饭后）。⚠️ 改 `zh` 的同时
  `en`/`ask`/`insight_en`/`meta.count` 都要跟着改，清单在 `PATCH_03` §5。
- **两个只有 组词成句 读的可选字段**：`seg`（词语级拼块，**73 条**）与
  `accepted_zh`（替代语序，7 条）。⚠️ 看句选词 / 我的词语表 / 搜索索引 **一个都不读**。
  **一个干扰项都不存**——运行时从目标词的同组别抽取。
- ⚠️ **干扰项永远取自同一个 `组别`，没有例外。** 另有一份互斥黑名单（包子/饺子、猪肉/牛肉、
  汤/面、茶/咖啡、同学/朋友、车/德士、豆干/面包、摊位/菜市场、平板电脑/电脑），
  在同组抽取**之后**再排除，不因此放宽同组规则。
  ⚠️ 黑名单的正确维护依据是 `store.stats[].confused` 的真实数据，**不要凭感觉加预防性条目**。
  ⚠️ **黑名单以前只管 MCQ 选项，管不到 连线**（owner 2026-08-16 深夜报「猪肉 牛肉 绝不能
  同时出现，图太像」）：`mates()` 只在 `distractors()` 里被调用，而 连线 的板面**就是
  回合词表**，从不走那条路。现在 `startRound` 在 `mode === "match"` 时逐个准入，
  与 `distractors()` 同一套逻辑。实测 食物 范围 14 局 8 组板，零冲突。
  ⚠️ 若黑名单把可用词压到不足一板，**宁可开一块小板，也不重新放行被禁的一对**。

### ⚠️ 搜索索引的连带依赖
**任何 `data/*.json` 的改动，必须在同一次会话里跟着跑
`local-admin/generate_search_index.py --write`**，否则索引与线上资料静默漂移。
当前 2,144 条 / 178KB。它读的是**已发布的 JSON**而不是母表——这样索引不可能和线上不一致。

---

## 6. 内容规则

- **课文例句已从发布的 JSON 中彻底移除**。每一句发布的句子（释义措辞、填空句）都是 owner 自撰，
  所以站点可以公开、可以随便分享，没有版权圈地问题。母表保留 课文例句 仅供参考。
  ⚠️ 未来若要恢复例句，**必须同时具备书面 CPDD 许可与登录层**；在那之前永不重新引入。
- **填空句 `cloze`**：恰好一个 `__`，且目标词不得在句中别处出现。
- 填空挑战**永不出没有有效 `__` 的题**：缺 cloze 的词跳过并在控制台警告，绝不退化成显示答案。
- **综合填空（多空段落）** 在 CL 教研组人工审阅前排除在外。

---

## 7. 设计系统（已锁）

- 四个学段共用一套 BVSS 视觉身份（校誉高于分科主题化）。
- 调色板（`cs.css :root`）：晴空 #DFEDF7→#BAD6E8 · 旭日 #FFEDA8/#F5C443 · 青山 #5B8A66 ·
  深山 #3F704F · 碧海 #2E6391 · 深海 #1F4A70 · ink #243B4A · gold #E3A63C ·
  **`--gold-ink` #8A5A0F**（小字用金；`--gold-deep` 在卡片上只有 3.19:1，够大号衬线数字、不够小字）。
- 地平线场景 = 校徽语言：**三座山峰**（中间最高、在前），山海一律通到画面边缘，
  **太阳在右**（旭日东升）。
- 浅底配深墨字白卡片；深海蓝面板配浅色字。
  ⚠️ **深色面板上的注解必须用浅色**——同一个深金色压在深蓝概览条上只有 1.62:1。
- 竖排对联用 `width:1.15em` + `word-break`，**不要用 `writing-mode`**（渲染不可靠）。
- ⚠️ **代码内嵌的引号一律用 「」**，不要弯引号，不要直引号。
  （**词汇数据**里的引号是另一回事，用弯引号 “ ”。）
- ⚠️ **散文与文档不用破折号**，用冒号和逗号。
- ⚠️ **横屏是首要设计目标**（PLD：带键盘的 iPad、Chromebook）。竖屏手机是可用的次要回退。
  **永不锁定方向**——网页也做不到。断点：≥900px 双栏，以下堆叠。
- **动线编号 ①②③**只用在真正多步的决定流程上；**每个画面从 ① 重新开始**；
  **可选的辅助永不编号**；**排行榜／成就墙／我的词山不编号**（它们是去处，没有正确顺序）。
- **液态玻璃**：`--glass*` 六个 token 驱动所有面板。
  ⚠️ alpha 在 .5–.72 是刻意的（面板压在学校 Chromebook 的花哨底图上），
  **不要为了效果调低**；密集小字的面板（`.pop-card`、搜索结果）反而**调高到 .95**。
  ⚠️ `@supports not (backdrop-filter)` 回退块必须留在 `cs.css` **最末尾**，新玻璃面要加进去。
  ⚠️ **旧 iPad 上 `backdrop-filter` 很吃效能**，而旧 iPad 正是 G1/G2 学生的主力机型。
- **功能先于装饰**：行为稳固之前不做纯美化；**永不擅自重新设计**徽章、地平线、精灵。

---

## 8. ⚠️ TTS（只读中文，硬规则）

这是华文 App：**只有中文会被读出来**。英文释义提示一律静音。

1. ⚠️ **永远不要把拼音字符串喂给引擎**（会被读成无声调英文）。**只传汉字**。
   真读错的词只能用**同音汉字**修，不能用拼音。（`POLY_MAP` 已于 2026-08-12 移除，不要重建。）
2. ⚠️ **音色要评分，不能取第一个 zh-\***：受管 Chromebook 装的是 eSpeak-NG（报 zh/cmn 但无声调），
   常排在 Google 普通话 前面。eSpeak 评到最后，只当兜底。`tools/voices.html` 用来在学生设备上核对。
3. ⚠️ **`cancel()` 后必须等 50ms 再 `speak()`**：同一 tick 内会被 ChromeOS 静默丢弃。Samsung 同理。
4. `voiceschanged` 监听 + 200ms 重试（音色异步加载）。
5. 完全没有中文音色时给一次性提示；根治要 IT 管理员在 ChromeOS 后台启用 Google 普通话。
6. ⚠️ **iOS/iPadOS 需要 primer**：只有在**用户手势里同步调用** `speechSynthesis.speak()`
   才会拿到语音会话。`primeTTS()` 在第一次点击时同步发一条 `volume=0` 的空白 utterance。
   **那一句 `speak()` 永远不许包进任何定时器**，包了就等于没修。
7. ⚠️ **朗读永远不能泄题**：喇叭读的是**选项**或**题干**，不读答案。
   （码头三个玩法曾因「点图片就念出答案」而集体泄题。）
8. ⚠️ **读音以数据为准，不让引擎自己猜**（`js/tts.js`，2026-08-16）：
   喇叭拿到的是汉字，多音字读成什么由引擎决定——owner 报的「姐姐长得清秀」
   屏幕上写 zhǎng、声音是 cháng。现在 `speak(text, py)` 把 py（`py`/`zhPy`/`clozePy`）
   一起传进去，送给引擎的字符串里把多音字换成**同音的单音字**（长 zhǎng → 掌），
   **屏幕上的汉字一个都不动**。
   ⚠️ 每个字**只钉一个读音**——引擎最容易猜错的那个。两个方向都钉过：
   「长时间」变成「常时肩」，引擎拿到一串非词只能逐字念，语调全散。
   ⚠️ 替换字三条：只有一个读音 · 声韵调完全相同 · 常用字。
   自动从字典挑会挑出「叾」「苆」这类生僻字，已废弃，表是手工维护的。
   ⚠️ 音节数对不上就整句不动（和 `rubyText` 同一条判断）。
   ⚠️ 没有逐字拼音的地方（HCL 的句子、码头的生活空间句）维持引擎默认。

---

## 9. ⚠️ 音效（WebAudio，与 TTS 是两条路）

「TTS 有声、音效没声」是这套问题的典型症状。

- Apple 平台整个页面只有**一个共享音频通道**，`speechSynthesis` 会把它拿走，
  把 AudioContext 推进 WebKit 独有的 **`"interrupted"`** 状态（既不是 running 也不是 suspended）。
- ⚠️ **对 `interrupted` 的 context 调 `resume()`，Promise 可能永远不兑现。**
  所以**不要把播放挂在 `resume()` 上**：照样调，但同时挂 **120ms 兜底**；
  兜底时若仍未 running 就**丢掉旧 context 重建**（1 秒内不重复、总共最多 8 次）。
- 每个 context 挂一个 gain 恒 0 的**静音保活源**，通道才不容易被回收或被语音夺走。
- 每一句朗读结束（`u.onend`/`onerror`）都调 `reviveAudio()` 把通道要回来。
- ⚠️ **判断只能写 `c.state !== "running"`**，写回 `=== "suspended"` 会漏掉 `interrupted`。
- ⚠️ **重建额度必须能归零，而且朗读进行中不要花它**（2026-08-17，见 §18t）：
  `_acFails`／`_rebuilds` 原来只增不减，于是那 8 次是**整页寿命**的额度，
  却被花在「朗读占着通道」这种**注定失败**的尝试上——答对八次就永久没声音。
  现在 context 真的 running 就清零，`speechBusy()` 时直接不花。**上限本身要留着。**
- ⚠️ **永远不要把音效当作唯一的正反馈**（见 §18t）：静音开关那一条意味着
  「那一声就是『答对了』」在静音的 iPad 上从构造上就是坏的。声音是加分，不是信号。
- ⚠️ iPad 侧面的**静音开关会掐掉网页音效但不影响朗读**，症状一模一样。先排除它。
  `tools/sound.html` 是现场诊断页。

---

## 10. ⚠️ 学习支援（拼音 / 英文）

- **机制是 CSS 闸门，不是重绘**：gloss span **永远在 DOM 里**，靠 `body.py-aid` / `body.en-aid`
  （码头是 `body.xh-py-on` / `body.xh-en-on`）显隐。切换只是一次 class 翻转。
  ⚠️ 这不只是性能：**会重绘的开关会重抽干扰项**，那就是泄题（见 §14）。
- **范围**：只注解**导航与按钮壳文字**。题干、释义、句子、选项**永远纯中文**——
  和只读中文的 TTS 是同一个沉浸逻辑。**不要把 `enl()` 用到词汇数据上。**
  唯一例外是**拼音**可以注在句子/释义上（`<ruby>`，见下）。
- ⚠️ **key 必须就是屏幕上那串中文**。`pyl()` 找不到 key 时返回空字符串——**漏了是静默的**。
  三条自检：两张表 key 一致 · 每条拼音音节数 == key 里的汉字数 · 每个调用点可见中文 == key。
- ⚠️ **拼音是手写的，不是生成的**（固定字符串手写就绕开了所有多音字陷阱）。
  **每加一条 `EN_LAB` 就补一条 `PY_LAB`**；码头是 `XH_PY`。组别／子类的英文在 `XH_GROUP_EN`
  （那是**数据**，不属于界面标签表）。
- 可用范围：**G1/G2/G3 有，HCL 一个字节都不发**。码头**默认两个都开**。
- ⚠️ **`.xh-always`**：码头上少数几处绕过闸门——看图学词卡片的拼音与英文
  （那是「先学再测」的那一面，藏起来等于让零基础学生盯着两个不认识的字）、
  词海钓鱼答错后显示的拼音、
  **组词成句的英文题干**（`p.en` 就是题目本身：学生要排出的正是这句英文的中文说法，
  关掉它等于给一盘没有提示的词块）。**其余一律走闸门。**
  ⚠️ **看句选词的英文译文已于 2026-08-16 晚移出这份名单**（owner 报「英文关了还在」）。
  真相是它**从来没进过闸门**：那一行 `.xh-ph-en` 既没有 `.xh-en` 也没有 `.xh-always`，
  不是被豁免，是被漏掉。现在它带 `.xh-en` 走闸门，而它的样式覆盖**必须写成
  `body.xh-en-on .xh-ph-en`**（见 §18e：不带前缀就输给闸门规则，字号会掉到 12.5px）。
  ⚠️ 组词成句 的英文仍然豁免，理由不同且仍然成立：那是**题干**，不是注解。
- **`<ruby>` 注音**（G1/G2/G3，HCL 无）：`rubyText()` 每个 CJK 字消耗一个音节，
  ⚠️ **数目不符时返回 null 退回纯文本**，绝不让后面的读音整体错位。

---

## 11. 模式与「什么算掌握」

### 四座山
- **学习**：填空挑战（⭐2 / ⭐⭐3 / ⭐⭐⭐4 / ⭐⭐⭐⭐打字，另有 打拼音 档）· 华文解释 ·
  英文翻译 · 词语闪卡
- **闯关**：词雨灵露 · 攀山快答 · 组字成词（四个学段，2–8 字词）· 词语汉兜（**仅 G3/HCL**，12 次两栏）
- **其他**：我的词山 · 营地与商店 · 成就墙 · 词山风云榜 · 我的词语表 · 结伴登峰 · 同伴挑战

### 出发码头
- **学习**：**两张卡，和山上的 学习 一模一样**（owner 2026-08-17，§18p）——
  **词语闪卡**（分 词语卡 / 句子卡）· **学习挑战**（**七种题型，分三排**：
  认词 = 英文选词 · 看图识词 · 听音识图 ‖ 用词 · 生活空间 = 看句选词 · 组词成句 ‖
  配对与拼字 = 连线 · 组字成词）。
  ⚠️ 学以致用 与 词语挑战 都已不是门也不是名字；②③ 现在与山上逐字对应。
- **闯关**：词海钓鱼 · **沙滩快跑**
- **右栏（浏览层，不受 ①学习范围 约束）**：我的词语表 · **走进社区**（十个场景，
  一句一句读；§18p）· 航海徽章 · 码头风云榜 · 我的海滩（hero 本身）
- **模式**：**沙滩快跑** 是 闯关 里的一扇门，门后选用哪种题型来跑——
  它仍然是一个皮肤，不是一种题型（见 §18i／§18m）
- **其他**：我的词语表 · 航海徽章（九级）· 我的海滩与小铺 · 码头风云榜

### 掌握闸门（已裁定 2026-08-16，三处已对齐）
**四座山上有四个模式触发 `markMastered`**（海拔 +1，首次掌握 +10 历练值）：
**填空挑战**（含各星级与打字档）· **攀山快答** · **华文解释** · **英文翻译**。
房间模式另经 `conferMastery`（按词文本计分，不变）。
⚠️ 裁定方向是**代码去追文案**：`showMasteryInfo()` 一直就是这四个模式的说法，
它是对的，**不要改文案**。此前只有前两个模式真的计入，是代码落后。
⚠️ 码头**不在此范围**：它没有 华文解释，用自己的 航程／贝壳 机制。
⚠️ **打拼音／组词拼音维持原样**：只给一成历练值、不给海拔、不进连对。
- **打拼音 / 组词拼音** 刻意只给**一成历练值**、不给海拔、不进连对（否则可刷便宜的连对倍率）。

### ⚠️ 无图组别不得静默失败
`图档` 为空的词（数字组）**不能进 看图识词 / 听音识图**——那两个玩法的答案就是图片。
`hasPic()` / `modeNeedsPic()` / `poolForMode()` 同时过滤**回合池与干扰池**。
⚠️ **只过滤仍会静默失败**：学生选了模式、按下出发、什么都不发生。
所以这两个玩法在无图范围下**变灰 + 写明「这组没有图片」**，
记忆中的模式若被封锁则自动切到可用的。未来任何无美术的组别只要 `图档` 留空就自动继承。

### 看句选词（生活空间，**原名 传声筒**）
⚠️ **目标是「让零起点学生反复遇见这些词」，不是「掌握这些句子」。**
学生看不懂整句是**正常状态**；句子是**语境**，不是考核对象。
凡在「更难」与「更多曝光」之间取舍，**一律选曝光**。
- **每题恰好一张贴图 = 目标词的贴图，从一开始就显示。**
  ⚠️ 贴图**不是提示，是题目的一部分**：`这包＿＿多少钱？` 单看句子，
  香料/龙虾/苹果/糖 全部成立，**这题无解**；配一张糖的图，答案唯一。
  它同时消除了同组抽取的「偶然正确」失效模式（图就是答案）。
- ⚠️ **量词泄漏是刻意的，不要「修正」**：`这支＿＿多少钱？` 会把答案收窄到手机一类。
  ⚠️ **例句 2026-08-16 从 `这台` 改成 `这支`（手机不用 台），规则本身没变**（PATCH_03 §5.3）。
  在山上算送分；**在码头这正是量词的教学价值**——从「台」推出答案就是学会了「台」。
  **永远不要为了「更难」把量词从题干挖掉。**
- `insight_en` **可选**，留空是正常状态。

---

## 12. 经济与进阶

- **历练值**：`round(base × attemptDecay × streakMultiplier)`（最少 1）+ 首次掌握 +10。
  attemptDecay 1.0/0.40/0.15；streak ×1 → ×2.0。已掌握词的重复：`max(1, round(base×0.25))`，
  每词每天最多 3 次计分。⚠️ `TERMS` 数组每年更新一次（MOE 学期日期变动时）。
- **灵露**：`LINGLU_BASE(10) × tier × pinyin(×0.65，仅打字模式) × decay(100/50/25/10%)`。
  ⚠️ `awardLingLu` 必须在 `gymNote()` **之前**调用——gymNote 会清掉待巩固，
  先调它就静默丢掉复习补偿。
- **房间模式（结伴登峰／同伴挑战）照常计分**：历练值 + 灵露 + 掌握。
  同一套 per-word 灵露衰减与 per-day 历练值上限照样生效，所以房间不是捷径。
  ⚠️ 跨学段房间**按词文本而不是按 id** 计分——外来 id 会污染海拔。
- **段位**：四级（初行客 → 寻径人 → 踏云者 → 凌霄客），按 `store.pts.total`，**每个学段各算各的**。
  ⚠️ 数值仍是提案，真实数据跑满一学期后重新校准。
- **徽章三个家族，永不混算**：
  - **A层 里程碑** 五枚（板块章 → 单元章 → 年级章 → 顶级词王），键前缀 `c·/u·/l·/t4`
  - **B层 对战奖牌** 八枚（结伴登峰／同伴挑战 各金银铜 + 5 金称号），键前缀 **`b·`**，
    展示顺序 铜→银→金→称号（⚠️ `BATTLE_RANKS` 的**下标就是名次**，不要反转它，
    展示顺序另有 `BATTLE_DISPLAY`）
  - **航海徽章** 九级（码头），另一套美术、另一套计数
  ⚠️ `Object.keys(store.badges).length` **不等于** A层徽章数：用 `achBadgeCount()`。
  以后加 C/D 层务必用新前缀并走这个函数。
- **码头的两张费率表**（`SAIL_PTS` / `SHELL_PTS`，js/xh.js）：
  pic 2/1 · enmcq 2/1 · listen 3/1 · match 3/1 · type 4/2 · phrase 3/1 · sort 4/2 ·
  **build 4/2** · learn 0/0。⚠️ **凡是会出题的模式都必须显式列在两张表里**，
  加模式时两张一起加：缺 key 与「故意写 0」长得一模一样，看句选词 就是这样静默零收益上线的。
- ⚠️ **③难度 只乘贝壳，不碰航程与航海值**（HANDOFF §3.3，2026-08-16 晚）：
  ×1 / ×1.5 / ×2 向下取整，乘在「非首次答对减半」之后。
  理由：航程 是「认不认得这个词」，简单档答对和挑战档答对认得的程度一样（§4.1）；
  把难度乘进掌握度等于**倒扣这一层最该服务的那批学生**。
- ⚠️ **航海徽章是开放式里程表，不是集邮**：门槛 10/25/50/75/100/150/200/300/400 是**绝对值，
  永不重新编号**；**没有终点徽章**（破 400 就加一级，600/800 已预留）；
  灯塔 = 400 词的远方标记，**不再等于「全部词语」**。
  理由：码头是开放的，词表一天之内 36 → 100 → 150。「集齐」是一条**会往后跑的终点线**，
  等于凭空收回学生已经付出的努力。
  ⚠️ 顺序刻意排成 生物→器物→船→器物→船，相邻两级不同类。
  ⚠️ 计数基础是 `store.done`——**这正是那个字段绝对不能删的原因**（见 §13）。

---

## 13. ⚠️ 绝对不要做的事

- **不要删 `store.done`**。它同时驱动 航程、**九枚航海徽章的门槛**、
  学习范围组别 chip 的「学过了 N」、码头风云榜识词数榜。
  （某份设计文档要求把它当「图鉴的收集字段」删掉——那是错的。）
- **不要把 `enl()` / 拼音注解用到词汇数据上**（题干、释义、句子、选项）。
- **不要在没有 owner 书面裁定的情况下改掉掌握闸门。**
- **不要把三个数字（海拔／历练值／灵露）合成一个排名。**
- **不要重新设计 A层五枚徽章**，也不要让三个徽章家族共用美术或计数。
- **不要引入 service worker / build step / PWA 打包**（当前阶段仍排除）。
- **不要加 home-screen / manifest / apple-touch-icon**：MOE 受管 PLD 不允许，owner 已否决。
- **不要在四个学段与码头之间抄进度或货币**（见 §4 水线）。
- **不要把 `xh_foam_lap.png` / `xh_wave_chase.png` 接回沙滩快跑**，除非**重画过并验过去溢**
  （见 §18l）；也不要用新美术把「潮水追赶」做回来。
- **不要给商店的商品重新加边框**，也不要手改 `SPRITE_SCALE` 的某一行（见 §18l）。
- **不要重新引入 课文例句** 到已发布文件（见 §6）。
- **不要给 沙滩快跑 加失败条件**：潮水是反馈不是追兵，一局到题数结束就结束（HANDOFF §1.4）。
  零起点学生**永远不该被半途掐掉**。
- **不要在码头再造第二条计分路径**：进度、货币、掌握一律走 `noteRight`／`noteWrong`。
  连线 是唯一的例外，那正是它上线至今没发过贝壳的原因（见 §14）。
- **不要加验证码**：威胁模型对不上（防的是机器人，实际是无聊的学生手打垃圾），
  纯前端的可绕过，第三方的涉及未成年人数据。挡滥用的是**署名 + 可停用额度**。

---

## 14. ⚠️ 已知陷阱（都真实发生过）

| 陷阱 | 症状 | 规则 |
|---|---|---|
| **选项重洗＝泄题** | 切换拼音/难度/出题方式会重抽干扰项，正确答案是唯一每次都活下来的——切两次就看出答案 | 每题**缓存**干扰池（key = `state.i + "\|" + w.id`）；加宽难度只**追加**选项 |
| **按钮嵌套按钮** | 浏览器把内层按钮**拎出**外层，版面静默错位 | 需要在可点区域里放按钮时，外层改 `<div>` + 一个透明覆盖按钮 |
| **喇叭嵌在选项里** | 手指偏 2px 就**提交了那个选项** | 🔊 一律是**兄弟节点**，`::after` 扩大热区，中间留 gap |
| **孤儿 `</div>`** | DOM 查询说在右栏、画面却在左边 | 改栏位结构后核对 div 平衡，别只信 `closest()` |
| **grid/flex 子项不缩** | 画布或面板溢出，把内容推出屏幕 | 加 `min-width:0`；grid 轨道写 `minmax(0,1fr)` 不写 `1fr` |
| **`align-items:start` 挡住拉伸** | `flex:1` 无空间可拿 | 要拉伸的那一栏单独 `align-self:stretch` |
| **基础规则写在 media query 之后** | 媒体查询静默失效 | 同优先级后者赢：把 `@media` 块放在基础规则**之后** |
| **内联自定义属性** | media query 永远覆盖不了 | 坐标类变量写在 CSS 类里，**不要写在 `style=""` 上** |
| **像素画缩放** | 海面比岛糊 | 背景图**保持原分辨率不缩**，只做 256 色量化 |
| **抠圆按颜色** | 银牌／螺钿环接近纸白，阈值法会把徽章自己吃掉 | **按几何抠**：从画框边缘**向内**扫找第一段非白，Kasa 拟合圆（残差应 <1%） |
| **磁红去背** | 硬阈值留洋红边，或把红旗一起吃掉 | `min(R,B)−G` signature + despill 斜坡；薄雾要**反混合**不是阈值 |
| **合并切图** | 数量对得上但映射错位（2026-08-15，142 词错 36 个） | **凡需合并才切出的素材，用前必须逐张肉眼核对。数量不是证据。** |
| **三个精灵家族互指** | 营地美术悄悄变样／圆形缩略图里六个身子挤一团 | `art/avatar/*`（方形朝左，选择器）· `art/camp/pet_*`（营地）· `art/sprite/avatar/*_sprite`（6 帧朝右）**永不互指** |
| **云端回调不返回** | 全部学段卡在「正在装载词库…」 | 受管网络下 `signInAnonymously()` 不抛错也不回调；`CLOUD_WAIT_MS = 6000` 超时照常开门 |
| **bfcache 还原** | 从科目页返回后海图瘫痪 | 用 `pageshow` 复位，不是 `load` |
| **重复加 class 不重启动画** | 第二次航行不动 | remove → 读 `offsetWidth` 强制回流 → add |
| **`serverTimestamp()` 刚写入是 null** | 最新一条从 `orderBy` 结果里消失 | 客户端排序，不要 `orderBy` |
| **`textContent` 抹掉注解** | 拼音／英文／步骤数字消失 | 会被重写的标签用 `innerHTML` 并重新发出注解 |
| **只设 `background-image`，不设 `background-size`** | 屏幕比图宽就开始平铺，右边冒出第二座山 | size/repeat/position 写在 CSS 里；内联的 `background-image` 覆盖不掉它们 |
| **flex 子项被拉伸** | `height:auto` 无效，图片被 `align-items:stretch` 拉长；配 `object-fit:cover` 就裁成一条竖缝 | 缩略图要 `align-self:center` + `object-fit:contain`，两个都要改 |
| **关掉的 AudioContext 还被闭包引用** | 兜底重建之后照样没声音，且不报错 | 播放函数把 context 当**参数**传进去，不要闭包引用外层变量 |
| **注解覆盖没带 `body.*-aid` 前缀** | 覆盖静默失效：`.tts-lab .pylab` 是 (0,2,0)，输给文件更靠后的 `body.py-aid .pylab`，深金压深蓝只有 1.62:1 | 任何 `.pylab`/`.enlab`/`.xh-py`/`.xh-en` 的覆盖**必须**带闸门前缀（山上 §18e 已记过一次，2026-08-16 晚在 🔊 按钮上又中一次） |
| **第二条计分路径** | 连线 上线至今**一颗贝壳都没发过**，且不报错：`SHELL_PTS.match` 一直是 1，缺的是**调用**，不是 key | 一切进度／货币走 `noteRight`／`noteWrong` 一条路。整盘计分的玩法也要调同一对 award 函数 |
| **注释里少一个 `*/`** | 后面**整条 CSS 规则静默消失**（`.xh-run` 高度变 0，画面里什么都没有） | 往既有注释块里加字就加在块**内**；改完在浏览器里量一次 `getBoundingClientRect` |
| **按颜色找水印** | 「洋红且绿通道被抬高」同时也描述了**任何浅色边缘的过渡像素**：一次就在浪花上打了 24 个洞 | 水印要三重判定：**颜色签名 + 固定角落 + 四周是空板**（孤立）。差一条都会误伤 |
| **裁剪无缝贴图** | 交接文要求「裁成 2 的幂宽度」，一裁接缝就断（4.5 → 6.4） | 无缝带**只验缝、不裁**。CSS 的 `background-repeat` 不需要 2 的幂 |
| **货币图标被卡片的图片规则吃掉** | 海滩小铺每个价格都画着一枚 **62px 的大贝壳**（owner 2026-08-17「way smaller」）。`.shell-icon{width:17px}` 是 (0,1,0)，`.beach-card img{height:62px}` 是 (0,2,0)——**不是配色问题，是优先级**，和 §18e 英文注解那次同一类 | 货币芯片的图片规则**必须比卡片的图片规则更具体**（`.xh-scost img` / `.shop-cost img`）。新加任何「卡片里的小图标」都先想一遍谁更具体 |
| **拿元素盒当角色大小** | 同一个高度下 沙僧 只有 鼠 的一半大（owner 2026-08-17）。六帧表**格子高度统一是 104，画进去的creature 不统一**：沙僧 41×72、鼠 101×92 | 角色尺寸乘 `WSProfile.spriteScale(id)`（profile.js 的实测表）。⚠️ 度量是**画出来的质量**不是画出来的高度——按高度归一会把 蛇 和 龟 撑成巨兽 |
| **放大精灵之后动画顶出画面** | 沙僧 的庆祝帧画满整个格子，`.xh-sea` 又是 `overflow:hidden`——欢呼时脑袋被切掉 | 抬高任何精灵之前算一次余量：`base × k × (1 + 抬升 − inkTop/104) ≤ 可用高度`。垂钓的欢呼因此从 18% 降到 5% |
| **Firestore `undefined`** | 直接抛错 | 用 `delete payload.x`，不要设 `undefined` |

---

## 15. ⚠️ 验证方法（先试这个，别先读代码）

- **离屏 WebKit 渲染器**：`swiftc` 编一个 60 行 WKWebView（加载本地 URL → 执行 JS →
  `takeSnapshot` 出 PNG）。**引擎和 owner 的 Safari 一致**，既能量 `getBoundingClientRect`
  也能真的**看见**页面。Browser pane 时灵时不灵，先试它。
- ⚠️ 配 **no-store** 的 `http.server`（普通 `SimpleHTTPRequestHandler` 会发 `Last-Modified`，
  于是你在 debug 一个旧文件）。
- ⚠️ **Promise 过不了 `evaluateJavaScript` 桥**（WKErrorDomain 5）：
  异步测试把结果挂 `window.__R`，由 Swift 端轮询取回。
- ⚠️ **四个学段页在离屏环境会卡在「正在装载词库」**（6 秒兜底走 `setTimeout`，被节流）：
  用一个**不加载 firebase 的临时测试页**绕开（`window.WSCloud` 为 undefined，`boot()` 直接开门）。
- ⚠️ **离屏不跑 rAF**：canvas 是空的。把 `requestAnimationFrame` 换成自己的队列，
  再用 `setInterval` **分多次**泵帧（一口气同步泵完没用，图片解码需要回到事件循环）。
- ⚠️ **点击后才插入的 `<img>` 常常 `complete=false`、量到 0×0**，即使服务器已 200。
  用 PIL 按同一套坐标重新合成来核对构图。
- ⚠️ **`backdrop-filter` 子树里的背景图在 `takeSnapshot` 时不合成**（截图里是空的）。
- 没有 Node：语法检查用 **JavaScriptCore**（`osascript -l JavaScript`，`new Function(src)`）。
  ⚠️ 批量改字符串拼接之后**必须立刻 parse**：留下的未闭合字符串只会报 `Unexpected EOF`。
- **断言证明数据流，证明不了事件有没有到达你的处理函数**——两天里有两个功能通过了全部
  headless 断言却在真浏览器里第一次接触就失败（攀山快答横屏分栏、营地拖动）。
- **几何类的东西要用脚本验算，不要用眼睛估**：山路、台阶落点、海图航线、标牌避让——
  本仓库每一处「看起来对」的手工坐标都出过错。
  ⚠️ 山路的正确做法是**owner 在副本上用洋红涂一遍，脚本提取**（颜色检测会跟着受光草地跑）。
  ⚠️ 海图改任何一个岛的坐标，都要重跑 **20 条航线**的避让验算。

---

## 16. Firebase 与安全规则

- 匿名登录 + Cloud Firestore（asia-southeast1）。**学生 PII 一律不收**，昵称是化名。
- 集合：`users/{uid}`（进度）· **`claims/{code}`（恢复码，见 §18ae）** · `scores/{uid}`（山上榜）· `dockScores/{uid}`（码头榜）·
  `rooms/{code}` + `players/{uid}`（房间，TTL 靠 `expiresAt`）·
  `feedback/{ticketId}` · `feedbackQuota/{uid}` · `teachers/{uid}` · `teacherRequests`。
- 只有 `category === "student"` 的档案会发布到排行榜；老师／家长／公众只浏览。
- ✅ **规则已全部发布并在真实资料库验证过（2026-08-20）**：users · scores · dockScores ·
  claims · rooms · moderation · feedbackQuota 逐一探测通过。
  （2026-08-16 那句「若干块尚未发布」已经过时，本次核实后改写。）
  未发布的块表现为 permission-denied，**其余功能不受影响**——这条判断方式仍然有效。
- ⚠️ **owner 的副本在 `Documents/VocabSummit/firestore/`，`repo-clone/firestore.rules` 是真相来源。**
  🐛 **那份副本 2026-08-15→08-20 是过期的**（少了 claims 区块），而它的 README 写着
  「Claude Code 会同步更新这个文件夹」——**那是一句约定，不是一个机制**，加 claims 时就没做到。
  症状很隐蔽：owner 打开副本、全选、粘贴，**Publish 是灰的**——因为粘贴的内容与线上
  一模一样，Console 判定「没有改动」。**看起来像 Console 坏了，其实是副本落后了。**
  ⚠️ **改完 `firestore.rules` 立刻 `cp` 到那个文件夹，并在它的 README 里加一段说明。**
- ⚠️ **Firestore TTL 只删 room 文档，不级联删子集合**——player 行要自己清。
- ⚠️ 新的 `orderBy` 字段需要**复合索引**；首次查询会抛出带控制台链接的错误，从那里建。
- ⚠️ **教师端改档案只改云端**：没有 profile 的云→端同步，学生下次保存会把旧值推回去。
- **反馈额度**：默认 20/天，靠**工单 ID 形如 `{uid}__{YYYY-MM-DD}__{n}`** 做服务端硬上限
  （规则数不了文档，但每人每天只有 N 个合法文件名）。`feedbackQuota/{uid}.max = 0` 即停用。
  ⚠️ **额度数字不在表单上显示**（owner 2026-08-16 晚）：「今天还可以提交 20 次」
  对最可能灌垃圾的那批学生读起来是一个**待破的记录**。限制照旧生效，只是不宣传；
  用完时的提示也不带数字（「今天的反馈次数用完了」）。

---

## 17. 工程约定

- 单一用途的定点补丁，改前**断言原字符串存在**。
- 纯 vanilla JS，IIFE，**无 build step、无框架**。
- 本地测试要用 HTTP server（`file://` 下 fetch 会失败）。
- IME 输入：跟踪 `compositionstart/end`，组字期间忽略 Enter。
- 游戏版面要跟着 visual viewport 重排（屏幕键盘）。
- **会话连续性**：每次会话的产出是下一次的基线，永不回退到更早的版本。
- ⚠️ **两份必须保持逐字一致的代码**：昵称选择器（`cs.js` 与 `nickname.js`）。
- ⚠️ **刻意的重复**：`teacher.html` 独立，重复了调色板与部分样式；
  `xh.css` 末尾抄了 `profile.js`/`nickname.js` 需要的规则；`arena.js` 有自己的一份词雨常数。
  **改一处要改两处**——漏了的症状是「面板在码头没样式」或「两边手感不一致」。
- ⚠️ **加新画面前先看看现成的类名**：自造 `.opt-row`/`.xh-spk` 而不用既有的
  `.xh-optrow`/`.xh-otts`，结果喇叭没样式、飘在按钮下方。

---

## 18. 当前状态与未完成

### 刚上线
通用搜索（五站，只读）· 船只四级（双币）· 传声筒（学校场景已跑通）·
码头 149 词八组 · 九级航海徽章 · 十张场景背景 ·
**组字成词** · **一档难度** · **沙滩快跑** · **码头的意见反馈按钮**（见 §18h/§18i）·
**沙滩快跑改成跑者跨栏跑（沙滩不再卷动）** · **两个商店改成 Pikmin Bloom 式无框货架** ·
**头像精灵按实测质量等大**（见 §18l）· **码头 ③ 重新分家 · 学以致用并进 词语挑战 ·
我的海滩自由摆放**（见 §18m）· **句子层拿回名字（用词 · 生活空间）· 读过 N 句 ·
房间排行榜滚动修复 · 攀山快答第一帧头像修复**（见 §18n）·
**排行榜 UID 截断到 8 位 · 我的档案头像 132px · 营地去掉自由试炼 · 档案里不再重复反馈按钮 ·
码头 学习 收成 词语闪卡＋学习挑战 两张卡 · 码头顶栏液态玻璃 · 走进社区**（见 §18p） ·
**一段进度码涵盖五片陆地（VS3，落地页也能生成与恢复）**（见 §18r）·
**我的背包 · 走进社区用场景底图 · ③ 去掉副标题 · hero 精简**（见 §18s）·
**教师后台 结伴登峰出题设置照学生端重做**（见 §18u）·
**教师后台 学生档案卡 · 后台重建进度码 · 码头进度上云**（见 §18z）·
**我的词山 六处：藏起来的单元 · 营地被 年度试炼 拦住 · 营地不再吞点击 ·
单元卡按 板块 分组 · 返回／学习／挑战 · 卡上的拼音闸门终于接上**（见 §18af）·
**我的词山 只剩五个地标 · 单元搬进关卡 · 营地改成「你在这里」地图针**（见 §18ag）·
**② 从切换改成两扇门（四座山与码头一起）· 门后是一页只有活动卡的干净页 ·
顶栏名牌在手机上不再是一根竖条 · 四座山的名字有了英文**（见 §18ak）

### ⚠️ 未完成
> **完整的一份在 `docs/OUTSTANDING_待办清单_20260822.md`**，每条都实测过，
> 按「等 owner 出内容／卡在美术／待建功能／只差真机验一次」分好了。
> ⚠️ **这份清单不在任何 QA 回路里**：写错了没有任何症状，只会让人去做一件已经做完的事。
> 2026-08-22 核实时就抓到两条已经不成立的（Firestore 规则、场景底图），已删。
> **每划掉一条，就在同一次会话里删掉它。**

- ✅ **恢复码 `claims/{code}` 全部落地，规则已发布并在真实资料库跨设备验证（2026-08-20）**：
  A 设备生成码 → B 设备（**不同 uid**、完全空白）**小写带空格**输入 →
  昵称、掌握词、灵露 340、贝壳 120、道具、装备、纪录、海滩摆位、头像与船只**全部回来**，
  且**没有继承旧的恢复码**。四条安全性质同时验过：`list` 被拒 · 伪造他人 uid 建立被拒 ·
  篡改既有文件的 uid 被拒 · 过短的码被拒。
- ⚠️ **我的海滩的拖动没在真机上验过**（§15：营地拖动正是那两个「通过全部 headless 断言、
  真浏览器第一次接触就失败」的功能之一）。**iPad 上要再点一次。**
- ✅ **`segPy` 已回填并上线**（owner 2026-08-19 审毕，见 §18aa）。
- ⚠️ **海图对角船只美术待重画**：八张 `*_diag` 龙骨只画到 20° 左右，桶心是 ±45°，偏差 20–33°。
  简报在 `docs/BRIEF_海图船只美术_20260817.md`（自足，可整份贴进 AI 聊天构思）。
  ⚠️ 量角度**必须用龙骨线拟合**（每列最下方不透明像素做最小二乘），整张图的 PCA 会被帆和桅杆带偏。
- ⚠️ **素食摊 owner 已裁定加进词表，但卡在一张美术上**（见 §18a）：`摊位` 已经占用
  `xh_stall.png`，共用会让 看图识词 出无解题、连线 出两张一样的瓦片。
  **拿到新图之后**才能加词，并同时跑 `generate_search_index.py`、把 §5 的 148 改成 149。
- ⚠️ **语句覆盖率 74/148**（2026-08-17 实测，与评估表逐格一致）：
  天气与自然 **1/14**、数字 **0/13** 是最薄的两组。⚠️ **这不是缺陷，是内容进度**——
  `poolForMode()` 会过滤掉没有句子的词，组灰掉并写明理由，行为是对的。
  但**天气与自然 只有 1 句**：学生把范围设成这一组再进 用词，一整回合都在同一个词上打转。
  **这是 owner 侧最值得先补的一组。**
- ⚠️ **数字组补不补图仍未定**（评估 A-3）。已向 owner 说明：13 个词是
  零一二三四五六七八九 + 十 + 百千，**其中 十百千 没有诚实的画法**，
  所以补图必然只能补到 10/13。⚠️ 另有一条没走完的路：`数码` 字段已经存在且
  `img()` 已经把它渲染成阿拉伯数字，理论上「瓦片写 3 → 选 三」是一道**合法且有用**的题，
  与 §11「数字瓦片本身就是答案」那条既有裁定冲突，**要 owner 拍板才动**。
- **看句选词只跑通了 学校 一个场景**；其余九个尚未逐场景过一遍。
- ✅ **组词成句 已上线**（`seg` 73 条已合并，见 §18g）。
  ⚠️ 含逗号的 9 条与 7 条 display-only 句 MVP 不给 `seg`（交接文 §3.6）——**这是正常状态**。
  ⚠️ **农场只有 4 句，永远不会被选为回合主题**（`startRound` 只挑填得满的场景），
  删句没有改变这一点，**不是缺陷**。
- **四个词失去了唯一的语句**：猪 · 公园 · 钱包 · 海豚（另 书包 失去唯一出场，
  但它本就不是 `ask`）。它们**不会报错**：`poolForMode()` 会把没有语句的词过滤掉，
  在 看图识词／听音识图／词海钓鱼／连线／我的词语表 里照常存在，失去的只是**语句层的曝光**。
  owner 已裁定可接受，留待日后扩充语句时补回。
  ⚠️ **不要为此删除任何 `store.done` 条目**，已答对过的学生记录照旧。
- ⚠️ **消耗品／道具与 沙滩快跑 的关系已定**：交接文 §5 明确「沙滩快跑 不上道具」。
  攀山快答 的三件道具与它无关。
- ✅ **生活空间 的独立入口（门 A）已建，名字是 走进社区**（owner 2026-08-17，§18p）。
  ⚠️ 它在**右栏浏览层**（我的词语表 旁边），**不在 ③**：③ 的每一扇门都受 ①学习范围
  约束并发贝壳，而它走完整个句库、不计分、不看范围。
  ⚠️ 词条上的 `scenes` 筛选轴**仍然没加**，也不需要——按场景取句是 `p.scene`，
  用不到词条上的字段。
- 约 70 句 `insight_en` 留空——**这是正常状态，不是待办**。
- 消耗品／竞速道具：**目录、库存与商店已建；效果逻辑与赛前选择未建**（设计定稿见
  `docs/HANDOFF_掌握闸门与消耗品_20260816.md`：词雨 7 件消耗品、攀山快答 3 件道具、
  实时竞技场景一律禁用）。
  ✅ 已建：`CONSUMABLES`/`POWERUPS` 目录 · `store.items`（可叠加持有数）·
  `store.itemSlots` · 营地商店两个分区（十件都能买、能重复买、显示持有数）。
  ⚠️ 待建：**赛前选择界面（3 个槽）** 与 **十种效果各自的结算逻辑**。
  ⚠️ **价格是我定的**（交接文 §3-5 明确留给 owner）：60-120 灵露，
  落在小摆件与中档装备之间——消耗品是用掉的，装备是留下的。单个数字，随时可调。
  ⚠️ **实时竞技天然无道具，不需要开关**（交接文 §3-4 的未决项，已确认）：
  结伴登峰与同伴挑战都走 `WSArena.open()`，arena.js 有自己的渲染器，
  根本不进这两个游戏循环。
  ⚠️ 边界规则：道具**只能改变测试条件（时间／容错／选项范围），不能替代或降低对词义的掌握要求**。
  ⚠️ 已确认（交接文 §2.2 的未决项）：**攀山快答三种题型全部是四选一 MCQ**
  （`SPRINT_MODES` 三种都渲染成 `.sopt` 选项按钮，没有任何输入框），
  所以 **司南（剔除一个错误选项）在所有关卡都适用**，不必分关卡类型。
- **未在真设备上验过的**：iPad 上的滑杆手感、手机海图航行、码头三行注解后的页面高度、
  Chromebook 上 `backdrop-filter` 的性能。

### 路线图
1. 当前构建的用户测试打磨
2. 跨学段掌握继承（已定：自动且对称，载入时按**词文本**取并集）
3. 各模式的弱项优先队列
4. 剩余小游戏（反向打词 · 近义快辨 · 笔顺挑战）
5. 例句恢复（需 CPDD 书面许可 + 登录层）
6. PWA 打包（最后）
7. 前后测评量表（学习成效证据）

---

## 18a. 生活空间：场景底图（2026-08-16）

十张场景图终于**用起来了**（此前入库但代码零引用）。
- **一局只画一个场景**：`startRound` 的 phrase 分支只从**能填满这一局**的场景里挑
  （农场只有 4 句，就永远不当主题，而不是害整局变混）。都填不满时取最丰的那个，跑短一局。
- ⚠️ **场景图是氛围底图，不是舞台**：没有任何互动元素跟它对位，目标词的图在**面板里面**。
  所以十张图原样可用——包括熟食中心那张正中一张大圆桌的，在旧模型下是缺陷，在新模型下不是。
- ⚠️ **压暗压灰只有一个旋钮**（`.xh-scene-bg` 的 `filter`）。哪张偏亮就调它，
  **不要回头改美术**：十张图亮度不一致是必然的，改美术等于把一个参数拆成十份。
- 🐛 **答案本身是 tile-only 的那一句**（熟食中心#5「这里有素食摊吗？」）**至今仍然出不了题**。
  素食摊 不是词条，而 `phrasesFor` 按 `have[p.ask]` 筛选，于是它被**静默丢掉**——
  偏偏它带着全库最有价值的那条文化注解（素食摊与清真摊），
  且 PATCH_02 §4.1 给它 `pic` 正是为了让它可出题。
  ⚠️ **这一段本来写着「现在句库为它单独声明 `askPy` 与 `grp`（地点），渲染时按句子合成
  选项对象」——那是错的，2026-08-17 核实推翻。** 数据里 `askPy`／`grp` 确实在，
  但 `xh.js` **从来没有读过这两个字段**（`grep askPy` 零命中）：功能没做，文档先写了。
  代码里那几处 `tileOnly` 分支（`noteRight`／`noteWrong` 的守卫）是为它写的，
  一直没被走到。⚠️ 这正是**不要从文档推断当前行为**的活例子，而且是在这份
  「今天什么是真的」的文件里发生的。
  ⚠️ **owner 已裁定把 素食摊 加进词表（地点组）**，但**卡在美术上，尚未落地**：
  `摊位` 已经占用 `xh_stall.png`，两个词共用一张图会让 看图识词 出一道无解题、
  让 连线 出两张一模一样的瓦片（与 §5 删「两」同一个失效模式）。**要一张新图才能加。**
  ⚠️ 真加进去之后它**才**开始记进度，那几处 `tileOnly` 守卫届时要重新审一遍。
- ⚠️ **这条 ≥.90 的规则已被 owner 推翻（2026-08-16 晚）**：要求是「面板要真的液态玻璃，
  要看得见场景」。现在用 `.xh-board` 那条同款渐变略微加浓（.78/.62/.66），
  白纱从 .46/.60 放淡到 .26/.38，压暗旋钮从 `saturate(.62) brightness(.82)`
  放到 `saturate(.74) brightness(.90)`。
  ⚠️ **`@supports not` 的纯色回退因此更重要，不是可选项**（现为 .95）：没有模糊时，
  白纱与那层 alpha 就是文字与摊位照片之间仅有的东西。
  ⚠️ 已在 菜市场 上验过；**最亮的那张场景图还没验**，再动之前先看一眼白字。

---

## 18b. 房间：花名册与颁奖台（2026-08-16）

- **玩家行现在带 `av`（头像的文件路径，不是 id）**。⚠️ 必须是路径：`teacher.html`
  没有头像目录，给它 id 查不出文件。规则不校验字段名，所以**不需要改 Firestore 规则**。
- **大厅有花名册**：每个人都看得见同房间其他人的头像与昵称。
  ⚠️ players 的 onSnapshot 以前**一进来就对非房主 return**，所以非房主永远不知道谁进来了；
  现在先更新花名册再判断房主（写 `playerCount` 仍然只有房主能做）。
- **颁奖台 `js/podium.js`**：前三名 + 彩带，**老师投影与学生自己的屏幕同时出现**。
  - ⚠️ 它是 `teacher.html` **唯一**加载的共享 JS。「教师端独立」针对的是
    cs.css/cs.js/profile.js（带学生状态与整套引擎）；一个无依赖、无状态的纯呈现模块
    不属于那一类，把 200 行在两处各抄一份才是真的坏。
  - ⚠️ 它**自建全屏 overlay**（z-index 200），不渲染进调用方的盒子：投影要整屏，
    而且塞进 `.arena-card` 就得跟那层的 z-index 和 scrim 打架。可点背景／按 Esc／按钮关闭。
  - ⚠️ **名次口径跟着各自的排序走**：结伴登峰按分数，同伴挑战按答对数。
    台上显示的数字必须是学生刚刚看到的那个。
  - ⚠️ 彩带会**自己停**（120 片落完或 6.5 秒），关闭时也会 `cancelAnimationFrame`——
    否则投影挂一节课会一直烧 GPU。`prefers-reduced-motion` 下不放彩带、不做升起动画。
  - ⚠️ 只有一两个人时**不摆空台子**；教师端每场**只放一次**（`podiumFor` 闸门），
    否则 `renderArena` 每次 snapshot 重跑都会重放一遍彩带。
  - 底图是落地页那张登山图，压淡到 .55/.68。
    ⚠️ 这条规则在 `cs.css` 与 `teacher.html` 各有一份，**唯一合理的差异是 `url()`**：
    前者 `../art/…`（相对样式表），后者 `art/…`（内联 style 相对页面）。

---

## 18c. 其他（2026-08-16）

- **通用搜索现在是五片陆地上唯一的搜索**：学段页那张 in-stream 搜索卡已删除，
  改成顶栏的 🔎 查词 药丸，和码头同一个位置。
  ⚠️ **已知取舍**：那张卡能显示 释义 与 年级·单元，五站索引不能——索引刻意只带
  词/拼音/英文/所属站 才保持 178KB 并可懒加载。真要按学段看释义，那属于 我的词语表。
- **地景横幅与码头 hero 的深色蒙版已移除**（owner）。文字改用自带的双重阴影
  （`.lp-couplet` 那一招），底部只留一丝渐变。⚠️ 蒙版本来是为可读性存在的，
  要再动之前先在最亮的那张场景图上看一眼白字。
- **横屏海图错落**：G3 上移 5%、HCL 下移 5%，`--ty` 同步移动。
  ⚠️ 这次验算顺带查出一条**本来就存在**的 dock↔HCL 压 G1（与本次调整无关，
  调整前的线上版本同样会压），已用 `SEA_DETOUR["dock|HCL_index.html"]` 修掉。
- ⚠️ **航线改为向下沉入开阔水域**（owner 直接在截图上画出想要的航线）。
  以前是**向上拱**（`-H*0.07`），会从岛带上方穿过；现在向下沉（`+H*0.20`，
  二次贝塞尔实际可见的下沉是一半）。**20 条全部避让通过**，而向上拱的版本有 4 条不通过——
  G2↔HCL 因此**不再需要绕行点**，少一个手工数字。
  ⚠️ **唯一例外 dock↔HCL 仍然上拱**，这是物理限制不是疏漏：owner 画的线从 G1 下方穿过，
  而 G1 painted 底边到 900px 屏底只有 **70px**，船身约 **68px**，穿不过去。
  试过把 G1 抬到 12% 来开出通道：这条确实通了，但 g3↔hcl 与 g3↔dock 反而被 G1 挡住——
  用两条坏的换一条好的，不划算。
- ⚠️ **G1 必须离码头最近**（owner 2026-08-16，距离要读成难度）。
  修的是**把 G2 推远**，不是把 G1 拉近：试过的每一个把 G1 往码头挪的位置，
  不是压到码头美术就是弄断航线。现在 1600×900 下 dock→G1 352 < G2 413 < G3 477 < HCL 803。
  ⚠️ 距离随视口长宽比变化（tx 按宽、ty 按高），在 844×390 这种极端比例下 G1 与 G2 几乎并列；
  笔电与 iPad 横屏（本项目的首要目标）下排序正确。
- ⚠️ **短横屏（手机侧放，高 ≤560px）需要单独一块 media query**：岛按 vw 定尺寸，
  在又宽又矮的视口里 G3 会长得比屏幕还高、山顶被切掉（实测 844×390 时 top = -2）。
  ⚠️ 那一块**必须放在岛的坐标定义之后** —— media query 不增加优先级，
  放前面会被后面的 `.i-g3{--w:26.9vw}` 覆盖，改了等于没改（我第一次就是这样）。
- ⚠️ **竖屏码头美术从 58vw 缩到 48vw**：58 时它的 painted top 到 y=565，
  正好把 G1 的名牌（554-598）盖住。缩码头是唯一不打架的办法：
  G1 抬不上去（G2 的名牌就在它头顶 1px），名牌也抬不上去（会盖掉半座岛）。
- ⚠️ **船头永远指向去处**：航向以前用**弦**的方向算一次，而航线是二次贝塞尔——
  弯航线上弦方向只在中点等于真实行进方向，于是船横着走、靠岸时侧身。
  现在按**切线** `B'(t)=2(1-t)(C-S)+2t(E-C)` 每 90ms 换一次精灵，
  位置仍是纯 CSS transform（动 left/top 正是 owner 报过的 jank）。
  泊位记录的是**抵达时**的航向，不是弦航向。
- ⚠️ **新增 `checkGroupLabels()` 完整性自检**：音节数自检只能抓**错**的拼音，
  抓不到**缺**的（`xhPy` 找不到 key 返回空字符串，是静默的）。
  地点 与 数字 就是这样上线时没有拼音也没有英文的。这个自检遍历真实数据，
  任何 组别/子类 少了任一张表就在控制台告警。

---

## 18d. 拼音 · 朗读 · 版面（2026-08-16 晚）

- **拼音全量审计**：8,359 组 文本↔拼音，音节数 0 错位；读音改了 303 处。
  最大的一类是**副词记号「地」被读成 dì**（214 处），其余：都会 dū 19 · 长 12 ·
  落下 là 9 · 论语大写 6 · 动态助词「了」6 · 名词「穿着」5 · 更深 5 · 行为/行驶 4。
  ⚠️ **名词的「地」一个都没动**：地方/当地/原地/目的地/栖息地/就地/之地/
  翻天覆地/天时地利/地道 全部保持 dì，逐条核对过。
  ⚠️ 错的成因是 `zhPy`/`clozePy` 由 pypinyin 生成，同一个字符串在几个学段里
  重复出现，所以错误是**成片**的，不是零散的。
  ⚠️ **母表现在整列带拼音**：`vocab-lists/*.xlsx` 新建了「释义拼音」「例句拼音」
  两列并填满 2,309 行（这两列本来就是 `add_pinyin_fields.py` 读的覆盖列）。
  重新生成不可能再把 pypinyin 的错误带回来。
  ⚠️ **打量 = dǎ liang**（轻声，词典形），ruby 上会看到没有声调符号的 liang。

- **复习范围是固定高度的滚动盒**（≥900px，`#scopeCard`）：展开 中一 以前会把
  ②③ 整体推下去，左栏每开一级就换一个高度。用 `height` 而不是 `max-height`——
  max-height 仍然会一路长到上限（实测 ② 位移 54px），固定高度才是真的不动。
  ⚠️ 只在横屏。手机上整页就是一栏，滚动盒套滚动盒比长卡片更糟，
  而且旁边没有东西需要跟它对齐。

- **`body` 的背景要自己写 size/repeat**：`applyAmbience()` 只设 `background-image`，
  于是图按原尺寸平铺，宽屏上右边会冒出第二座山。cover 不用 contain——
  背景留黑边看起来像坏了。

- **船头方向：两端拉回到「指向岛」的那条线**（`headingAt`，`nickname.js`）。
  纯切线是物理上诚实的朝向，但航线下沉 `H*0.20`，t=0 与 t=1 处真实行进方向
  与两泊位连线**差 40–95°**（30 条航线实测）：出发时一头扎下去，靠岸时斜着爬上来，
  配 45° 一档的精灵就是「侧身靠岸」。
  权重 `w=|2t-1|`：半程完全信切线（绕行航线只有那里两者才真的不同），
  两端权重为 1，朝向就是两泊位连线。改完 30 条航线的靠岸朝向偏差全部为 0°。
  ⚠️ **没有动航线本身**：下沉量是避让其他岛的依据，而那次避让验算是按
  **美术的实际着色范围**做的，`getBoundingClientRect` 复现不了——
  用元素矩形重算会把线上已验证的值也判成相撞，所以它不能拿来重新调参。

- **码头版面**（iPad 照片）：学习范围八个组改 `minmax(112px)` 两到三栏
  （原来单栏 ~760px，把 ② 推到折线以下）；我的词语表（当时叫 航海图鉴）封面不再被裁成竖缝。

- **传声筒答对后等朗读读完再翻页**，另有 floor / ceiling / 「朗读是否真的开始」
  三道闸门（见 §8、§9 与 `advanceAfterSpeech`）。

- **学段首页三处减法**（owner 2026-08-16 晚）：
  - **游戏卡片不再挂「历练值 / 灵露」小标记**：几乎每个模式两样都给，于是每张卡都是
    同样的两枚药丸，只把卡片撑高、没有区分作用。`REWARD_TAG`/`rewardTag()`/`.rwd-*`
    已删；那张「哪个模式发什么」的对照表留在 `camp()` 上方当注释。
    ⚠️ 要再加回来之前，先想清楚它能**区分**什么。
  - **全选/清空 移到 `#scopeCard` 的标题行右端**，与「已选 N 个单元 · 共 N 词」共用一行，
    省掉一整行高度（卡片是固定高度的滚动盒，行数就是钱）。
    ⚠️ 它是普通 flex 子项，**不是 `position:absolute`**——卡片自己会滚，绝对定位的角
    会跟着滚走。
  - **全选本级 / 清空本级 已删**：一级只有 5-6 个单元，逐个点并不慢，而每展开一级就
    多一行虚线按钮。卡片顶部那一对已经覆盖「全部」与「全无」。
  - **`.card + .section-label{margin-top:18px}`**：`.card` 没有下外距、`.section-label`
    没有上外距，两层玻璃边贴在 0px，而卡片投影还外扩约 8px——看起来就是
    ②选择学习方式 压在 ①复习范围 上。

## 18e. 码头改版（owner 2026-08-16 晚，同一批）

- ⚠️ **航海图鉴 改名 我的词语表**，**航海徽 改名 航海徽章**。
  前者的理由是「零起点学生读不懂航海隐喻」，而且它本来就是山上那块 我的词语表 的同一块屏；
  后者是跟山上统一用词。`XH_PY` 的 key 跟着改了，改名时**两张表要一起改**（§10）。
- ⚠️ **我的词语表不再是集邮**：没了**剪影**（未认得的词原来是自己精灵的黑影 + ？）、
  没了 **全部集齐 印章**、格子换成**列表行**（缩略图 · 词语 · 拼音 · 英文 · 已认得/还不认得）。
  未认得的词是**最值得读的那一个**，把它涂黑是收藏家的吊胃口；而且 §12 已经裁定
  「集齐」对一个会长大的词表是错的框。**不要加回来。**
  「还没认得」一律改成「还不认得」。
- **看图学词 按钮与 全部/已认得/还不认得 同一行**（`.xh-log-learn{margin-left:auto}`）。
- ⚠️ **学词页 ① 从五个题型收成两张大卡**：📖 看图学词（图卡）与 🎯 词语挑战，
  四种题型收在 词语挑战 底下的一行小卡里（**不编号**——它是 ① 的细化，不是新的一步）。
  和山上 学习 的 词语闪卡 / 学习挑战 同形。`store.mode` 仍存真实模式 id，
  `store.quizMode` 另存最后用过的题型，否则从图卡切回挑战会忘记。
- ⚠️ **我的海滩 不再有 tile**：hero 那张大图就是它的入口（同一批美术、同一条船），
  两个入口叠在一起是重复。剩下三块：**我的词语表 占整行**（`.xh-tile.wide`），
  航海徽章 与 码头风云榜 平分下面一行。
- ⚠️ **航海徽章 tile 显示真的徽章美术**（九张 26px，未得的灰度锁），不再是 🎖️ emoji。
- ⚠️ **英文注解在深色面上一直是失效的**（owner：「too low visibility」）——
  **不是配色问题，是优先级问题**：闸门规则 `body.xh-en-on .xh-en` 是 (0,2,1)，
  而所有覆盖都写成 `.xh-hero-sub .xh-en` (0,2,0)，**一条都没生效**，
  于是英文在深蓝药丸、金色按钮、hero 上全都渲染成浅底用的深灰 #46596A。
  拼音那一侧从上线起就带 `body.xh-py-on` 前缀，所以只有英文出问题。
  ⚠️ **新写任何 `.xh-en` 覆盖都必须带 `body.xh-en-on` 前缀。**
- **hero 的副标题移到自带的半透明深色小板上**（`inline-block`，只包住那三行字），
  数据格从 .42 提到 .62。⚠️ 这**不是**把 owner 撤掉的整块深色蒙版加回来：
  那块是盖住整张沙滩美术的 74%/80%，这块只有文字那么大。
  两者都有 `@supports not (backdrop-filter)` 纯色回退。

## 18f. Owner batch（2026-08-16 深夜）

### 组字成词：字块数量滑杆改成字面数字
⚠️ **滑杆上写 6 块，屏幕上就是 6 块。** 旧规则把滑杆读成「两字词语要几块」，
每多一个字加两块（`asmChipCount() + (len-2)*2`），于是滑杆 6 配四字词语铺 10 块、
滑杆 9 铺 13 块。旁边一直有一行「本题 N 块」的说明，owner 仍然一眼判定是 bug：
**滑杆是量词控件，读数与画面不符就是坏的，一行注解救不回来。**
- 唯一保留的覆盖是**下限 `len+2`**（至少两个干扰字，否则盘面就是答案摊在那里），
  另加一条**按范围内不重复汉字数封顶**（窄范围凑不出 24 块时，读数也要跟着收）。
  两种情况那行说明各有各的说法。
- ⚠️ **已知取舍，owner 已裁定接受**：同一设定下长词的干扰字比短词少，所以长词略容易。
  旧规则正是为了修这一点，但它是用「读数说谎」换来的。
- 实测：G3 全范围 44 轮，每一轮 `.asm-chip` 数目 == 滑杆读数。
- ⚠️ 顺带核实：`组字成词` **没有**重复字 bug（交接文 §2.3 说它有）。
  `target` 是 `split("")` 的数组，重复字各自成块，按位比对，妈妈/星星 都正常。

### 四座山：板块 chip 改成徽章 + 小字
`复习范围` 的板块筛选行从纯文字药丸改成 `BADGE_IMG` 的徽章 + 名字（拼音／英文照旧）。
同一份美术，没有新素材。关掉的板块走灰度（那是**筛选**状态，不是「未得到」）。
⚠️ 布局是 **grid `repeat(auto-fill,minmax(92px,1fr))`，不是 wrap flex**：
flex 让每张卡按自己的文字宽度撑（「生活空间／Everyday life」104px、「核心／Core」66px），
四张卡断成三行参差；grid 给每个板块同样大的盒子。`align-items:stretch` 让同一行等高。

### 出发码头：②③ 回到同一页，学词→学习，闪卡分词语／句子
- ⚠️ **② 又是切换，③ 又在首页**（owner：「follow the mountains … right now the pier
  interface is too clunky」）。②当导航只活了一天：点 学词 打开的配置页要同时承担
  「做什么」和「多难」，零起点学生两下点击之后第一眼看到的是一页滑杆。
  山上从来不这样：② 选 学习/闯关，③ 就地铺出活动卡，设置在**门后面**。
- **③ 的四扇门**在 `ENTRIES`（**不是** `MODES`）：学习 = 词语闪卡 · 词语挑战；
  闯关 = 词海钓鱼 · 连线。⚠️ `词语挑战` 一扇门后面是四种题型，
  和山上 `学习挑战` 里装着 填空/华文解释/英文翻译 同构。四种题型摆在外面正是那个 clunky。
- `renderModeConfig(kind)` 的 `kind` 现在是 **ENTRIES 的 key**，不再是 learn/play 那一侧；
  每扇门只问它自己有的问题（闪卡问看什么卡、挑战问题型+题数+难度、连线问几组）。
  旧的 "learn"/"play" 仍被接受并映射过去，免得残留调用落空。
- ⚠️ **学词 → 学习**（这一侧现在也放句子）；**看图学词 → 词语闪卡**（山上就叫这个名字）。
  两张表（`XH_PY` 与文案）同步改过。hero 副标题仍在用「看图学词」四个字，**旧 key 不能删**。
- ⚠️ **闪卡分 词语卡 / 句子卡**（`store.cardKind`）。句子卡走 `xh_phrases` 的同一批句子，
  **一个字都不挖**：§11 说在「更难」与「更多曝光」之间一律选曝光，整句读完就是曝光最大。
  句子卡**不记航程**（航程是「认得几个词」，读一句话不是）。
  范围内没有句子时（数字）自动退回词语卡，不留一个按了没反应的 出发。
  读完之后的「开始测验」跟着那一面走：句子卡 → 传声筒，词语卡 → 看图识词。
- ⚠️ **两栏等高**（owner）。原来只有右栏 `align-self:stretch`，谁矮谁先结束。
  现在两栏都 stretch，并且为了不让左栏顶到 1074px（右栏就得把三张 tile 撑到 664px、
  封面浮在空卡中间）做了三处减法：③ 用普通 `.xh-mode` 不用 `.big` ·
  ② 的两个按钮改成图标与文字同行（`flex-wrap` + 注解 `flex:0 0 100%`，
  ⚠️ flex 会把子元素 blockify，写 `display:inline` 没用）·
  ①学习范围 在 ≥900px 变成 `flex:1 1 auto` + `max-height:250px` 的滚动盒。
  实测 1280×900 与 1024×768 下两栏像素级等高。

### 码头徽章条：九枚一行 + 计数在旁 + 全部上色
- 原来是 `flex-wrap`，按宽度断成 **8 + 1**。改成固定九列（`repeat(9,minmax(0,26px))`），
  窄屏整排等比缩小仍是一行。⚠️ `height:auto` + `aspect-ratio`，写死 26px 会压成椭圆。
- 「N / 9」移到徽章旁边（`.xh-badgerow`），美术不必再挤两行。
- ⚠️ **首页这一排全部上色**，灰度锁只留在 航海徽章 墙上：和四座山同一条规矩，
  dashboard 是「这套徽章长什么样」的预告，「得到没有」点进去才画。
  旁边的计数已经把进度说清楚了。
- ⚠️ 将来加第十级要回来改列数。

## 18g. 学以致用 · 组词成句 · 未认得优先（2026-08-16 深夜）

依据 `HANDOFF_学以致用与组词成句_20260816.md`（它作废了同日早前的
`HANDOFF_排词成句与传声筒改名`）。

### 传声筒 → 学以致用（容器卡）+ 看句选词（题型）
⚠️ **id 一个字节都没改**：`store.mode` / 记忆模式 / `SHELL_PTS` / `poolForMode` /
`SCENE_BG` 全部按 `"phrase"` 索引，改 id 会让老用户的记忆模式指向空值。**只改了标签。**
- 学习 面现在是 **词 与 句 两层**：词语挑战 = 词的层面（三个题型）·
  学以致用 = 句的层面（看句选词 · 组词成句）。原先 传声筒 混在四个词级题型里，
  是唯一考句子的那个，形状不一致。
- ⚠️ **学以致用 是成语，单看比 传声筒 难读——这是知情的取舍**：它现在与 词语挑战 并列，
  **容器卡允许抽象，题型卡不允许**，拼音与英文副标承担实际释义。
  **不要「顺手」把它改回大白话。**
- ⚠️ `load()` 的 `quizMode` 白名单**移除了 `phrase`**，另加 `useMode`（`["phrase","sort"]`）。
  老用户 `quizMode === "phrase"` 会被重置为 `"pic"`，这是正确的。
  ⚠️ **一扇门一个记忆位**：`ENTRY_MEM` 把 quiz→quizMode、use→useMode 分开，
  以前那个无条件写入会让两扇门互相覆盖。
- ⚠️ **两个题型是两张卡，不做局内切换**：看句选词 答对后会显示整句，
  当场切到 组词成句 拿到的不是提示，**是完整语序**，而语序正是 组词成句 唯一考的东西。
  ⚠️ **不要误读成「码头不能有局内切换」**：组字成词 的 释义/英文/填空 是安全的，
  它换的是**题干**不是**机制**，答案自始至终同一个词。

### 组词成句（新玩法 `sort`）
「按顺序点出**句子的词**」，是 组字成词「按顺序点出**词语的字**」的同一手势升一级，
版面与交互照抄，学生不必学第二套操作。
- ⚠️ **点选放置，不做拖拽**：触屏拖放要跟 `touchmove` 与滚动打架、长按选字、
  拖一半松手。点选在 组字成词 与头像选择卡上都验证过。
- ⚠️ **每块的身份是 `seg` 的下标，判定比对的是文本序列**——两块同文本可以互换，这是对的。
  实测「我喜欢我的学校」盘里两个 我，可解。
- ⚠️ **前缀锁定，不是全对全错**：对的格变绿锁住，从第一个错位起其余退回；
  提示写明锁到第几格。学生看到的是**语序在哪一段断掉**。
  ⚠️ **答错不扣任何东西**，也**不做「最少移动步数」评分**（那是拼图玩家的指标）。
- ⚠️ **不显示贴图**：看句选词 需要图是因为 `这包＿＿多少钱？` 没图就无解；
  这里所有词都在盘里，答案没有歧义，加图只会挤版面并把注意力从语序引开。
- ✅ **拼块上现在有拼音了**（owner 2026-08-19 审毕回填，见 §18aa）。
  ⚠️ 上一版这里写着「没有拼音是正常状态」——**已经不成立**，但它当初的理由仍然成立：
  运行时生成拼音是 §8 明令禁止的，所以这份数据是**手工审过**的（202 个不重复词块，
  owner 全部过目），不是算出来的。
- 干扰词块：自己的常量 `SORT_EXTRAS = [0,2,4,6]`，⚠️ **不复用 `OPT_TIERS`**
  （那是「屏幕上几个选项」，语义不同）。⚠️ **最低档 0（刚好够）是真档位**：
  零起点学生第一次排八个中文词本身已经是最难的部分。
  干扰词取自 `ask` 的**同一个组别**，且排除已在句中的词。
- `SHELL_PTS.sort = 2`（产出型，与 词海钓鱼 同级），`phrase` 维持 1。
  ⚠️ 费率不同是可以的，因为它们是两张分开的卡；**若日后改回局内切换，费率必须先并成一个**。

### ⚠️ `seg` 是 owner 侧交付物，代码只读不写
`data/xh_phrases.json` 每条**可选**的 `seg` 数组（词语级切块，固定短语整块不拆）。
**数据已于 2026-08-16 深夜交付并合并：90 句里 73 条有 `seg`**（`SEG…v3`）。
未切的 17 条是**正常状态，不要补切**：7 条 display-only · 9 条含逗号（MVP 排除）·
1 条 `老师来了！`（只有两块，区分度过低，且 `来了老师` 在特定语境也成立）。
⚠️ **绝不在运行时分词**：码头是扁平文件无构建步骤，更重要的是
**切块方式本身就是教学决定**（`我/每天/搭/巴士/去/学校` 与 `我/每天/搭巴士/去学校`
是两节不同的课）。
代码里有校验，**不合格的句子自己出局**：拼回去逐字等于去掉句末标点的 `zh` ·
`len>=3` · 块内无标点。
⚠️ **「`ask` 必须独立成块」这条已废除**（PATCH_03 §3B）。它在第一版数据里造成了实际损害：
为了让 `ask=汤`/`ask=咖喱` 成块，`鱼汤` 被拆成 `鱼/汤`、`咖喱鸡` 被拆成 `咖喱/鸡`——
**为了校验器好看而拆坏了词**。`ask` 只服务 `store.done` 与未认得优先分桶，
字符串相不相等不影响任何功能。现在有三条刻意这样发布（樟宜机场 · 鱼汤 · 咖喱鸡）。
⚠️ 日后真要建 `ask ↔ 拼块` 映射，**另设显式字段，不要靠字符串相等去猜**。

### ⚠️ `seg` 的顺序只是「其中一个正确答案」（PATCH_03）
**学生排出的句子只要在当前题面下自然通顺，就不得判错。** `农夫在喂鸡和牛` 与
`农夫在喂牛和鸡` 一样对，判错是**缺陷不是严格**。
- `accepted_zh` **存字符串不存拼块数组**：第二份数组是同一批拼块的副本，
  不同步时没人发现；字符串只在判定时比对，不可能不同步。
  ⚠️ **不写原句**（`seg` 已经提供），**不含句末标点**。
- ⚠️ **每条 `accepted_zh` 必须能由该题拼块恰好用完拼出**，否则就是一个学生
  **永远拼不出来**的答案，比没有更糟。7 条全部验证通过。
- ⚠️ **前缀锁定要对所有合法答案打分，取最长匹配的那个当参照**。
  死盯 `seg` 会让正在往替代答案方向排的学生眼看着对的一段被退回，**比全对全错更困惑**。
  实测 `scene_aquarium-5`：某个排列对 `seg` 匹配 0 块、对 `accepted_zh` 匹配 3 块，
  锁定取 3。
- ⚠️ **多解扫描是筛子不是保证**：按「和／比」扫描只命中 4 条，实际有自然替代答案的是 7 条。
  **新增句子仍须人工过一遍语序，不要为了「0 warning」去改自然中文。**
⚠️ **句末标点 `。？！` 是固定装饰**，印在末格右边，不打乱、不可点、不占一格。

### ⚠️ 未认得优先（`weakFirst`）——这是**全码头**的改动
`startRound` 以前是 `shuffle(draw).slice(0, need)`，纯随机不分桶。已经认得 80 个词的学生
一轮五题可能一个新词都碰不到，而**航海徽章的九级阈值直接数 `store.done` 的条数**，
于是徽章进度卡住。山上从第一天起就有这个逻辑（`cs.js startQuiz`），这里照抄。
- 桶的 key：词序列用 `store.done[w.词语]`，句序列用 `store.done[p.ask]`。**桶内打乱**。
- ⚠️ **两处豁免**：`learn`（按数据顺序走完整组，是一课不是抽样，山上的 `flash` 同样豁免）·
  `distractors()`（收窄干扰池会毁掉题目）。
- ⚠️ **在 子类 主题筛选之后**分桶，否则先选词后选主题，破坏 `SPEC_XH_vocab_v3 §6`。
- ⚠️ **句模式是「场景内」生效**：先在能填满回合的场景里挑**未认得句子最多**的那个，
  再在场景内分桶。场景约束优先，否则跨场景取题会破坏单一背景图。
- ⚠️ **一个诚实的偏差，不要「修」它**：组词成句 教的是**语序**，
  而 `store.done[p.ask]` 记的是**词**。学生可能已经认得 图书馆 却从没排对过那句话，
  分桶会当成复习。精确做法要新建一份「排对过的句子」记录，而本文件明写着
  **先加词，别再加系统**。**不要为它新建 store 字段。**

### 🐛 修掉一个静默已久的 bug：看句选词 一直不发奖励
`SAIL_PTS` 与 `SHELL_PTS` **都没有 `phrase` 这个 key**，
`SAIL_PTS[mode] || 0` 返回 0，`awardSail` 下一行就 `return`——
所以 传声筒 从上线那天起**既不给航海值也不给贝壳**。
不报错，因为「缺 key」和「故意写 0」在这里长得一模一样。
现在补上 `phrase: 3 / 1`、`sort: 4 / 2`。
⚠️ **`learn: 0` 是这两张表里唯一有意的零。凡是会出题的模式都必须显式列在这里；
加新模式时两张表一起加。**

### 板块 · 来源：折叠成一个「筛选」块
⚠️ **默认收起并记忆**（`store.filtOpen`）。两者都是「设一次就不再动」的收窄控件，
而 板块 chip 换上徽章美术之后是这张**固定高度滚动盒**里最高的东西——
①真正要给的 单元 反而被挤到折线以下。
⚠️ **收起时标题仍然写明生效的事实**（来源 与 板块 N/M）：折叠可以藏控件，**不能藏事实**，
否则上周只留一个板块的学生看到词数不对却找不到原因。
⚠️ chip 是 class 翻转不是重render，所以 `updateFiltSum()` 必须跟着调用，
不然标题会停在 4/4。
⚠️ **四个板块排 2×2**（G2/G3）：auto-fill 按宽度装，三列会排成 3+1，最后一张落单。
列数按 stream 固定，用 class 发出去，**不用 `:has()`**——G1/G2 学生正是老 iPad 的用户。

### ⚠️ 交接文里两条**与线上不符**的断言（已核实，不要照着改）
1. **`组字成词` 没有重复字 bug。** 两份交接文都写它「至今有一个已知缺陷：
   重复字被去重，学生凑不出答案」。实测 60 轮、20 个重复字词
   （叽叽喳喳 · 瓶瓶罐罐 · 干一行爱一行 · 吞吞吐吐 …），**每一个的块数都对、每一个都能解**。
   `target` 是 `split("")` 的数组，重复字各自成块，按位比对。**没有东西要修。**
   （新玩法仍按下标建块——那条规则本身是对的。）
2. **`沙滩快跑` 交接文 §2.4 的库存**：清点时是 150 词、**≥2 字的 101 个**。
   ⚠️ **整鸡 并入 鸡肉之后是 149 词 / 100 个**（食物 30 · 动物 17 · 地点 15 ·
   日常用品 14 · 学校 12 · 交通 8 · 天气与自然 4 · **数字 0**），见 §18i。
   数字组全是单字，组字成词 对它必须整组灰掉。

## 18h. 码头对齐四座山 · 看句选词 三处（owner 2026-08-16 晚，第二批）

owner 报的原话：「码头有 返回 和 海图 两个按钮，和山上不一样，很混乱……
把码头的控件对齐山上，另外把其他会让学生困惑的地方也找出来。」

### 一个返回控件，在顶栏，随处境改去处
山上从来只有**一颗**返回键（`cs.js setTopbar(backTo)`）：在学段首页去海图，
在玩法里回首页。码头却是**顶栏一个「← 海图」+ 回合条里一个「‹ 返回」**，
两个位置、两个去处。
- 现在 `quitBtn()` **返回空字符串**，`wireQuit()` 改成把**顶栏那一颗**指向 `renderMenu`。
  ⚠️ 函数**保留**没删：它的十几个调用点是对的（「这个屏幕可以离开」），
  删掉等于同时改十几处而毫无行为变化。新屏幕照旧走它。
- ⚠️ 顶栏那颗仍然是 `<a href="index.html">`：JS 挂了也还能回海图，不会变成一颗死键。
- ⚠️ **文案是「‹ 回码头」不是「返回」**（owner 问「码头该用哪个词」）：
  山上那句 `回营地` 之所以好读，是因为它**说出你会落到哪里**；返回 只说了会倒退。
  ⚠️ 山上的顶栏箭头没有文字、`回营地` 在内容区按钮上；码头保留文字，
  因为码头的学生正是最需要每个控件都带注解的那批人。**这个差异是刻意的。**

### 顺带对齐的三处
- **中EN / 拼拼音 的顺序**：`cs.js` 是 en 在前，码头是 py 在前。已对调。
- **码头补上 意见反馈 悬浮按钮**：山上每一屏都有（`ensureFab`），码头**一个入口都没有**——
  最读不懂字的那批学生反而没有报错的地方。同一个面板、同一份额度。
  ⚠️ `.fb-fab` 的样式**在 cs.css 与 xh.css 各一份**（§17 那种刻意重复），改一处要改两处。
  ⚠️ 码头**不隐藏**它（山上在词雨/攀山快答时隐藏）：码头没有任何限时玩法，
  词海钓鱼 的鱼是按答题上升不是按时钟。**沙滩快跑 上线后要在那里隐藏它。**
  ⚠️ `WS_FEEDBACK_CTX` 从 `state` **现算**，不在每个 render 里推送：
  码头有两种题目形状（词行／句行），每个渲染点写一行 setter 是下一个新玩法一定会忘的那行。
- **顶栏副标题改成「词山学海 Vocab Summit · 学海起步」**：原来写的是
  「学海起步 · 看图学词」——既是**一个已改名的玩法**（现叫 词语闪卡），
  又把「学段」那一格填成了活动名。海图上码头岛的 `.lv` 同样从 看图学词 改成 学海起步
  （它的 aria-label 本来就是对的）。
- **六个屏幕删掉只剩一个标签的回合条**（配置页 · 航海徽章 · 我的海滩 · 海滩小铺 ·
  我的词语表 · 码头风云榜）：返回搬走之后，那条只剩一个标签，而**面板标题就在下一行说同样的话**。
  留着回合条的是**条上有别的东西**的那几个：航程条，或 连线 的「范围 · 连线 N」。

### 看句选词：英文闸门 · 面板下移与玻璃 · 朗读改按钮
- **英文译文进闸门**（见 §10）。
- **面板下移 + 真玻璃**（见 §18a 的改写）。
- ⚠️ **答对不再等朗读读完才翻页**（owner：「想快的学生不该被朗读拖住」）。
  `advanceAfterSpeech` 从两个句子玩法里移除，改成**固定停留**
  （看句选词 1400ms／有文化注解 2400ms；组词成句 2200／3000）。
  朗读没有消失，**搬到了句子旁边的一颗 🔊**，答题**前后都能按**。
  ⚠️ **那颗喇叭读的是挖空后的题干**（`p.zh` 里把 `ask` 换成「，」，
  和 `cs.js` 的 `speakCloze` 对 `__` 用的是同一招）。直接读 `p.zh` 就是**把答案念出来**（§8.7）。
  ⚠️ **组词成句 的喇叭只能出现在答对之后**：那个玩法考的就是语序，
  提前读整句等于把答案给了。
  ⚠️ 答对后**不再自动朗读**：这时候起的一句会一路盖到**下一题**的句子上。

### 四座山：板块筛选默认全开
`清空` 以前**连板块一起关掉**——源码里对称，效果上不对称：单元已经清空了，
范围本来就是 0 词，多关的那一下什么也没买到，却留下一个陷阱：
学生再选一个单元，还是 0 词，而解释原因的 筛选 块**默认是收起的**（§18g）。
现在 `清空` 只清单元，另加 `repairComps()` 开机自愈：
**整个 stream 的板块全部关闭 = 损坏，不是选择**（没有人会选「什么都不学」）。

---

## 18i. HANDOFF_XH_沙滩快跑 落地（2026-08-16 晚）

`~/Downloads/XH_沙滩快跑_20260816/`。§1–§6 全部落地；§7 的构建顺序照走。

### 组字成词（§2）
「按顺序点出**词语的字**」——组词成句 手势往下一级，版面照抄，学生不学第二套操作。
- ⚠️ **≥2 字才进池**（§2.4）：149 词里合格 **100 个**
  （食物 30 · 动物 17 · 地点 15 · 日常用品 14 · 学校 12 · 交通 8 · 天气与自然 4 · **数字 0**）。
  数字 整组灰掉，**理由文案是这一扇门自己的**（「这组没有两个字以上的词语」），
  不是那句写死的「这组没有图片」。
- ⚠️ **拼块按下标建，重复字各自成块**（§2.3）。码头库里 **星星** 就是这个例子，
  实测 40 题里抽到并解开了。
  ⚠️ 交接文说「这是 词山 组字成词 已知的那个 bug」——**山上没有那个 bug**（§18f 已实测 60 轮）。
  规则本身是对的，只是它引用的前提过时了。
- ⚠️ **干扰字取自同一 `组别`**（§2.5），且**排除目标词自己已有的字**：
  多一块「妈」不是更难，是两块都对而学生不知道为什么。
- ⚠️ **答题前没有任何喇叭**：图是题，词是答案，读出来就是给答案；
  逐字读同样等于把答案按顺序念一遍。答对后由 `noteRight` 读一次。
- `SAIL_PTS`/`SHELL_PTS` = 4 / 2（§2.6，产出型，与 词海钓鱼 同级）。

### ③难度：一根滑杆管所有题型（§3）
⚠️ **结构照交接文，数值用 owner 的**（owner 2026-08-16 晚当面裁定）。
`DIFF_OPTS [2,3,4]` · `DIFF_MATCH [3,5,8]` · `DIFF_SORT [0,2,4]` · `DIFF_BUILD [1,2,4]`，
一律按 `[简单, 中等, 挑战]` 排。交接文 §3.2 给的是 连线 3/4/5、选项 3/4/6——
那些是在真盘面上调出来的数字，**8 组的连线板就是它的上限**，所以只借结构不借数值。
- ⚠️ **唯一的代价：组词成句 的 +6 档没了**，没有第四个难度可以安放。
- ⚠️ **`OPT_TIERS` / `MATCH_SIZES` / `SORT_EXTRAS` / `BUILD_EXTRAS` 四个常量已删除**，
  不是留着不引用：留一个 `SORT_EXTRAS` 在那里，下一次改动就会去拿它，
  然后两套难度模型同时在跑。
  ⚠️ `store` 里那四个字段**保留但永不读取**，它们是「学生当初选了什么」的唯一存档。
- ⚠️ **`store.diff` 从 `optsN` 迁移**，且在 `optsN` 的归一化**之前**判断：
  老档案带 2/3/4 直接映射，新档案什么都没有——不能继承旧的默认值 4，
  否则新生的**第一盘连线就是 8 组**。新生落在 中等。
- ⚠️ **滑杆下面必须写出这一档在这个玩法里是什么**（「一次连 5 组」）：
  三个抽象档位而不说买到了什么，正是 §18f 重写 字块数量 的原因。

### 沙滩快跑（§1）
⚠️ **这一节的「世界卷动 · 跑者固定在 26% · 潮水」已于 2026-08-17 整段作废，见 §18l。**
下面保留的是仍然成立的部分。
**一层皮，不是第二条管线。** 整段代码不出题、不判分、不发货币、不写 store。
- ⚠️ **它是「模式」不是第六个泊位**：配置页上多一行 ①模式，
  同一盘 看图识词 两种模式下**题目、贝壳、航程完全相同**，只有反馈不同。
- ⚠️ **位置由 `state.i` 推算，绝不累加**。按答对次数累加，只要有一个玩法走了不调
  钩子的路径（看句选词 的 tile-only 那一句就是），跑者就会**早一格或晚一格**到终点。
  位置是「现在第几题」的纯函数，所以算两次也不会自相矛盾。
- ⚠️ **没有 rAF、没有 canvas**：一题一步，本来就是离散的，CSS transition 就是全部动画。
  这也让它不上老 iPad 的合成器——G1/G2 用的正是那批机器。
- ⚠️ **没有失败条件**（§1.4）。**以后不要给它加失败条件**，也不要拿新的潮水美术
  把追赶感做回来（潮水本身已随美术一起撤掉，见 §18l）。
- ⚠️ **题型闸门看的是「一次作答是不是一个事件」**，不是难易：
  组字成词 与 组词成句 点很多下，但**作答事件只有一个**（检查答案），所以可以跑；
  连线 是一整盘、词海钓鱼 被键盘挡住，所以不行。
  ⚠️ **灰掉不隐藏**（§1.5），且**记忆里的 surf 不会在被封锁的玩法里建出海滩**。
- ⚠️ **零新美术**（§1.6）：复用 攀山快答／垂钓 同一批 6 帧精灵（`art/sprite/avatar/*_sprite`）。
- ⚠️ **band 是 `#xhView` 的兄弟节点，普通文档流**：每个渲染器都 `innerHTML =`，
  做成子节点就会被抹掉并重启所有 transition；做成 `position:fixed` 又要知道顶栏高度，
  而顶栏高度**随拼音／英文闸门变化**——正好对开了两个闸门的学生就会错位。

### 美术管线（§6）——`local-admin/process_run_art.py`，产物在 `art/xh/run/`
⚠️ **顺序就是全部**：① 网改色 → ② 擦水印 → ③ 抠图。
- ⚠️ **没有一张的洋红是 `#FF00FF`**，连交接文说是 ChatGPT 出的那几张也不是
  （props 249,4,241 · shell 246,4,235 · marker 253,29,248 · finish 255,53,255）。
  底色**逐张实测**，永不假定。
- ⚠️ **晾网架的网：magenta 的不只是网眼，是网绳本身**（220,16,198 → 123,31,110 一片深浅）。
  容差够宽到吃下底板抖动，就一定会把网吃掉，只剩几根绿色高光——一个网的鬼影。
  按亮度映射成哑光海绿**之后**再抠。
- ⚠️ **Gemini 星芒水印：四张里只有三张有**（foam_lap · run_marker · run_finish），
  一律在**距右下角 47px** 的 24×24 处。**wave_chase 那张查不到**，交接文 §6.3 说「四张都有」
  这一条不成立。
- ⚠️ **§6.1 有两条指示被拒绝并写明理由**：沙滩「裁成 2 的幂宽度」会**弄断它本来接得上的缝**；
  路标「缩到 64px」是 §14 的像素画缩放陷阱，码头每一张精灵都是原尺寸交给 CSS 缩的。
- ⚠️ **海面波带交付时就接不上缝**（18.5，沙滩同一指标只有 4.5）：已用环绕融合修到 0.7。
- ⚠️ **踏浪四帧逐帧配色**：整张一起调会保留帧间色差，四帧循环起来是**一闪一闪的色相跳动**。

---

## 18j. 注解可读性 · 反馈表单 · 答案大字（owner 2026-08-16 深夜）

- ⚠️ **🔊 按钮里的拼音与英文一直是深色压深蓝**（owner：「G1/G2 蓝色按钮里很难看清」）。
  `.tts-lab .pylab{color:白}` 那一行**从来没生效过**：它是 (0,2,0)，而
  `body.py-aid .pylab` 同样 (0,2,0) 且在文件更后面。已挪进 cs.css 那个
  **深色面板专用块**（`.harbour` / `.rain-hud` / `.home-foot` 旁边），
  并按 §7 用浅色 `rgba(232,243,250,.92)`。实测 display:block、颜色已生效。
  ⚠️ **任何新的深色面上出现注解，都要加进那个块**——这是它存在的理由。
- ⚠️ **意见反馈表单现在跟随拼音／英文闸门**（码头与 G1/G2/G3）。
  `profile.js` 被两个家族同时加载，而两边闸门不同（`.pylab/.enlab` + `body.py-aid`
  对 `.xh-py/.xh-en` + `body.xh-py-on`），所以每条注解**同时挂两套 class**，
  哪张样式表在就认哪一套。HCL 不需要特例：它从不设 `body.py-aid`。
  ⚠️ **那段很长的同意声明只给英文、不给拼音**：它是散文不是标签，
  整段拼音既是噪音，也会让每次改字都掉进音节数陷阱。
- ⚠️ **额度数字已从表单移除**（见 §16）。
- ⚠️ **看图识词 / 英文选词 答对后，词语大字显示在精灵右边**（owner：不要在下面放小字）。
  `.xh-sprite` 从 `display:block;margin:0 auto` 改成 `inline-block`——它本来是一块
  **整幅面板宽的木牌，图片挂在最左边**，右边一大片空白正好放答案。
  56px 衬线大字 + 拼音 + 英文（后两者仍走闸门）。
  ⚠️ **听音识图 不套用**：那里选项本身就是图片，没有精灵位。
  ⚠️ 窄屏（≤560px）下改成图上字下，仍然是大字。
- ⚠️ **码头 换一组 回到该活动的设定页，不再回码头首页**（owner）。
  `entryForMode()` 是 `entryModes()` 的反查。
  ⚠️ `renderModeConfig()` 开头加了 `runTeardown()`：海滩是 `#xhView` 的兄弟节点，
  不清掉的话从跑完的一局回设定页，整片海滩会留在设定面板上方。
  ⚠️ **已知小瑕疵，留给 owner 定夺**：按钮still 叫「换一组」，而**组**是在首页选的，
  设定页只能换题型与难度。要么改标签，要么接受。

---

## 18k. Owner batch（2026-08-16 深夜，第三批）

- ⚠️ **四座山 ③：词语闪卡 排在 学习挑战 前面**（owner：「learn then test」）。
  左到右读，教的那张必须在考的那张之前。和码头 ③ 的顺序一致。
- ⚠️ **码头 换一组 已删除**（owner：「和 回码头 走同一条路，那就删掉」）。
  连带把返回做成**三级**：海图 ← 码头首页 ← 活动设定页 ← 一局。
  `wireQuit()` 用 `state && state.seq` 判断「在不在一局里」——所有辅助屏
  （徽章／海滩／小铺／词语表／风云榜／设定页）进入时都 `state = null`，自然落到 回码头。
  标签跟着层级走，永远说出去处：`‹ 连线` / `‹ 回码头` / `← 海图`。
  ⚠️ **不要再往结果页加第二个出口。** 再来一次 不算——它留在这一局里。
- 🐛 **海图：停在哪座岛就点哪座岛，船原地不动 1.7 秒再跳页**（owner：「sailing animation
  is gone, the boat just teleports」）。船会停泊在上一次的目的地，所以再点同一座岛
  等于要它航行**零距离**：`--dx`/`--dy` 实测 `0.0px`/`-0.0px`，曲线塌成一个点。
  现在位移小于一个船身就**直接进站**。
  ⚠️ 这不是边角情况，而是海图上最常见的一次点击：从学段页回来，再点同一座岛。
- 🐛 **数字在 连线 里是五张空白卡**（见 §5 的 `img()` 改动）。
- 🐛 **猪肉／牛肉 会同时出现在 连线 板上**（见 §5 的黑名单改动）。
- ⚠️ **「两」已从词表删除**（见 §5）。

---

## 18l. Owner batch（2026-08-17）：精灵大小 · 沙滩快跑重做 · 两个商店

### ⚠️ 六帧精灵表**不是等大画的**——`WSProfile.spriteScale()`
owner：「Can you make the human figures and animals avatars the same size? 沙僧 is way
smaller than the rat.」——**确实如此，而且所有渲染点本来就已经用同一个高度**。
问题在美术：格子高度统一是 104，**画进格子里的东西不统一**——沙僧 画了 41×72，
鼠 画了 101×92，同高度下鼠盖住的像素是沙僧的 2.3 倍。五个西游记角色又瘦又矮，
十二生肖填满格子。**按格子定尺寸，就一定不等大。**
- 表在 **`js/profile.js` 的 `SPRITE_SCALE`**，由 `local-admin/measure_avatar_scale.py`
  实测生成，取 `WSProfile.spriteScale(id)`。⚠️ **不要手改单独一行**，改 `REF` 一个数
  就能整体放大缩小。未知 id 返回 **1，不是 0**。
- ⚠️ **度量是「画出来的质量」（六帧 alpha 面积的平方根平均），不是画出来的高度**。
  按高度归一是直觉答案而且是错的：蛇 与 龟 本来就画得又低又宽，配高度会被撑成巨兽。
  质量把「高瘦的和尚」和「矮宽的乌龟」判成一样大，这正是眼睛的判法。
- ⚠️ **美术一个字节都没动**。这些表在屏幕上本来就是非整数倍缩放的（CSS 高度），
  再去重采样源文件就是 §14 的像素画缩放陷阱，白赔画质。
- ⚠️ **攀山快答不走这张表**，它 2026-08-16 就用 `avatarInk()` + `AVATAR_INK_H`
  在 canvas 上运行时量 ink 解决了同一个问题（外加 42% 宽度上限）。canvas 能实时量，
  背景图不能，所以 DOM 那三处（词雨跑者 · 垂钓渔夫 · 踏浪跑者）用实测表。
  **两套写法，一个意图；改任何一套之前先看另一套。**
- ⚠️ **放大之后要重算余量**：`base × k × (1 + 抬升 − inkTop/104) ≤ 可用高度`。
  垂钓面板只有 58.6% 在木板之上，而沙僧的庆祝帧画满整个格子——**欢呼的抬升因此
  从 18% 降到 5%**，基准高度是 **38%**（不是屏幕上的最终高度，乘 k 之后才是）。
  踏浪跑者基准 **50%**，上限来自 46% 的跳跃弧。

### 沙滩快跑：跑者动，沙滩不动（作废 §18i 的卷动模型）
owner：「can we see the avatar run across the beach instead of having the beach items
move to the left?」——原来是无限跑酷的标准做法（世界卷动、跑者钉在 26%），
读起来是**布景在往左滑**，不是**我走了多远**。现在整片沙滩是一张定住的画，
`--rx` 把角色从 **11% 走到 82%**，终点拱门在 **86%**，一局的进度就是走过的距离。
- ⚠️ **一题一跳，弧线抄 攀山快答**（owner：「beach obstacle run with jumping similar
  to sprint climbing」）。`left` 的过渡与 `.hop` 的弧线**同为 .62s**，弧顶落在 50%，
  所以正好越过摆在**跳跃中点**的路标。空中用第 3/4 帧（攀爬帧，表里唯一的离地姿势）。
- ⚠️ **这一步在答题路径里迈出，不在下一次 `render()`**：`runPaint` 要等下一题画出来
  （1400–3000ms 之后）才跑，弧线早就落地了。`runX(i,n)` 是纯函数，所以答题时算一次、
  render 再算一次也不会打架。
- ⚠️ **路标改成跨栏，而且每 k 跳才有一个**（k = ceil(n / 6)）。20 题把跑道切成 3.4%
  一格，800px 宽的带子上就是 28px 一根柱子，柱子比这还宽——会连成一道栅栏。
  k 让柱子始终 4–6 根、始终原尺寸。**没有栏可跨的那几跳照样是跳，读起来就是奔跑。**
- ⚠️ **最后一跳永远没有栏**（`i < n - 1`）：栏会正好长在拱门的左柱里，
  而零起点学生最后看到的应该是一条通向终点的空路。
- ⚠️ **风景道具退到 bottom 22–26%、缩到 16%**，且**不站在跑道线上**：
  棕榈和小船不是小孩会跳过去的东西，把它画在跳跃中点等于承诺一个弧线兑现不了的跳。
- ⚠️ **两层会动的浪已整个删除**（owner：「remove the animated waves - need to
  regenerate as it is janky and has magenta not removed」）：`.xh-run-foam`
  （`xh_foam_lap.png`）与 `.xh-run-wave`（`xh_wave_chase.png`）都用 `steps(1)` 跳帧，
  两张图各自还留着约 9,000 个洋红去溢余像素（浪尖上的 255,221,255，§14「按颜色找水印」
  同一族的疏漏）。**美术留在 `art/xh/run/` 但代码零引用**，等重画。
  ⚠️ **重画好之前不要接回去**，而且要**先验去溢**再接。它驱动的潮水是纯反馈（§1.4）、
  从来不是失败条件，所以整个玩法不依赖它。
- 🐛 **顺带修掉天空的接缝**：世界不卷动之后，`xh_run_sky.png` 那道 26.3 的接缝
  **永远停在同一个位置**，读起来像天上裂了一条缝（以前它会滑过去）。
  已在 `process_run_art.py` 里对天空也做环绕融合，26.3 → 1.7。
  ⚠️ 顺带把海面重跑了一遍（`SEA_REF` 取自天空底部 90 行，天空一改它就跟着微调），
  最大差 14，属预期。

### 两个商店改成 Pikmin Bloom 式（`海滩小铺` 与 `营地商店`）
owner 给了 Pikmin Bloom 的截图：「no rectangular panels framing each item,
take ref for currency icon and text size too」。
- ⚠️ **每件商品外面不画框**。原来码头是玻璃卡片格、山上是带边框的横排——
  框子在跟它框住的美术抢注意力，而**美术是零起点学生唯一不费力就能读懂的部分**。
  现在是：大图 → 名字 → 一枚小货币图标加数字。
- ⚠️ **整张卡片就是按钮**（`button.xh-sitem` / `button.shop-tile`），
  绝不是 div 里再放 button（§14 按钮嵌套按钮）。每件商品**同时只有一个动作**
  （买 / 摆上 / 开这艘），所以一张卡一个按钮就够。
  ⚠️ **没有动作的状态渲染成 `<div>`**：「正在开」是事实，不是一个变灰的报价。
- ⚠️ **原来当边框用的状态色改成一个词 + 图后面的一层柔光**。手臂长度上看，
  「已装备」的框和「这是一件商品」的框长得一模一样。
- ⚠️ **图坑固定高度、宽度自由**（`.xh-sic` / `.shop-ic`）。写死宽度会把长条形的东西压扁。
- 🐛 **贝壳画成了 62px**（owner：「the seashell currency image need to be way
  smaller」）：`.shell-icon{width:17px}` 是 (0,1,0)，输给了 `.beach-card img{height:62px}`
  的 (0,2,0)。**不是配色，是优先级**，与 §18e 英文注解那次同类。现在货币芯片的规则
  写成 `.xh-scost img` / `.shop-cost img`，比卡片的图片规则更具体。
- 🐛 **帆船比舢板小**（owner：「the junk boats need to look larger than the sampan」）：
  统一高度把 285px 高的 简朴帆船 压得**比 117px 高的 舢板还窄**。现在用
  `max-width/max-height` 让原始比例自己说话，帆船约为舢板的两倍高——美术本来就是这么画的。
- ⚠️ **持有数是图上的角标**（`.shop-n`），不再是副标题里的一句「持有 3」：
  消耗品是两个商店里唯一能重复买的东西，而埋在副标题里的数字正是学生买第四个之前会漏掉的那个。
- ⚠️ `.beach-tag` **属于航海徽章墙，不属于商店**，改版时差点被一起清掉；
  它现在挪到 `.sailbadge-*` 旁边了。
- ⚠️ 两份样式**各自一份**（`cs.css` 与 `xh.css`），是 §17 那种刻意重复：**改一处要改两处。**

### 词海钓鱼：渔夫从 30% 提到 38%（×k）
owner：「the student avatar is way too small compared to the fishing rod and the
question sprite」。原来 30% 是**格子**高度，沙僧 只填到格子的 69%，
于是屏幕上是 55px 的人配 95px 的鱼竿和 130px 的海豚。现在基准 38% 再乘 `spriteScale`，
一般的表落在「约 52%」，画出来的人差不多与鱼竿等高。
⚠️ 数字是**在美术上量出来的**（PIL 按同一套百分比重合成 + 离屏 WebKit 复核），不是估的。

---

## 18m. 码头调整（HANDOFF_码头调整_20260817，owner 口述）

依据 `docs/HANDOFF_码头调整_20260817.md`。那份交接文把 ①③ 的摆法列成两套读法并要求
先问 owner——问了，答案在下面。

### ③ 重新分家：闯关只剩「真的是游戏」的两个
owner：「play mode for pier should only show fishing and beach run」＋
「连线 组字成词 live in study mode instead of game mode」。

| ② | ③ 的门 |
|---|---|
| 学习 | 词语闪卡 · 词语挑战 · **连线** · **组字成词** |
| 闯关 | 词海钓鱼 · **沙滩快跑** |

- ⚠️ **上面这张表已于 2026-08-17 当天被 §18p 推翻**：学习 现在只有 **词语闪卡 · 学习挑战**
  两张卡，连线 与 组字成词 是 学习挑战 里 配对与拼字 那一排的两种题型。
  「连线 与 组字成词 归 学习 而不归 闯关」这一条**仍然成立**，变的只是它们是门还是题型。
- ⚠️ **原来拒绝合并的理由已被回应，不是被无视**（详见 §18p）：那条理由是
  「塞进去要在 ③ 里各开一个特例」——而 ③ 是那排门，合并之后 ③ 里**没有任何分支**；
  唯一真实的差别（连线 没有 每次题数）落在**配置页**，而配置页从写出来那天就按题型分支
  （词语闪卡 连 难度 都不显示）。山上的 学习挑战 正是这样：填空挑战 有一排 ⭐ 档，
  另外两种没有。
- ⚠️ **沙滩快跑 升格成门，`store.runMode` 现在由「开哪扇门」写**，学生不再回答第二遍。
  `renderModeConfig` 每一扇门都写（surf 写 `surf`，其余写 `plain`），漏写就会让记忆里的
  surf 在 词语挑战 底下升起一片海滩。
- ⚠️ **`entryModes("surf")` 由 `RUN_MODES` 推导，绝不手写第二份**：它的题型集合按定义
  就是「能跑的那些」，手抄一份迟早和 `runAllowed()` 说的不一样。
- ⚠️ **surf 有自己的记忆位 `runQuizMode`**（`ENTRY_MEM`）。两扇门的题型集合重叠，
  共用 `quizMode` 会让「在海滩上选了 看句选词」偷偷改掉 词语挑战 下次打开的题型。
- ⚠️ **`entryForMode()` 在 `runMode === "surf"` 时优先返回 surf**：每个能跑的题型现在
  同时属于两扇门，纯粹按 ENTRIES 顺序找会把学生送回 词语挑战 的设定页——另一块屏、
  另一个记忆位。
- ⚠️ **各玩法配置页里的 ①模式 那一行已删除**（连同 `[data-rm]` 的 handler）。
  同一个决定出现在两个层级，就是 owner 说的 clunky。

### 学以致用 并进 词语挑战，成为第 4、5 种题型
owner：「put 学以致用 into quiz yourself as a test mode」。
- ①挑战方式 现在**分两排**：**认词**（英文选词 · 看图识词 · 听音识图）与
  **用词**（看句选词 · 组词成句）。
  ⚠️ **五个平铺一行在码头的卡宽下太挤**，而且这两排承担的正是当初拆成两扇门的教学区分：
  前三个问「认不认得这个词」，后两个问「会不会把它用进句子」。**并回一扇门只该少一次点击，
  不该丢掉这个区分。**
  ⚠️ 小标题**不是动线编号**（§7）：编号的步骤是「挑战方式」，这两排是它里面的两层货架。
- ⚠️ **§18g「看句选词 与 组词成句 不做局内切换」仍然成立**。同门不等于局内切换：
  题型仍在**出发之前**选定，一局只跑一种。**不要因为它们成了邻居就加一个局内切换按钮。**
- ⚠️ **`load()` 的迁移按 `s.mode` 判断，不是按 `useMode`**：`useMode` 从它存在起就默认
  `phrase`，包括从没开过那扇门的学生，无条件抄过去会把 看图识词 的常客拖到 看句选词 上。
  `s.mode` 是唯一记录「上一局真的在哪一侧」的字段。
  ⚠️ `useMode` **保留但不再读取**，与 §18i 对四个难度常量的处理一致。
- ⚠️ `quizMode` 白名单**重新收进 `phrase` 与 `sort`**（§18g 特地移除过，现在条件反了）。

### 组词成句 移出 沙滩快跑
owner：「it needs the backdrop of specific scenes」。
- `RUN_MODES` 删掉 `sort`。⚠️ **不是缺功能**：`renderSort` 一直在画 `SCENE_BG`，
  真正的冲突是两层背景打架——海滩带压在 `#xhView` 上方，而它该待的 菜市场／动物园 在面板后面。
- ⚠️ **理由文案是这个玩法自己的**（「这个玩法要用场景背景」），不是那句 generic 的
  「这个玩法没有答对答错」。已实测：记忆是 surf 的老用户开 组词成句 **不会**看到半截海滩。
- ⚠️ **`phrase`（看句选词）画同一批底图，但仍然允许跑**。这是 owner 给的范围，不是我漏了：
  看句选词 一题一点，组词成句 是一整盘拼块压在同一张画上。**要改先问。**

### 🐛 我的海滩：不是拖动坏了，是**根本没有拖动**
owner：「I can't drag and drop the purchased items … it responds to clicks for
repositioning but snaps back to its original position once I let go」。
交接文 §3 按「拖动实现有 bug」列了五条排查方向——**五条都不适用**：
`BERTH_SLOTS` 是**刻意的固定挂点**（SPEC：「营地挣到了拖动，码头还没有」），
`xh.js` 里从来没有一行监听过这些精灵上的指针。学生按住时跟着手指走、松手弹回的，
是 **Safari 的原生图片拖拽**——美术在宣传一个从未实现的手势。
- **已按 owner 裁定实装自由摆放**（照抄 `cs.js` 的营地拖动，含它踩过的两个坑：
  move/up 绑在 **document** 上并且**只绑一次**；`passive:false`）。
- ⚠️ **`draggable="false"` 与 `-webkit-user-drag:none` 是承重的**，两处都要。
- ⚠️ **`touch-action:none` 只给可拖的那一份**：落地页 hero 里的同一批精灵必须保持惰性，
  否则 hero 那颗按钮就点不动了（`beachSpritesHtml(movable)`）。
- ⚠️ **坐标按「物品」存，不按「位置」存**（`store.berthPos`）：换下来再摆回去，
  学生放的地方还在。**`BERTH_SLOTS` 仍然承重**——它是新买的东西落地的地方，
  也是商店里「一个位置只能摆一样」的那个位置。
- ⚠️ **必须有 整理海滩**：自由摆放没有还原键就是陷阱。它只还原**位置**，一件东西都不会少。
- ⚠️ **没在真机上验过**（§15：营地拖动正是那两个「通过全部 headless 断言、真浏览器第一次
  接触就失败」的功能之一）。离屏只证明了 pointer 事件链与落库，**iPad 上要再点一次**。

### ⚠️ 落地页与注册页现在也有 拼音／英文（owner 2026-08-17）
owner：「make sure the landing page and profile registration screen has English and
pinyin toggle so that non chinese users can also create their profile with ease」。
两个辅助以前**只存在于五个活动页**，而**进门的两块屏幕一个都没有**——落地页与昵称／注册流程
是任何人看到的头两屏，却是纯中文的。家长或非华文学生根本读不懂那块「你是谁」的屏幕。
- 偏好存在 **`ws2_profile` 的 `aidPy` / `aidEn`，是全局的**，不分学段：这是一块不属于任何
  学段的身份屏，而 §4 明写身份是允许跨水线的三样之一。
  ⚠️ **它不碰 `store.pyAid` / `store.enAid`**（那是 per-stream 的，学段有自己的开关）。
- ⚠️ **两个都默认开**，与四座山相反、与码头一致：这块屏幕的全部职责就是让还读不懂它的人读懂。
  沉浸是**活动**的事。
- ⚠️ **profile.js 不会自己应用闸门**。页面必须调 `WSProfile.ownAid()` 声明「本页没有引擎
  在管 body 的 class」。cs.js 与 xh.js 各自按自己的 per-stream 设定驱动那些 class，
  多一个写入者就会在每次 render 上打架。**落地页是唯一没有引擎的页，所以是唯一的调用者。**
- ⚠️ **档案面板里的那对药丸只在 `ownAid()` 之后才出现**（`aidPillsHtml()` 否则返回空串）：
  在学段页与码头，顶栏那一对仍是唯一的控件。少了这道闸门就会有两个开关写两份设定。
- ⚠️ **`.lp-aid` 是 `position:fixed` 且 z-index 21**，压在 `.lp-sea`（fixed, 20）之上。
  作为 `.lp-hero` 的绝对定位子元素时，海图一开就被盖住了——而海图正是最需要它的两块屏之一。
  开图时它让开左上角（`返回首页` 占着），移到右上。
- ⚠️ **昵称选择器的注解走一个脚本改两份文件**（§17 逐字一致），改完脚本自己比对。
  四个身份按钮（学生／老师／家长／公众人士）各带拼音与英文——**这是全屏最需要读懂的一处**，
  家长 与 老师 决定昵称会不会上排行榜。
- ⚠️ **深色板与金色板要分开算**（§18e 那一类）：`.sea-label`（藏蓝）与 `.lp-card`（藏蓝）
  进深色块用浅字；**`.lp-enter-btn` 是金色的**，一度被我放进深色块，结果金底上是近白字。
  **量一次，不要猜。**
- ⚠️ `.sea-label` 原本 `white-space:nowrap`：注解会把牌子撑宽，压到 owner 2026-08-16
  要求让开的山体上。现在改成正常换行 + 宽度上限，牌子**只往下长**。

### 组词成句 的拼块拼音：数据已生成，功能未做
owner：「leave it while you generate the pinyin data for me to vet」。
- ⚠️ **直接查表会泄题**：干扰块全部取自词表（查得到拼音），句子块是 `seg` 切出来的普通词语
  （多半查不到）。「有拼音」＝「是干扰项」，比没有拼音糟得多。
- 生成器：`local-admin/make_segpy_draft.py` → `segPy_校对_20260817.xlsx`。
  361 块 / **202 个不重复**，其中 **71 个已有词表拼音（owner 早已审过，不必看）**，
  需要过目的是 **131 个**，多音字与「我已修正」的排在最前。
- ⚠️ **`得` 在动词后是补语记号，读轻声 `de`**，pypinyin 每次都读成 `dé`——已自动修正并
  在表里标出来（游得 / 跑得）。这与 2026-08-16 那 214 处副词「地」被读成 dì 是同一族。
- ⚠️ **owner 回填之前不要建 `segPy` 字段、不要改闸门**。回填之后：`segPy` 与 `seg`
  **长度必须相等**，校验器与 `seg` 现有的三条校验放在一起；**只有整句齐全的句子才发拼音**。

---

## 18n. 句子层可发现性 · 读过 N 句 · 两个 owner bug（2026-08-17，第四批）

依据 `~/Downloads/REVIEW_平台评估_20260817.md`（A/B 批，owner 当面裁定 A-1 乙、A-2 加计数、
D-2 加词）。⚠️ **那份评估里我核过的每一条断言都成立**，包括语句覆盖率逐格一致；
唯一被它推翻的反而是本文件 §18a 自己（见上）。

### 句子层拿回名字（A-1 乙 / B-1）
owner：「I couldn't find the sentence mode for the pier beginner mode」。
⚠️ **这不是 bug，是 §18m 那次合并的代价**：合并本身是对的（少一次点击），
代价是句子层从**一扇有名字的门**变成**一块没有名字的货架**。
整块首页上「句」只出现一次，就是 词语挑战 副标里的一个字；
**看句选词／组词成句 这两个名字一次都没出现过**。owner 找不到的直接原因是
**她在找一个界面上从来没印过的名字**。
- `ENTRIES.quiz.sub` → `认词三种 · 用词两种（看句选词 · 组词成句）`。
  ⚠️ `subEn` 一起改：码头两个辅助默认全开（§18m），英文那行学生真的在读。
- ①挑战方式 的 用词 货架标题 → **`用词 · 生活空间`**，`XH_PY` 同步加 key。
  ⚠️ **生活空间 是 owner 自己一直用的名字，此前只活在文档里。**
- ⚠️ **货架小标题不是动线编号**（§7）：①挑战方式 才是那一步，这两排是它里面的货架。
  **不要给它们编号。**
- ⚠️ **没有恢复 学以致用 为第三扇门**（评估的读法丙）：那等于推翻 §18m 的 owner 裁定。

### 读与考互相指路（B-2）
句子卡 在 `词语闪卡 → 看什么卡`，看句选词 在 `词语挑战 → 用词`：**同一个 90 句语句库，
两块屏幕，彼此从不提对方**。两边各加一句话，互相说出对方在哪。
- ⚠️ **只加文字，永不加跳转按钮**：跳转会把学生从一个 `ENTRY_MEM` 记忆位甩到另一个，
  而 §18m 专门为此拆了 `quizMode` / `runQuizMode`。文字指路没有这个风险。

### `store.readLines`——句子卡的里程数（A-2）
句子卡 是码头上**曝光量最大的活动**，此前在系统里**一点痕迹都没有**：
读完 90 句，航海徽章与教师后台都当它没发生过。
- **存的是句 id 的集合，不是累加计数器**：会累加的数字量的是「滑了几下」，
  「读过 N 句」必须是 N 条不同的句子（和 `store.done` 同一个口径）。
  集合同时让写入**幂等**——`render()` 在每次拼音／英文切换时都会重跑。
- ⚠️ **它不是进度，也永远不许变成进度**：不进 航程（§4）· 不进
  `SAIL_PTS`／`SHELL_PTS`（`learn: 0` 是那两张表里唯一有意的零，**继续是唯一的**）·
  不进九级航海徽章的门槛（那数的是 `store.done`，§13 红线）· 不上任何榜。
  **只在 我的词语表 显示一行，别处一个字都没有。**
- ⚠️ **不写分母**。「N / 90」在句子退役时就会变成 91/90（2026-08-16 退了两句），
  而且框错了：这是里程数不是集邮册（§12：会长大的词库配「集齐」＝会往后跑的终点线）。
- ⚠️ 样式**刻意比 `.xh-log-sail` 弱**：海里 是 航程，徽章与排行榜建在它上面；
  这一个只是曝光。给它同样的药丸就读成了平起平坐的第二个分数，正是 §4.1 禁止的。

### 🐛 房间模式排行榜看不到前两名，且无法向上滚
owner：「Room mode Leaderboard cannot show top two, can't scroll up」。
**`.arena-ov` 是 `justify-content:center` 配 `overflow:auto`。**
居中的 flex 容器一旦内容比自己高，就会**向上下两个方向同时溢出**，
而**向上那一半永远滚不到**：`scrollTop` 的 0 已经在第一行下面了。
结果卡最多 20 行加一枚奖牌，于是被切掉的正好是所有人最想看的 第一名 与 第二名。
- 修法是 **auto margin 一对伪元素**（`::before{margin-top:auto}` / `::after{margin-bottom:auto}`）
  配 `justify-content:flex-start`：auto margin 在 justify-content 之前吃掉剩余空间，
  两端各一个就是平分（**装得下时与原来像素级相同**），装不下时解析成 0，
  于是内容顶到上边、整个可滚。
- ⚠️ **不要「简化」成 `justify-content:safe center`**：老 iPad 正是 G1/G2 的主力机型，
  `safe` 关键字到不了那里。
- ⚠️ 离屏实测（§15，不是用眼睛看）：1280×720 需要滚 359px、旧版只给 180px，
  **179px 滚不到**；1024×768 是 311 / 156，**155px 滚不到**。改后两个尺寸都是 **0**。
  内容装得下时上下留白 203/203，与旧版完全一致。
- ⚠️ **`.pod-overlay` 没有一起改**：它是 `overflow:hidden` 的三人颁奖台，尺寸全 `clamp()`，
  且要投影。**不要顺手把这一招套上去**，没验过。

### 🐛 攀山快答第一帧画的是旧的登山者
owner：「first frame still shows the old climber avatar before changing to the current
player avatar」。`drawClimber` 里那句
`if (av || (SPRITE_IMG.complete && SPRITE_IMG.naturalWidth))` 是错的：
`SPRITE_IMG` 在模组初始化时就加载，**永远先解码完**，
而学生的头像表要等 `startSprint` 调 `avatarSheet()` 才开始下载。
于是解码窗口里条件命中了**兜底分支**，画出学段登山者——一个完全不同的角色——
几帧之后再换掉。
- 两处都要改，**缺一不可**：
  1. `renderSprintConfig()` 开头**预热** `avatarSheet()`，把下载提早一整个配置屏。
  2. `drawClimber` 加守卫 `if (avSheet && !av) return;`——**有头像的学生一帧都不许被画成
     学段登山者**。那几帧什么都不画读起来是「在载入」，画错角色读起来是 bug。
- ⚠️ **兜底分支对其他人仍然是对的**（没头像／未解锁／404）：`avatarSheet()` 的 `onerror`
  会把 `_avSprite` 置空，所以真的缺档会落到兜底而不是永远不画。
- ⚠️ 预热只是预热，**不是修复**：学校 wifi 冷缓存照样能输掉这场竞速，守卫才是修复。

### 「看图学词」这个名字已从屏幕上彻底清掉
2026-08-16 晚改名 词语闪卡 时漏了两处，于是一个**退役的名字**在首页第一屏和一颗按钮上
又活了一天：hero 副标题「看图学词 · 看图听音，慢慢来」与 我的词语表 的批量按钮
「📖 看图学词 · 学这 N 个」。owner 2026-08-17 裁定两处都改。
- 和 §18n 开头 生活空间 那件事是**同一族**：屏幕上留着一个别处已经不存在的名字。
  两个方向都会伤人——一个名字只在文档里、一个名字只在屏幕上。
- `XH_PY` 里两条旧 key 一并删除（删之前 grep 过调用点）。
  ⚠️ 新 key 音节数按 §10 数过：`词语闪卡 · 看图听音，慢慢来` = 11 汉字 / 11 音节。
  ⚠️ 实测 `XH_PY` 全表 **106 条，音节数不符 0 条，重复 key 0 条**。
- ⚠️ **注释与 CSS 类名里仍有「看图学词」四个字，那是代码内部的历史，不上屏幕，不要清。**

### CPDD 英文名（owner 2026-08-17）
owner：「for the translations - we need to align with CPDD」。英文一律改成 CPDD 的说法，
**中文一个字没动**：
`The Beginner's Pier` · `G1 Basic Chinese` · `G2 Chinese` · `G3 Chinese` · `G3 Higher Chinese`
（原为 Setting sail — the beginner tier · Foundation Chinese · Chinese · Express Chinese ·
Higher Chinese）。
- 落点：`index.html` 落地卡 4 处 + 海图牌 5 处 · `XH_index.html` 顶栏 1 处 ·
  `guide.html` 的 `STATIONS` 表 8 处。**共 18 处，改完逐条 assert 过唯一性。**
- ⚠️ **英文现在带年级前缀，与中文那行的「G1 基础华文」重复了一次 G1**——这是 owner 要的口径，
  **不要「顺手」去重**：CPDD 的正式名称本身就含年级。

### 🐛 船用侧舷在走：三个 tier 的 `away_diag` 精灵是镜像反的
owner：「from pier to G3 the boat moves with the side leading the way, I need the top of
the boat to lead the way」。
⚠️ **航向数学是对的，`headingAt` 一行都不用改**——错的是**美术**。
- 码头→G3 的方位角是 **−38.8°**（右上），落进 `away_diag` 桶、不翻转。
  而 `art/xh/boat_t1_away_diag.png` 画的船身轴是 **+27°（右下）**——**整整反了一个镜像**。
  船身与航向差 **65.8°**，那正是「侧舷在前」。
- ⚠️ **只有码头→G3 这一条会踩到**：dock 出发的另外三条落在 `broadside` 与 `away` 桶里
  （G1 +13.7° · HCL −14.4° · G2 −104°），所以 owner 只报了这一条。**不是巧合。**
- **`t2` 是对的**（它与旧的 `art/seamap/boat_*.png` 逐字节相同，代码当初就是照着它写的）。
  `t1 / t3 / t4` 三套新美术的 `away_diag` 全部反了；`toward_diag` 四套都对。
- 修法：把三个 `away_diag` **水平镜像**（`FLIP_LEFT_RIGHT`，无重采样、无调色板变化，
  所以不碰 §14 的像素画缩放陷阱）。原件留在
  `archived_art/boat_t*_away_diag_MIRRORED_20260817.png`。
- ⚠️ **PCA 要量船身，不能量整张图**（§15：几何的东西用脚本验算）。
  t3/t4 是带桅杆的帆船，整张 alpha 的主轴被**桅杆和帆**主导，量出来是 −87°／+79° 这种垂直值，
  完全看不出问题；**只取画面下 45%（船身）** 才读得到真实的 +26.3°／+20.6°。
  第一遍我就是被桅杆骗过去、差点判 t3/t4「没问题」。
- 实测：码头→G3 的船身／航向夹角 **65.8° → 11.8°**；其余航线一度未变。
  四个 tier 的 away_diag 与 toward_diag 现在符号一致（全为负）。
- ⚠️ **t3/t4 的对角视图本来就画得偏侧**（船身轴 −26°／−21°，而桶心是 −45°）：
  这是那两套美术的固有限制，不是这次的 bug。真要更准就得重画，**不要用旋转去凑**。

### 词海钓鱼：汉字与图并列（owner 2026-08-17）
owner：「we need the huge characters to be shown alongside the picture for the players to
recognise with both, right now sometimes it's hard to just recognise using picture」。
一张 摊位 的图和一张 菜市场 的图差得不远，学生等于要先**猜出是哪个词**，才能开始打拼音。
- ⚠️ **这不泄题，而且把题目还原成了它本该考的东西**：这个玩法的答案是**拼音**，
  汉字是**题面**。看着汉字在心里读出来，正是它要练的能力。
- ⚠️ **这行大字底下永远不许出现拼音**——不加闸门版、不加 `.xh-always`、不加 `<ruby>`。
  拼音就是答案（§4.4 只在答错之后才给）。同理 🔊 仍然锁在 `said` 后面：
  **汉字不会泄露读音，声音会。**
  实测：整块面板上唯一的拉丁字母是 收线 按钮自己的 `shōu xiàn` 与 `reel it in`。
- ⚠️ **数字组没有图档，`img()` 吐阿拉伯数字**，于是这一组变成 **3 与 三 并排**——
  对那一组来说这恰好是最该问的问题。
- ⚠️ **标签是 `position:absolute`，不是 flex 行**。先做成 flex 行是错的：
  按钮用 `translate(-50%)` 居中，行一变宽就把**图片往左拖半个标签宽**，
  而 `.xh-sea` 左边三分之一是栈桥和鱼篓——`民众俱乐部` 把海龟摆到了码头上。
  让按钮的布局宽度**只等于图片**，图片就一动不动，标签朝右长进空水面。
- ⚠️ **`width:max-content` 是承重的，不是整理**。绝对定位元素只设 `left` 时，
  会对着**包含块**（这里就是图片那 140px）做 shrink-to-fit；偏移 `left:100%` 之后
  剩余宽度是负的，于是塌成**一行一个字**竖着掉出水面。
- ⚠️ **竖屏手机改成字在图下方**，不是并排。并排在任何 cap 下都做不对，原因值得记下来：
  右边剩多少空间**取决于这一题的图有多宽**，而码头的图从窄窄的 猫 到很宽的 动物园 都有。
  用 vw 去 cap 等于在猜一个每题都在变的数——实测 `38vw` 与 `30vw` 都会把
  `民众俱乐部` 从右边切掉半个字。**一个读不出来的词，等于这次改动白做。**
  放到图下方，可用宽度就是整块面板，不再随题变化；`.xh-catch` 同时上移到 22%
  让「图 + 两行字」仍在水面之内。
- ⚠️ **`.land`（抛进鱼篓的弧线）不受影响**：`--landx/--landy` 是运行时从实际 box 量的，
  会自己跟上新位置。
- 已在 1280×900 / 1024×768 / 390×844 三个尺寸截图看过（不是只跑断言）。

### 🐛 CPDD 英文名漏了一处：码头顶栏的 `<h1>`
`XH_index.html:34` 的 `<h1>出发码头…<span class="xh-en">The Departure Pier</span></h1>`
在第五批里**没被 grep 命中**——我搜的是旧文案 `Setting sail — the beginner tier`，
而这一处用的是**另一个**旧英文名。
⚠️ **是截图发现的，不是搜索发现的。** 教训：改文案时**按中文 key 搜**（`出发码头`），
不要只按已知的旧英文搜——同一个中文可能挂着两个不同的旧译名。

### 海图的船在某些机器上不动 = `prefers-reduced-motion`，**不是 bug**
owner 报：学生 iPad、Chromebook、同事的 ThinkPad 都看得到船在开，**她自己的 ThinkPad 不会**。
`skipSail()`（`nickname.js`）现在**只剩这一个跳过条件**，命中就直接 `location.href = go`
——那就是 owner 看到的「瞬移」。
- **解法在 Windows 设定里，不在代码里**：设置 → 辅助功能 → 视觉效果 → 动画效果 → 开。
  （Win10：轻松使用 → 显示 → 在 Windows 中显示动画。）
- ⚠️ **不要为此拿掉这个闸门**。它是无障碍偏好，不是变通；而且真拿掉了，
  设了这个偏好的学生会被强灌动画。

---

## 18o. guide.html 对齐设计系统（2026-08-17，第七批）

依据 `~/Downloads/guide_visual_alignment_handoff.md`。原诊断是对的：guide.html
用同样的 token **名字**配了另一套**数值**，读起来像一个外挂的文档站。

### ⚠️ 它仍然是自足的，**没有**去 link `css/cs.css`（owner 当面裁定）
交接文 §2a 要求直接 link。**不这么做，理由是可验证的三条**：
- guide 有 **19 个类名与 cs.css 撞车**，其中 `.card` `.opt` `.opts` `.msg` `.py` `.sub`
  `.zh` `.en` `.sun` 在 cs.css 里是**裸选择器**。link 之后每个演示挂件都要改名成 `gd-*`。
- 它会**变成部署仪式的第八处**（§3）。交接文 §4.7 说 guide 仍在「独立名单」上——
  那句话在它开始加载共享文件的那一刻就不成立了。
- guide.html **不在任何人的 QA 回路里**，日后 cs.css 一次重构就能静默弄坏它。
**做法是把 cs.css 的真实数值抄进来**，与 teacher.html 同一种刻意重复（§17）。
每一条抄来的规则在源码里都标着 **⟨cs.css⟩**：`:root` · `.topbar/.back/.tb-name/.tb-sub` ·
`.section-label` · `.card` · `.nav-btn / .nav-btn.primary` · `.prof-chip` · `--glass-edge`。
⚠️ **cs.css 改了，这里要跟着改。**

### ⚠️ 唯一刻意的差异：面板 alpha 抬到 .90-.95（cs.css 是 .56-.74）
**这是量出来的，不是口味。** 这一页整页都是密排双语散文，而 双语 模式下每一行英文都是
`--ink-soft` #5A7080；body 渐变是 `background-attachment:fixed`、底端就是 `--sea1`，
所以贴着视口底边的面板压在 #2E6391 上：照 cs.css 的 alpha，`--ink-soft` 只有 **2.54:1**，
低于 AA。抬高后是 **4.3-4.7:1**。这与 cs.css 自己给 `.pop-card` / 搜索结果 的例外
是同一条（§7：**密集小字往上调，不往下调**）。
⚠️ **渐变的形状、那道亮边、投影一个字没动**——读成「这是本站」的是它们，不是 alpha。

### 「开着的那个」是金色渐变，不是蓝色实底
guide 原来用 `--sea1` 实底表示选中（分页 · 语言 · chip）。**平台上没有这种东西**：
一个主要动作是金色渐变（`.nav-btn.primary` / `.prof-chip.on` / `.wchip.got`），
次要动作是玻璃板（`.nav-btn`）。三处都换过来了。
- ⚠️ **这把 §18e 的陷阱从反方向又踩了一次**：那三个「开着」的状态原本在
  「深色面注解用浅色」那张名单里，现在它们是**浅金色**——留在名单里就是白字压金底。
  现在深色面（`.demo-head`）与金色面（`.btn` / `.chip[aria-pressed]` / 选中的分页）
  **是两条规则**，金色那条用 `#4A3505`（对金渐变暗端 5.43:1）。
  ⚠️ `.ghost` 是淡玻璃板，两条都不能进。
- ⚠️ **演示挂件的内层控件也改成半透明**（`.opt` `.mtile` `.tile` `.slot` `.meter` `.wl-side`
  从 `#fff` 改成 `rgba(255,255,255,.80)` + `--glass-line` + 亮边）。依据是 cs.css 自己
  那句注释：「Inner controls are translucent too, or the card looks like glass with
  opaque stickers on it.」**行为一行没动。**

### 🐛 语言闸门漏光：单语模式下 **27 个标签两种语言一起显示**
`.gd-stat span` / `.why .lbl` / `.demo-foot .lbl` / `.cost .lbl` 各写了一句 `display:block`。
闸门是 `.zh,.en{display:none}` **(0,1,0)** 与 `body.L-* .zh{display:block}` **(0,2,1)**——
一条**两个选择器**的 `display` 是 (0,2,0) 或 (0,1,1)：**赢得过「关」那一半，输给「开」那一半**，
于是那个标签在 中文-only 和 English-only 里都永远亮着（`注意看 NOTICE` 叠着出现）。
⚠️ **规则：凡是可能是 `.zh`/`.en` 的元素，永远不要写 `display`。** 只写排版，显隐交给闸门。
实测漏光 27 → **0**，两个方向都是。

### 🐛 顶栏高度是四个手打的数字，而且四个都已经是错的
`scroll-padding-top:118px` · `.toc{top:96px}` 与 900px 断点下的 150/132：实测顶栏
**134px**，所以粘住的目录条一直**停在顶栏后面**，锚点跳转也落在栏下。
高度**不可能手工维持正确**：顶行换不换行取决于视口**和标签长度**，而标签长度随语言开关变。
- 现在 JS 实测写进 `--gd-topbar-h`，`scroll-padding-top` 与 `.toc{top}` 都读它；
  `resize` · `setLang` · `setTab` · `fonts.ready` 各重测一次。CSS 里那个值只是 JS 跑之前的兜底。
- ⚠️ 顺带把手机上的顶栏从 **184px 压回 126px**（≤560px 隐藏 `.tb-sub`）：
  改用真顶栏之后顶行会换行，粘住的壳吃掉了竖屏的五分之一。

### 其他
- **返回控件改成顶栏那颗圆圈**（`.topbar .back`，§18h 的「全站只有一颗返回」）。
  仍是 `<a href="index.html">`，JS 挂了也不会变成死键。
- **`.num` 用真的 `.section-label` 药丸**。⚠️ `align-self:flex-start` 是承重的：
  h2 是 column flex，默认 `stretch` 会把药丸拉成一整条横杠。
- ⚠️ **对联没有改用 `.lp-couplet`**（交接文 §2b 要求）：那条规则用 `writing-mode`，
  §7 明令禁止；而且它是深色照片上的金字，压在这一页的淡色天空上就是白底金线。
  只借了字重 900 与行距。
- ⚠️ **交接文 §2b 的前提是错的**：`.horizon` / `.hero` / `.hero-title` / `.mini-horizon` /
  `.stream-grid` 在 cs.css 里**全是死代码**，仓库里没有一个 `.html` 或 `js/*.js` 用它们
  （子页面的横幅是 `.lscape`）。所以「复用子页面已有的山形」复用的是一段没人用的东西，
  guide 自己那张 SVG 留着。
- ⚠️ **交接文 §2g 的前提也不全对**：guide **本来就在用真美术**（海图五座岛 `art/seamap/*`、
  连线演示的 `art/xh/*`）。这次新加的是**九枚航海徽章**（§w10，真图、真顺序、真门槛）
  与 **灵露／贝壳 图标**（`art/camp/linglu.png` · `art/xh/dock_shell.png`，
  与平台同样的 16px 内联芯片）。
- ⚠️ **`@supports not (backdrop-filter)` 回退块现在列着十几个面**。
  和 cs.css 末尾那块同一条站规矩：**新加任何玻璃面都要加进去**——
  漏了在新机器上看不出来，那正是它会被漏的原因。
- **guide.html 仍然不进 `?v=` 仪式**：它不加载任何被版本化的文件。部署照旧只推那七处。

---

## 18p. Owner batch（2026-08-17，第八批）

### 排行榜的 UID 截到 8 位
owner 看着榜单问「still has full UID? can we truncate further?」——**是整串 28 位，
一个字符都没截**。`.lb-uid` 的 CSS 有 `text-overflow:ellipsis`，但那只在**溢出时**才生效，
10px 等宽字体下 28 位在那个宽度里放得下，所以省略号从来没出现过。
- 现在渲染 `r.uid.slice(0, 8) + "…"`。⚠️ **8 位不是随手取的**：`teacher.html:1458`
  在反馈工单旁边印的就是 `slice(0, 8)`，全站保持同一个长度。
- ⚠️ **`data-lbu` 仍然是整串**：点一行会打开那个人的 对战徽章，靠的是这个属性。
  截的只是**显示**。
- ⚠️ **这一行存在的理由是重名**：昵称来自固定选择器，两个学生真的会都叫 随遇而安·学士。
  8 位 base64 对一所学校足够，**不要顺手整行删掉**。
- ⚠️ **码头风云榜从来不显示 UID**（`xh.js` 的 `.xh-lb-row` 只有名次／昵称／数值），
  这次没有改它。

### 我的档案：头像 66px → 132px
owner：「there's plenty of space here, make the profile picture way bigger and more
prominent」。原来 66px 的圆头像挤在两颗 link 大小的按钮旁边，读起来像表单的图标。
- ⚠️ **132px 安全，因为 `art/avatar/*` 是 320×320**：这仍然是**缩小**，
  §14 的像素画缩放陷阱（说的是重采样源文件）不适用，一个字节的美术都没动。
- ⚠️ **金环跟着加粗到 3px**：132px 的圆盘配 2px 细线看起来像没画完。
  写法是 `border-width:3px`（0,2,0），压得住后面 `.prof-avatar` 的 `border:2px`（0,1,0）——
  **靠优先级，不靠顺序**。
- ⚠️ **≤560px 降到 96px**，那块 media query **必须留在基础规则之后**（§14）。
- ⚠️ **cs.css 与 xh.css 各一份**（§17 刻意重复），改一处要改两处。

### 我的档案不再有 意见反馈 按钮，但**只在有 💬 悬浮钮的页面**
owner：「remove feedback button from profile since it's already on every screen」。
- ⚠️ **判断依据是 `document.querySelector(".fb-fab")`，直接读 DOM，不用 `_provider` 之类的代理**：
  码头**不注册** code provider 却**有**悬浮钮，而**落地页两个都没有**
  （`index.html` 既不加载 cs.js 也不加载 xh.js）。任何代理都会在六块屏里judge错两块。
  **落地页那颗按钮是唯一入口，必须留着。**
- ⚠️ **「你已提交 N 条反馈」那一行两边都留**：它是信息，不是控件的第二份。
  有悬浮钮时说明文字改成「点右下角的 💬 反馈」——§18n 那条「文字指路，永不加跳转按钮」。
- ⚠️ **`#profFeedback` 的 onclick 必须判空**：不判就抛错，而它后面还wired着 保存／换头像／
  进度码 全部 handler——一颗按钮消失会静默带走半个面板。

### 营地不再是第二个启动台
owner：「remove all these challenge mode from the campsite and beach - this is just the
deco and shop section」。
- 营地场景下面那排七颗按钮（`🎯 自由试炼`）已删，连 `CAMP_MODES` 与 `launchMode()`
  一起删——**留着不引用的启动器比没有更糟**，下次改「某个模式怎么开」就有两处要改、
  漏一处没有任何症状。
- ⚠️ **什么都没被孤立**：填空/华文/英文 在 学习 的 学习挑战 后面，
  词雨/攀山/组词/汉兜 是 闯关 的四张卡，②已经全覆盖。
- ⚠️ **`.camp-board` / `.cb` 两条 CSS 一并删掉**：`.cb` 是共享样式表里的**两字母裸类名**，
  留着不是杂物是隐患——下一个自造「营地按钮」的画面会白拿一圈边框和 hover 抬升。
- ⚠️ **海滩本来就是干净的**：`renderBeach` 从来只有 小铺 与 整理海滩，没有题型排。
  owner 说的「and beach」不需要改动。

### 码头 学习 = 词语闪卡 + 学习挑战（推翻 §18m 的门数裁定）
owner：「pier learning mode should be like mountain: 词语闪卡、学习挑战（consolidate）」。
- 学习 从四扇门收成**两张卡**，②③ 与山上 学习 逐字对应。
  **`词语挑战` 这个名字已退役**，门叫 **学习挑战**（山上就叫这个）。
- **①挑战方式 三排**：`QUIZ_SHELF` 声明归属，不靠位置——
  **认词**（英文选词/看图识词/听音识图）· **用词 · 生活空间**（看句选词/组词成句）·
  **配对与拼字**（连线/组字成词）。没声明的落到 认词。
  ⚠️ 排头**不是动线编号**（§7）：①挑战方式 才是那一步。
  ⚠️ 拼音走 `XH_PY`，**不要内联**——xhPy 找不到 key 返回空字符串，是静默的。
- ⚠️ **唯一的真分支：`每次题数` 的判断从 `kind !== "match"` 改成 `store.mode !== "match"`**。
  连线 不再是门，问「这扇门是不是连线」永远为假，滑杆就会出现在它毫无意义的屏幕上。
- ⚠️ **`entryModes("surf")` 末尾的 `.concat(build)` 必须删**：build 现在已经在
  `entryModes("quiz")` 里，filter 自己就会挑到它，concat 会让沙滩快跑门上出现
  **两颗一模一样的 🧱**（和 §5 删「两」、并「整鸡」同一个失效模式）。
- ⚠️ **`load()` 的 `quizMode` 白名单要加 `match` 与 `build`**，否则上一局玩 连线 的学生
  下次载入被静默重置成 看图识词。
- ⚠️ **组字成词 的「灰掉理由」从门搬进 `modeBtns`**：它的理由是「这组没有两个字以上的词语」，
  掉进默认的「这组没有图片」会让学生去改 学习范围 找图片，而缺的是多字词。
- 实测：七种题型逐个从这扇门出发，全部正常开局，无重复瓦片；切到 连线 时 每次题数 消失、
  ② 自动renumber成 难度，切回 组字成词 又回来。

### 码头顶栏液态玻璃
owner：「the top bar should be liquid glass to match the mountains」。
`.xh-top` 原来是 `rgba(255,255,255,.9)` 平板配写死的 `blur(8px)`——全站唯一没进设计系统的
一条栏，压在码头自己那张海景上像贴了一条纸。现在**逐值抄 cs.css 的 `.topbar`**：
渐变 .66→.48 · `var(--glass-blur)` · `--glass-line` 下边线 · 同一组投影与内亮边。
- ⚠️ **模糊值用 token 不用字面量**（§7 六个 `--glass*` 驱动所有面板）。
- ⚠️ **渐变才是「玻璃」的来源**，不要压回单一 alpha。
- ⚠️ **`.xh-back` 一起变玻璃**：白底＋2px #B9CEDD 描边的药丸压在玻璃栏上，
  正是 cs.css 注释里说的「玻璃上贴不透明贴纸」。**仍然保留文字**（§18h：码头控件带字，
  这是刻意的差异），只是换成 `var(--glass)` + 1px `--glass-line`。
- ⚠️ **`@supports not (backdrop-filter)` 里要补 `.xh-back`**：它从近白变成 .5–.72，
  没有模糊的旧 iPad 会直接透出海景。

### 走进社区（门 A，终于建了）
owner：「for the authentic scenes where sentences were written into life scenes, to be
it's on separate section called 走进社区 - in the same bar as their vocab list, then when
student click in, they should see the full range of all the authentic settings like wet
market, school, supermarket etc. then when they press in they learn the sentences」。
- **右栏 我的词语表 下面一张 `wide` tile → 十张场景卡 → 一个场景的句子一句一句读。**
- ⚠️ **它是浏览层，不是活动**，所以在右栏而不在 ③：③ 的每扇门都受 ①学习范围 约束、
  都发贝壳；这块走**整个句库**、不看范围、**一分不发**（`mode:"learn"`，
  而 `learn: 0` 仍是两张费率表里唯一有意的零）。放进 ③ 它就会是唯一无视 ① 的那张卡。
- ⚠️ **display-only 的 7 句在这里是包含的，而且只有这里包含**：它们当初就是写来读的，
  别处排除只因为它们**当不了题**。§11 明写在「更难」与「更多曝光」之间取曝光。
- ⚠️ **复用 `renderSentenceCard` 一个字没改**（同一张卡、同一颗 🔊、同一处
  `store.readLines` 写入）。第二个句子卡渲染器就是「第二条计分路径」的美术版。
- ⚠️ **`state.walk` 让 `wireQuit()` 把箭头指回场景格**：它跑的是 `mode:"learn"`，
  按常规查表会解析成 词语闪卡，把学生丢到一块他从没打开过的设定页。
  三级返回：海图 ← 码头 ← 走进社区 ← 一个场景。回合条的标签同理写 `菜市场 · 走进社区`。
- ⚠️ **`开始测验` 只在真的跑得起来时才出现**（`phrasesFor(state.pool, "phrase")` 非空）：
  场景里有无 ask 的 display-only 句，农场 一共只有 4 句——给 startRound 一个出不了题的池，
  学生按下 出发 什么都不会发生，正是 §4.4 要防的静默失败。
- ⚠️ **PHRASES 载入失败时整张 tile 不出现**，而不是打开一个空格子：受管网络下句库 fetch
  是允许失败的（那个 `.catch` 把 PHRASES 留成空数组），码头其余部分照常工作。
- ⚠️ **读过的数目不写分母**（§18n）：句子会退役（2026-08-16 退了两句），
  「N / 90」会变成 91/90，而且这是里程数不是集邮册。场景卡上读完打一个**安静的 ✓**，
  **不要做成第十枚航海徽章**。
- ⚠️ **`SCENE_EN` 另立一张表，不进 `XH_GROUP_EN`**：后者被 `checkGroupLabels()`
  拿去核对词表里的 组别/子类，塞进场景名会让那份报告失去意义。
  场景的**拼音在 `XH_PY`**（§10），其中 学校/交通/组屋区 **已经是 key，不能重复声明**——
  对象字面量里重复 key 会静默覆盖。
- ⚠️ **`SCENE_ORDER` 是 owner 的阅读顺序**（离家最近的先：组屋区 · 学校 → 日常跑腿 →
  出游），不是数据顺序也不是笔画顺序。
- **两边互相指路**（§18n 的 B-2 同一条）：学习挑战 的 用词 排下面写「或者到 走进社区
  逐个场景读」，**只写字，不做跳转按钮**。

### 🐛 顺手修掉一条一直在报的标签告警
`XH_PY` 里 `"词语闪卡 · 看图听音，慢慢来"` 的拼音每次载入码头都在 console 报
「11 vs 10」。原因是**自检按空白切词**，而 `yīn，màn` 中间没有空格，被数成一个音节。
⚠️ **CLAUDE.md §18n 写着这条 key「音节数按 §10 数过 = 11 汉字 / 11 音节」——
手数的结果与自检不符，而自检才是对的。** 全角逗号后面补一个空格即已修好，
现在码头启动零告警。**这正是那个自检存在的理由，不要因为文档说数过就跳过它。**

---

## 18q. guide.html 改用站上的真美术（owner 2026-08-17，第九批）

owner：「use existing art in the game e.g. landing page, the mountains, the pier etc to
make it cohesive and illustrative」。§18o 把 guide 的**样式**对齐了平台，但它的**画**
仍然是为这一页现画的：一段扁平的 SVG 地平线，其余全是 emoji。现在整页只用
`art/` 里已经存在的文件，**一张新美术都没有加，一个字节的美术都没有改。**

### 页头换成落地页那张画
`art/bg/landing_hero_bg.png`——`cs.css` 给 `.lp-hero` 的同一个文件。扁平 SVG 三峰
地平线已删除。⚠️ **§7 的「三座山峰 · 太阳在右」管的是本仓库自己画的地平线**；
平台的正门就是这张画，一份讲平台的文件在这张画上开场是对的。
- ⚠️ **`center top`，不是 cs.css 的 `center center`。** `.lp-hero` 有 100vh，居中裁切
  几乎显示整张画；这条页头只有约 380px，居中裁切落在画面**最花的中段**——实测标题那一行
  横过去的亮度在 **51–202** 之间跳，任何单一颜色的字都活不下来。顶部裁切把天空稳在
  **110–161**。
- ⚠️ 标题与对联用的是 `.lp-couplet` 的**配色与三重深色阴影**（`#FFE9B0`）。
  gold 压这张天空是 **3.15:1**：够 AA-large（≥18.66px 粗体），**不够小字**。
  所以对联的字号下限从 12px 提到 **16px**——它必须**是**大字，否则就得给它一块落地页
  并没有给它的底板。≤560px 整个隐藏对联（`cs.css` 在落地页 900px 以下同样这么做）。
- ⚠️ 两行小字（`VOCAB SUMMIT · GUIDE` 与校名）另给**一小块深色板**：9–12.5px 是小字，
  `#FFF4D6` 压同一片天空只有 3.4:1。这就是 §18e 给码头 hero 副标题的那块板，
  **只包住那几个字，不是盖住整张画**。
- 页脚一道**向下**渐变接进正文的天空色 `#EAF4FA`，只有 26% 高：owner 2026-08-16 撤掉的是
  盖住美术的深色蒙版（§18c），这一道方向相反，是为了不留硬接缝。

### 两套新词汇：`.gd-band`（画）与 `.gd-strip`（一排道具）
- **band**：①`sea_tile` + 海图五座岛 · ③`sea_tile` + `mtn_g2`（就是学段页顶上那条
  `.lscape`）· ④`mountain_bg` · ⑤`dock_bg` · ⑤沙滩快跑（`art/xh/run/*` 的天空、沙滩、
  跨栏、拱门 + 六帧行走表的第一帧）。
- **strip**：12 个头像 · 五枚板块徽章（`cs.js BADGE_IMG`）· 八件营地装备 · 四级船 ·
  三个徽章家族十枚并排。
- ⚠️ **一律不放大**：最小的 `island_g1` 是 500×307，显示高度 54–66px，所以 §14 的像素画
  缩放陷阱在这里踩不到。断言已加进离屏检查（**显示尺寸 > 原生尺寸 = 失败**，实测 0 条）。
- ⚠️ **玩法卡片仍然是 emoji，这是刻意的**：四座山**没有**逐模式的美术，
  借一张码头的图来配山上的模式会教错东西。**宁可一排 emoji，也不要一张说谎的图。**

### ⚠️ ① 那条海图带子**不是海图的复制品**，永远不要当成
岛是等距排开的，只承载**顺序**。真实坐标（`cs.css .i-g1` 等）是按 **100vw×100vh** 定的，
换算到一条 1120×186 的带子上会印出**另一套距离**却看起来很权威，而 §18c 那 20 条航线的
避让验算还得为一个装饰重跑一遍。**「距离读成难度」这条规则底下配一张距离是假的图，
比不配图糟。**

### 陷阱：带子边上有东西站着的时候，说明必须让开
- **海图带**第一版把说明留在底边，那条渐变整个吃掉了 **出发码头** 与 **词星大冒险** 两个
  岛名。**踏浪带**同样，兔子、两根栏和拱门全压在字底下。两条都改成 `.cap.top`（渐变方向
  跟着翻），并给带子留出顶部 padding。
- ⚠️ **棕榈从 34% 降到 27% 是量出来的**：34% 时树冠顶在 79px，而说明最后一行的底边在
  80px——**差 1px 就是「字压在树上」**。§15：几何的东西用脚本量，不要用眼睛估。

### 陷阱：`.gd-row` 是 grid，不是 wrap flex（§18f 那一条又中一次）
十二个头像在 1120px 正文里排得下十一个，**第十二个自己掉到第二行**，一个孤零零的头像
读起来就是坏掉了。
- ⚠️ **头像那一排的列数写死 12 / 6 / 4**，不是 auto-fill。12 = 4 生肖 + 4 西游记 + 4 瑞兽，
  三档都整除，任何宽度都不留孤儿。
- ⚠️ 其余各排用 **auto-`fit`**，不是 auto-fill：auto-fill 留着空轨道，八件装备排在十一个
  轨道里会在四分之三处停住，读起来像被截断。
- ⚠️ **图要站在同一条线上，靠的是一个固定高度的图坑（`.gd-fig .ic`），不是对齐格子。**
  `align-items:flex-end` 是直觉答案而且是错的：它对齐的是**整个格子**，于是
  `生肖·鼠` 下面两行英文与 `西游记·孙悟空` 下面三行英文，会把前者的图**抬高 30px**。
- ⚠️ **船只仍然不统一高度**（§18l）：坑高 82px，用 `max-height` 让原始比例自己说话，
  帆船因此仍然比舢板高约一倍。

### 🐛 数字自己写会写错：两次都靠对源码才发现
1. **船价第一版是我编的**（「60/140/300/600」，两栏同价）。真实的在 `js/profile.js`
   的 `BOATS`：第一艘**免费**，其余 **80/250 · 200/550 · 400/1000**（贝壳/灵露），
   而且**只能一级一级买**。
2. **头像的解锁方式第一版也是错的**：写成「西游人物与瑞兽都要用灵露买」。真实的在
   `AVATAR_CATALOG`：生肖十二个全免费 · 西游记五个标价 300–600 灵露 ·
   **瑞兽四个一颗灵露也买不到，是年度试炼中一→中四逐级解锁**。
⚠️ **规则：guide 里出现的每一个数字都要对着源码那份数组抄，不许估。**
一份文档写错数字，比不写那个数字糟得多——读的人没有别的地方可以核对。

### 🐛 `p:` 与 `i:` 一字之差，船价整排静默消失
`BOATS` 写的是 `p: "80 / 250"`，而渲染函数读的是 `a.i ? … : ""`。**不报错**：
「这一项没有价钱」与「这一项的价钱写在另一个键上」长得一模一样。
⚠️ **和 §18g 那个 `SAIL_PTS` 缺 key 的 bug 是同一族**（缺 key 与故意写 0 无法区分）。
是**截图**发现的，不是断言发现的——断言证明数据流，证明不了你读的是不是同一个键。

### ⚠️ 竖屏手机上带子要**变高**，不是变矮
390px 宽时双语说明会折成**六行、141px**，而**那个高度跟视口宽度无关**。
把带子按 vw 压到 120px，六行字就吃掉全部空间。所以 ≤560px 那一档把 `.cap` 从
`absolute` 换回 `static`（配一块不透明的板），再用 padding 给图留一个 **150px 的固定窗口**：
说明在后的带子图窗在上，说明在前的（踏浪）图窗在下。**图变小了，但没有一块被字遮住。**
⚠️ `.gd-band.sea` **除外**：它是一条 flex 行，static 的 `.cap` 会变成第六个 flex 子项挤进
岛里面；它改成自己让出顶部 152px。

### 顺带修掉两处**与线上不符**的正文
`⑤` 与 `⑪` 都还在写「潮水在后面涨 / the tide rises behind」「跑者固定在四分之一处」——
那是 **§18l 已经整段作废的卷动模型**。现在写的是「跑一步、跨一栏，跑到拱门就结束」，
并在 ⑪ 补一句「早一版真的有潮水，2026-08-17 整个撤掉了」。
⚠️ **这一条本来可以一直躺着不被发现，是新加的那条踏浪带子把它顶出来的**：
画上没有潮水，字上有——**图与字对不上，是文档过期最容易看见的一种。**

### 两个必须跟着加的清单（漏了在新机器上看不出来）
- `@supports not (backdrop-filter)`：新增 `.gd-strip` 与 `.gd-herosub`。
- 「深色面上的注解」块：新增 `.gd-band .cap .en` 与 `.gd-herosub .en`。
  ⚠️ 这就是 §18e 那条：`body.L-both .en` 会把浅底用的灰**继承到画面上**，
  每一条覆盖都得带 `body.L-both` 前缀才压得住。

### 仍然成立的两条
- **guide.html 依旧不进 `?v=` 仪式**：它不加载任何被版本化的文件，`art/` 在本仓库全站
  都不版本化。部署照旧只推那七处。
- **它依旧自足，没有 link `css/cs.css`**（§18o 的三条理由一条都没变）。
  抄来的规则仍然逐条标着 ⟨cs.css⟩。

---

## 18r. 一段进度码涵盖五片陆地 · VS3（owner 2026-08-17，第十批）

owner：「is progress code shared across all 5 sections (pier and mountains)? they need to
be since the student is not going to take down all 5 if they differ.」
**答案是「不是」，而且比想象的更糟**：四座山各有一段码、彼此拒收（`decodeProgress` 明确报
「这个进度码属于其他 subject level」），而**出发码头一段都没有**——码头学生换设备就全丢，
没有任何找回的路。落地页更只印着「进度码在各科目页面里」，等于叫学生去收集四段字符串。

### 格式
    VS3.{sec}~{sec}~….{nickB64}.{ck}
      山：{stream}:{n}:{b64 已掌握位图}:{a-b-c-d-e 纪录}
      码头：xh:{词数}:{b64 已认得位图}:{句数}:{b64 readLines 位图}
- ⚠️ **分隔符是挑过的，不是随手取的**：base64url 的字母表是 `[A-Za-z0-9-_]`，
  所以 `-` 和 `_` **可能出现在位图里面**，任何贴着位图的层级都不能用它们当分隔符。
  `.` `~` `:` 都不在那个字母表里。（VS2 能把 `-` 连接的纪录放在自己的 `.` 段里，同一个道理。）
- ⚠️ **位图按已发布词序定位**，而项目规则保证词序**只追加不插入**（§5），所以加词不会让旧码解错。
  这也正是这套编解码**必须读数据文件**的原因。
- 实测长度 **795–796 字符**（五片陆地都有进度时）。

### 为什么放在 profile.js
- ⚠️ 它是**六个页面唯一都加载的文件**，所以进度码不再是「你恰好打开了哪一页」的属性。
  **落地页现在也能生成和恢复**——换了设备的学生第一眼看到的就是落地页，那时他还没选学段。
- ⚠️ **水线没有被跨越（§4）**。真正承重的保证是「cs.js 从不读 `ws_xh`，xh.js 从不碰
  `ws2_*`」，这一条**一个字节都没变**：两个引擎都没有新增一行跨线代码。
  profile.js 是身份层，§4 本来就允许它跨线；而备份把两个 store **并排装着**是**搬运，
  不是兑换**——恢复时码头的数字回 `ws_xh`、山上的回 `ws2_{stream}`，没有任何换算，
  也不暗示任何汇率。
- ⚠️ **当前页面那一段仍然由它自己的引擎写**：`commitAll` 把匹配 `_provider.stream` 的那段
  交给引擎的 `commit()`，另外四段自己写 localStorage。**反过来就是这套结构存在的理由**：
  cs.js 把 `store` 拿在内存里，下一次 `saveStore()` 会静默盖掉直接写进去的值。
  为此 **xh.js 现在也注册了 provider**（`stream: "xh"`）。
  ⚠️ xh 的 `addIds` 是**词语文本**不是 id（`store.done` 一直按 `词语` 索引，码头没有词 id）。

### 刻意不放进码里的东西
- ⚠️ **「没有货币」这一条已被 owner 推翻（2026-08-19，见 §18ae）——但先读完这一段。**
  VS2／VS3 不带 灵露 · 贝壳 · 历练值 · 航海值，理由不是整洁：
  **花 200 灵露买了头像，再恢复一段旧码，头像和 200 都在**。
  掌握／认得与纪录是**单调合并**（并集／取大），所以同一段码恢复两次不会有任何变化——
  **这个性质才是恢复安全的根据，而货币没有这个性质**。实测第二次 `commitAll` 的 `added` 是 **0**。
  ⚠️ **这段描述必须留着**：推翻的是结论不是分析。谁哪天想删掉全新账号闸门，
  会先撞上这一段，看见它挡的是什么。
- ⚠️ **不含 `store.stats`**（每词 shown/wrong）：体积大、不是进度，而且「只增不减」
  这句承诺对一个比率根本没法兑现。
- ⚠️ **`readLines` 例外地带着走**：它是 句子卡／走进社区 曝光的唯一痕迹（§18n），是个集合，
  和别的字段一样按并集合并。**到岸之后仍然不是进度**：写回 `store.readLines`，别处一个字都不读。

### 稳健性
- ⚠️ **`decodeAll` 与 `peekCode` 会去掉所有空白，不只是 trim**。一段 VS3 有约 800 字符，
  而文档教的保存方法就是「用邮件发给自己」——**任何邮件客户端都会硬换行**，
  不去空白的话学生把好好的码贴回来会得到「已损坏」。实测四处换行 + 尾随空格照样解开。
- ⚠️ **VS2 / VS1 仍然能解，而且现在能在任何页面解**：旧的编解码只认得「内存里恰好是哪个学段」，
  所以一段 G3 的码贴在 G2 页面上会被拒。同样的字节，覆盖面变大了。
  实测用 Python 独立按同一套 fnv1a + 位图铸了一段 VS2，在**码头页**成功恢复到 `ws2_g2`。
- ⚠️ **校验和改一个字符就拒收**（实测）。
- ⚠️ **某一片陆地在本机没有 store 就跳过，绝不新建**：从码里造一个 store 会得到一个
  **该引擎的 `load()` 从没迁移过**的半成品，那个引擎依赖的每一个默认值都会缺。
  没打开过的陆地保持没打开，学生哪天打开了再恢复。对话框会写明这一点。
- ⚠️ **某一片陆地的词库没载入就不写进码里**（不是写成空的）：写成空段会在另一台设备上
  恢复成「零个已掌握」，而合并之所以安全全靠它是并集。省略的含义是「这段码没提那片陆地」。
  面板会写出哪几片没包含。
- ⚠️ **撤销覆盖所有被动过的陆地**（`ws_prerestore_all`，sessionStorage）。实测
  「编码 → 清空五片 → 恢复 → 撤销」一轮，五片全部回到原样。
  用 sessionStorage 是刻意的：快照活过一周就等于让学生能回滚一周的学习。

### 🐛 顺手发现并修掉：`STREAM_LABEL` 在 profile.js 里声明了两次
我把 `xh` 加进了**上面那张**表，而**下面还有一个 `var STREAM_LABEL`**（我的进度 行用的短标签）。
同一作用域两个 `var` 同名，后面那次赋值对之后运行的一切生效——于是
`STREAM_LABEL["xh"]` 是 `undefined`，`decodeAll` 自己的 `if (!STREAM_LABEL[sec])` 守卫
**把码头段静默丢掉**：码里明明写着 `xh:148:…`，摘要里就是没有码头。
- 已把编解码那张改名 **`CODE_LABEL`**，消掉遮蔽。
- ⚠️ **不要把两张表合并**：它们印在不同地方、长度不同
  （码摘要里是「出发码头 · 学海起步」，进度行里是「词圣鸿文苑 HCL」）。
- ⚠️ **凡是按 section 索引的地方都必须用 `CODE_LABEL`**（section 里有 `"xh"`，短表没有那一行）。
  搞错就是上面那个 bug。

### 落地页与新设备
- 落地页那句「进度码在各科目页面里」**已删除**，连同它下面四个学段链接。
- ⚠️ **`peekCode` 的 `stream` 对 VS3 返回 `null`**：没有单一学段可以交接。
  `takePendingCode` 因此接受「stream 为空的待恢复码在任何页面都匹配」——
  少了这一条，昵称选择器存下的五片陆地码会永远躺在 localStorage 里谁都匹配不上。
- ⚠️ **`cs.js` 的新设备路径改走 profile.js 的解码器**（`decodeCode` + `commitAll`）。
  它原来调自己的 `decodeProgress`，而那个只认得内存里那一个词表——一段五片陆地的码
  在那里会被当成格式错误拒掉。**一个 planner，一个 writer**：两个解码器伺候一种线格式，
  就是线格式静默分叉的开始。
- ⚠️ `cs.js` 的 `encode`/`decode` 仍然挂在 provider 上、但面板不再调用：
  `encodeProgress` 是唯一还能吐出该学段 **VS2** 码的地方，而学生邮箱里已有的码说的就是 VS2。

---

## 18s. 码头第十批的其余改动（owner 2026-08-17）

- ⚠️ **③ 的门不再有副标题**（owner「this is too cluttered - remove subheadings」）。
  `ENTRIES` 的 `sub`/`subEn` 连字段一起删了。**这不是推翻 §18n**：那次修的是
  「句子层在界面上没有名字」，而名字现在在**更有用的地方**——学习挑战 里
  `用词 · 生活空间` 那排货架标题，以及右栏的 走进社区 瓦片。瓦片副标是三处里最吵的一处。
  ⚠️ 灰掉理由**不是**副标题，留着：它是唯一解释「这扇门为什么打不开」的东西。
- ⚠️ **走进社区 与 我的词语表 并排一行**（owner）。右栏现在是两行两列：
  两个浏览面在上，航海徽章 与 码头风云榜 在下。两个整行瓦片叠着会把徽章和榜单挤出笔电首屏。
- ⚠️ **走进社区 用场景自己的底图**（owner「everything in 走进社区 should use the authentic
  scene background instead of the pier background」）：句子卡与走完的结算页都套
  `.xh-scene-bg` + `.on-scene`，和 看句选词／组词成句 同一套机制、**同一个压暗旋钮**（§18a）。
  ⚠️ 按 `state.scene` 判断，所以**只有 走进社区 会拿到**：词语闪卡 · 句子卡 按 ①学习范围 取句，
  一局里可能跨场景，底图每张都换就是纯噪音。
  ⚠️ 场景格本身仍是码头底图：那一屏不在任何一个场景里，而每张卡已经带着自己的场景图。
- 🐛 **display-only 的句子没有贴图也没有词**（owner 报 `我的邻居养了一只猫。` 与
  `组屋区里有许多设施。`）。它们**没有 `ask`**——那正是 display-only 的定义——所以
  `p.pic || ask.图档` 什么都查不到，卡片在两张正常卡之间渲染成一句光秃秃的话。
  新增 `lineSubject(p)`：没有 `ask` 时取**句子里出现的最长码头词**（猫 · 组屋），连拼音一起印。
  ⚠️ **最长匹配，否则 `熊猫最喜欢吃竹子` 会挑到「猫」**。
  ⚠️ **这只在「走进社区 什么都不考」的前提下安全**。**绝不能给 看句选词 用**：
  那里图就是题的一部分，必须正好是目标词（§11：`这包＿＿多少钱？` 没有正确贴图无解，
  错的贴图会让它「有解但答错」）。
  ⚠️ 结算页给 startRound 的池仍然**只按 `ask` 建**：我们替学生挑的一张图，不是学生给过的答案。
- ⚠️ **整理海滩 已从 我的海滩 移除，换成 🎒 我的背包**（owner「remove 整理海滩 - also
  include a mechanism where students can choose to keep their items away, perhaps an
  inventory? I think we had the idea of their backpack - has it materialised?」）。
  **答案：没有materialise过。** 仓库里从来没有叫 背包 的东西；最接近的是山上的
  `store.items`（消耗品计数）与 `store.itemSlots`（选择界面至今未建，§18）。
  码头有 `store.owned`，但**没有任何屏幕列出它**：海滩小铺 只提供 摆上，
  所以想要一片空沙滩根本做不到。
  - ⚠️ **它不是第二个商店**：不卖东西、不显示价格，只列已有的，每件一个动作（摆上／收起）。
    价格留在 海滩小铺，否则同一个问题要查两个屏幕。
  - ⚠️ **`store.owned` 永远不减**。要是哪天 收起 从 `owned` 里删东西，
    学生花 45 贝壳买的东西会被误触摧毁。
  - ⚠️ **§18m「自由摆放没有还原键就是陷阱」仍然成立，只是搬了家并且长大了**：
    收起 会清掉**这一件**的拖动坐标（摆上再收起＝单件复位，用学生已经懂的动作表达），
    背包底部另有 整理位置 一次清空全部。**两层都不要删。**
  - ⚠️ **船不在背包里**：船永远有一艘在开，它出现在落地海图上，而且拥有记录在
    `ws2_profile`（§4 允许跨线的三样之一）。换船留在能看到阶梯的商店里。
- ⚠️ **hero 预览不再画摆件**（owner「the positions don't lock right. just display the
  original image」）。它原来按**和 我的海滩 一样的百分比**合成同一批摆件，本意是两边不会漂——
  但百分比在这里对的是**另一个盒子**（矮而宽的横幅，不是高的舞台），
  于是学生停在沙滩上的木箱落到了海里。自由摆放（§18m）让这件事更糟，不是更好：
  坐标现在是手指停在哪就是哪。
  ⚠️ **不要用缩放去「修」它**：两个盒子长宽比不同，没有单一变换能映射；
  而且预览根本不需要——海滩就在一次点击之外，那里画的是真实摆法。
  ⚠️ 顺带按 owner 要求**照 cs.js 的 `.lscape` 精简**：标题 + 一行安静的数字 + 角上一个
  我的海滩 › 链接，取代原来的副标题加三个方框数字。`statCell` 与 `.xh-hero-scene` 一起删除。
  ⚠️ **那一行里不要内联 `xhPy()`**：注解 span 在闸门下是 `display:block`，
  夹在句子中间会把一行拆成三行（走进社区 瓦片上刚踩过同一个坑）。

---

## 18t. 🐛 看句选词「没有音效、还会卡住」是三个缺陷（owner 2026-08-17，第十一批）

owner：「the authentic sentence tests have no positive feedback sound and it freezes
before progressing」，追问后确认 **iPad 与 ThinkPad 都一样**。
⚠️ **一句报告，三个各自独立的缺陷，凑在同一个 handler 里**：答对之后屏幕上**真的什么都没有
发生**，然后 1.4–2.4 秒才翻页。所谓「卡住」不是错觉，是**在那段时间里确实没有任何反馈**。

### 缺陷一：`.xh-hint` 是 `opacity:0`，而这个 handler 从来没加过 `.show`
`css/xh.css` 里 `.xh-hint{opacity:0}`，只有 `.xh-hint.show` 才看得见。
词海钓鱼 与 听音识图 都写整个 `className = "xh-hint show"`；
**看句选词 · 组词成句（两处）· 组字成词（两处）一共五处只赋了 `innerHTML`**——
文字一直在 DOM 里，从上线那天起就**一个字都没被看见过**。
- ⚠️ 已全部改走 **`xhHintShow()`**：取元素、顺手把 class 设好、返回它。
  **不要再直接给 `#xhHint` 赋 innerHTML**——漏了没有任何症状，文字只是永远看不到。
- ⚠️ 这是这份报告里**最主要**的那一条，也是最难自己发现的一条：断言全过、DOM 全对、
  肉眼什么都没有。

### 缺陷二：选项 class 名对不上样式表
`css/xh.css` 定义的是 `.xh-opt.right` / `.xh-opt.wrong`，而这个 handler 加的是
**`ok` / `no`**——全文件唯一一处自造这一对的地方，**两个都没有任何规则命中**，
所以学生点下去的那颗按钮**颜色一点都没变**。
（renderEnMcq · renderPic · renderListen 一直用的是 right/wrong。）
- ⚠️ 已改成 right/wrong。实测背景色从 `rgba(0,0,0,0)` 变成 `rgb(233,247,238)`。
- ⚠️ 这一类只能靠**量**，不能靠看：一个不存在的 class 和一个存在但很淡的 class
  在截图里长得一模一样。

### 缺陷三：音效的重建额度被朗读烧光，而且永不归零
`_acFails`（码头）／`_rebuilds`（山上）**只增不减**，所以那 8 次是**整页寿命**的额度。
而这个玩法的设计流程（§18h 把朗读搬到句子旁边那颗 🔊）就是「先听整句、再作答」，
一句 rate=0.85 要读 2.5–3.5 秒——学生几乎总是**在朗读还没结束时**答对。
那一刻苹果的音频通道在 `speechSynthesis` 手上，而 §9 已经写明
**`interrupted` 状态下新建的 context 一样是 interrupted**：这次重建**注定失败，
却照样扣一次额度**。答对八次，整页永久没声音，`revive()` 也救不回来（它走同一个函数）。
- ⚠️ **① 通道真的 running 了就把计数清零**（`noteRunning` / `noteCtxRunning`）：
  额度要计的是「这台设备根本放不出声」，**不是**「刚才被朗读打断过」。
- ⚠️ **② 朗读进行中不花额度**（`speechBusy()`）：那一次必然失败。等 `speak()` 的
  `onend/onerror` 触发 `revive()` 时再来一次——那时通道是空的，重建才有意义。
- ⚠️ **额度本身必须留着**：浏览器对一页能建几个 AudioContext 有硬上限
  （Chrome 历史上是 6），真的哑掉的设备还是得停手。**不要因为这个 bug 把上限删掉。**
- ⚠️ **两份都要改**（§17 刻意重复）：`js/cs.js` 与 `js/xh.js` 各有一套音频栈。
- 实测：模拟「朗读占着通道 + context 永远 interrupted」，连答 20 次，
  新建 context **1 个**（旧代码会烧掉 8 次额度）；朗读结束后仍能正常新建，上限没被耗尽。

### 顺带：正反馈不再依赖声音
⚠️ **§9 早就写着「iPad 侧面的静音开关会掐掉网页音效但不影响朗读」**——
所以任何「以那一声当作『你答对了』」的设计，在一台静音的 iPad 上**从构造上就是坏的**。
现在答对的主反馈是**空格自己被填上**：`＿＿坏了` → `电梯坏了`，绿字绿下划线，
就在面板最上方、题目本来的位置。
- ⚠️ **填空才是正确的反馈，不是花样**：这道题问的就是这句话缺什么，填上之后学生读到的是
  **完整的句子**——和「答对后显示整句」同一个理由。
- ⚠️ 确认行因此**不再重复整句**（就在上面，而且是完整的），只留一行 ✅ 答对了 与文化注解。
- ⚠️ 下划线保留、只换颜色：去掉它会让基线移动 3px，30px 的大字上读起来像抖了一下。
  金色＝空格，绿色＝已答，两者**不能长得像**。
- ⚠️ `.xh-filled` 已加进 `prefers-reduced-motion` 块：**词必须照样出现**（那才是反馈），
  只是不滑进来。

---

## 18u. 教师后台 · 结伴登峰出题设置照学生端重做（owner 2026-08-17，第十二批）

owner：「work on teacher html room mode, the interface is super cluttered, make it like
the student interface for easier selection」。**只改这一块屏幕**（`renderArenaSetup`），
房间的数据结构、`generateRoom`、`rooms` 文档字段、arena.js 一个字节都没动。

### 原来为什么挤
- **单元范围是 29 个裸 checkbox 铺成一整片墙**（G3 三行），而学生端 2026-08-14 起就是
  **按年级折叠 + 金色单元卡**（§18d、`cs.js renderHome`）。更糟的是**单元的主题一个字都不显示**——
  老师认得的是「新同学 新鲜事」，不是「中三·单元四」。
- 每个控件都是「小写字母间距的 `<label>` + 控件」竖着堆在**一栏**里，而这一页的壳宽到
  `min(1720px,97vw)`：一栏用掉三分之一，`生成擂台码` 被推到折线以下。
- **题数 / 时长 是 `<select>`**：全页唯一「要先打开才能看见选项」的两个控件。

### 现在
- **两栏（≥1000px）**：左 = 哪些词（①科目 ②单元范围）· 右 = 怎么考（③模式 · 模式设置 ·
  ④题数与时长），`生成擂台码` 横跨两栏在底部。
  ⚠️ **断点是 1000px 不是 1080**：1024×768 的学校 Chromebook 是这一页最窄的机器，
  实测两栏能把 ①–④ 全部放进一屏，堆叠反而把 ③ 推到折线下。
- **单元范围 = 学生那张卡的同一套**：`已选 N 个单元 · 共 N 词` 与 全选/清空 共用标题行 ·
  年级独占式折叠（一次只开一级）· 单元卡显示 单元名 / 主题 / N词，选中转金 · 固定高度滚动盒。
  值是从 `css/cs.css` **抄来的**，源码里逐条标着 ⟨cs.css⟩，与 teacher.html 本来就抄了调色板
  是同一种刻意重复（§17）。⚠️ **cs.css 改了这里要跟着改。**
- ⚠️ **点单元、开合年级、切模式一律不重render**：只翻 class + 更新计数（`updateSum()` /
  `syncRight()`）。重建那个盒子会把滚动位置和「开着哪一级」一起丢掉，而连点六个单元是常态。
- ⚠️ **五个模式排 3 + 2，不是 wrap flex 的 4 + 1**（`.ar-tiles.m3`，列数写死）：
  wrap 会让 词雨灵露 独占一整行，读起来像它是特例。三列正好切成「三种答题 / 两个游戏」。
  每张模式卡多了一行说明（`ARENA_MODE_SUB`）——原来五颗裸药丸里 华文解释 与 英文翻译 一眼分不出。
- **题数 / 时长 改成滑杆**（`qtySlider`/`wireQtySlider`，从 `cs.js` 移植，range 索引在**允许值**
  上所以拖不出非法值）。⚠️ **四个可选值与原来的 `<select>` 逐个相同**（10/15/20/30 题 ·
  3/5/8/10 分钟）：换的是控件，不是范围——放宽上限是 owner 的决定，不该是一次版面调整的副作用。
- **难度四档改成与学生端同一把梯子**：⭐ 两个选项 / ⭐⭐ 三个选项 / ⭐⭐⭐ 四个选项 /
  ⭐⭐⭐⭐ 打字输入。⚠️ **key 仍是 `2/3/4/type`**（arena.js 读它），改的只是标签——
  原来把 打字 画成第四档却只给三颗星，与学生看到的梯子不一致。
- **①②③④ 是真的动线编号**（§7）：科目 → 范围 → 模式 → 题数 是一串必须依次做的决定。
  ⚠️ **模式设置那块内嵌面板不编号**——它是 ③ 的细节，不是第五步（和 §18p 的货架标题同一条）。
- ⚠️ **`STREAM_SUB` 另立一张表，不要并进 `STREAM_LABEL`**：后者印在表格和 擂台 顶栏上，
  两行的名字会把那一行撑坏。
- ⚠️ **没有拼音／英文注解**：这一页是教职员用的中文页，学生端那套闸门在这里不存在
  （`pyl()`/`enl()` 都在 cs.js 里，而 teacher.html 不加载它）。

### 验证
离屏 WebKit（§15）+ 一个 stub 掉 Firebase SDK 的 teacher.html 副本（真页面要账号才进得去）：
1920×1080 / 1400×1000 / 1024×768 三个尺寸截图看过；点两个单元 → 计数 `已选 2 个单元 · 共 102 词`；
换年级与切模式后选择仍在；`生成擂台码` 一路跑到 擂台 实况页，写出的 room 文档是
`{stream:g3, mode:cloze, tier:type, qCount:20, durationS:300, scope:[中一·单元一,中一·单元二],
wordIds:20}`，零 console 报错。
⚠️ **这一批与第十一批同在 `20260817i` 里出去**：teacher.html 的改动被第十一批那次
`git add -A` 一起带上了（commit 97e81e5），七处版本号也在那次一并推进，**不需要再推一次**。
⚠️ 教训：**同时开两个会话改同一个仓库时，`git add -A` 会把另一边没写完的文件一起提交**——
提交前看一眼 `git status`，或者只 `git add` 自己动过的文件。

---

## 18v. 航海徽章明细卡（owner 2026-08-17，落地 2026-08-18）

owner：「pier badges need the same amount of detail as mountain badges where possible -
whether it's earned, when it is earned etc」。徽章墙原来是**一张看不进去的图**：
九个 `<div>`，只有 已获得 或一条进度条，点下去什么都没有。现在整块瓦片是 `<button>`，
点开一张明细卡。

### ⚠️「where possible」那句话在做实事——有两样搬不过来，而且**不该**搬
- ✅ **搬得过来**：已获得／还没获得 · **首次获得的日期** · 进度条 · 还差几个词 · 大图。
- ❌ **「已获得 N 次」不搬**：山上的板块章可以靠 再次挑战 重复获得，而航海徽章是**里程表**
  （§12：绝对值 · 永不重新编号 · 没有终点徽章）。走过 50 海里就是走过了，
  不存在「第二次走到 50」。**印一个永远是 1 的次数是假的细节**，比不印更糟。
- ❌ **「已掌握 3 / 3 词」+ 词语 chip 列表不搬**：板块章对着一个**固定词集**，
  航海徽章对着**整个词库的一个门槛**，没有对应的词集。
  替代品是 `nextUnmet(6)`——**「还没认得的词语，例如：…」**。那是这一屏真正能回答的问题：
  不是「哪些词属于这枚徽章」（一个都不属于），而是「还差哪些」。
  ⚠️ 取自**整个词库**，不是 ①学习范围：门槛算的是全部词，缩到当前范围会给出一份
  **加起来凑不出这枚徽章**的清单。

### `store.sailLog` —— 日期从现在开始记
`badgeKey -> {first:"YYYY-MM-DD", at:海里}`。
- ⚠️ **单独一张表，和山上的 `badgeLog` 同一个理由**（§12/§13）：「有没有得到」必须继续
  **只由 `store.done` 推导**，所以这张表是**装饰**。丢了只丢一个日期，绝不会丢一枚徽章。
- ⚠️ **在 `noteRight` 里、`store.done` 写完之后调 `noteSailBadges()`**：
  这样 `met` 已经含上刚答对的那个词，跨过门槛的那一天就是今天，不会记成明天。
- ⚠️ **幂等**：已有条目永不覆写，所以重玩一局不会把日期往后推。实测答对更多题后日期不变。
- ⚠️ **旧徽章只能显示「日期未记录」，绝不回填猜的日期**——和山上对
  `badgeLog` 里没有条目的徽章完全一致。**一个编出来的「首次获得」比承认不知道更糟。**
- ⚠️ `at` 记的是**跨过门槛那一刻的海里数**，不是门槛值：两者通常相等，
  但一局里连中两个词时会差一个，而真实发生的数字才有诊断价值。
- 实测：9 词起步，答对一题跨到 10 → `{"shell":{"first":"2026-08-18","at":10}}`；
  27 词时 贝壳徽 显示 2026-08-12（已记录）、珊瑚徽 显示 日期未记录（无条目）、
  珍珠徽 显示 27/50 · 还差 23 · 六个真的还没认得的词。

### 其他
- ⚠️ **已获得的徽章不给按钮**（只有 关闭）：山上的 再次挑战 在这里没有对应动作，
  「去认词」对一枚已经拿到的徽章是废话。**没有动作的状态不要摆一个变灰的按钮**（§18l 同一条）。
- ⚠️ **未获得的「去认词」落在 学习挑战 的设定页，不直接开局**：徽章屏不知道学生想用哪种
  题型、哪个范围，替他挑正是 §4.4 那类静默失败的来源（没有图的范围、跑不起来的题型）。
  送到他已经认得的那扇门，所有守卫都还在。
- ⚠️ **`xhPop()` 支持 Esc 关闭**，并在关闭时摘掉监听：码头也跑在带键盘的 Chromebook 上
  （PLD），一个键盘用户关不掉的模态就是陷阱。
- ⚠️ **瓦片从 `<div>` 改 `<button>` 之后要显式重置字体与颜色**：按钮自带 font/color/居中，
  半继承的按钮就会渲染成 13px 系统黑体。
- ⚠️ **墙上加了一行「点一枚徽章，看它要认得几个词语」**：会打开东西的瓦片必须看起来会打开，
  而这一层的学生多半在触屏上——**hover 状态在那里根本不存在**，不能靠它当提示。
- ⚠️ `.sailbadge` 与 `.xh-bd-chip` 都已加进 `@supports not (backdrop-filter)` 回退块。

---

## 18w. 码头房间：同伴挑战（owner 2026-08-17，落地 2026-08-18）

owner：「add in the equivalent of 结伴登峰 同伴挑战 for pier - name them 结伴出海 同伴挑战」。
**同伴挑战 已上线；结伴出海 还没有**，理由在最后一节，是刻意的。

### ⚠️ 复用 arena.js，不另建一套
房间的**生命周期**——六位房间号 · 大厅 · 花名册 · 房主开始 · 快照订阅 · 断线重连 ·
颁奖台 · TTL——两个家族**一模一样**，好几百行。再抄一份就是这份代码库反复吃亏的
「两处要改、漏一处没有症状」。真正不同的只有**出题渲染**和**发奖钩子**，
而 arena.js 本来就是通过 `ctx` 拿这两样的。
- ⚠️ **连 `rooms/{code}` 集合都复用，所以不需要发布任何新的 Firestore 规则。**
  已发布的规则只看 `hostUid` / `pk` / `isTeacher()` / `status`，**从不看 `stream`**，
  所以一个码头房间原样通过。另建 `dockRooms` 就需要 owner 手动发规则，
  而 §16 已经列着好几块规则还没发布。
- ⚠️ **水线仍然完好（§4）**：房间文档带 `stream:"xh"`；arena 在 `doJoin` 里**按家族拦截**
  跨线加入（码头学生进不了山上的房间，反之亦然）；下面每一笔发奖都只写 `ws_xh`。
  xh.js 依旧不碰 `ws2_*`，cs.js 依旧不读 `ws_xh`。
  ⚠️ 拦截**按家族比较，不是按 stream 相等**：跨学段的山上房间是**刻意的功能**（§12，
  按词文本计分），不能被这条规则误伤。

### 计分：照常发（owner 2026-08-18 确认）
和 §12 一致：**答对进 航程，也照发 贝壳 与 航海值**。
- ⚠️ **走的是 `awardShells` / `awardSail` 本人**，不是另写一套：非首次答对的减半、
  ③难度 的倍率，全都原样生效。**房间因此不是刷贝壳的捷径**——这才是它安全的理由，
  不是「发 0」。
- ⚠️ **`entering`（连对数）刻意忽略**：码头任何地方都没有连对倍率，
  只在房间里发明一个，房间就会变成全码头最好的刷法。
- ⚠️ **航程 在一局结束时由 `conferMastery` 统一记**（arena 的既有设计），
  中途只涨贝壳与航海值。顺带调 `noteSailBadges()`，所以在房间里跨过的徽章门槛
  也会记下日期（§18v）。

### 🐛 顺手修掉两处**与代码相反**的注释
两处都写着「房间不发 历练值／灵露」，而 `roomCorrect()` 从 2026-08-14 起两种模式都在发，
§12 也是这么写的。它们比没有注释更糟——**我这次就是差点照着它们做的**。
- `js/arena.js` 文件头：「arena awards NO 历练值 and NO 灵露 (locked decision 2026-08-12)」
- `js/cs.js` PK_MODES 上方：「a PK round awards NO 历练值 / 灵露」

### 出题：只有两种，理由和沙滩快跑那次一样
`XH_PK_MODES` = **看图识词 · 英文选词**。连线 是一整盘不是一道题，词海钓鱼 要键盘，
两个句子玩法要场景底图——和 §18i 判断「什么能上沙滩」是同一条尺子。
- ⚠️ **`pic` 渲染器是新加进 arena.js 的**，因为这个文件里其他每一种都是「文字题干 → 词」。
  题干是**图片**，所以**绝不朗读、也不带任何文字**（§8.7：朗读永远不能泄题——
  一边给图一边念词就是把答案交出去）。这一屏刻意没有 🔊。
- ⚠️ **`startPlay` 里另有一份模式白名单**，加渲染器时必须一起加。只加了渲染器的那一版，
  每一个 看图识词 房间都掉进「该模式即将推出」——看起来像功能没做，其实是两处只接了一处。
- ⚠️ **干扰项由码头自己出**（`ctx.distractors`）：§5 是绝对的「干扰项永远取自同一个 组别，
  没有例外」，另有互斥黑名单；arena 的兜底是「优先同词性」，而码头的词根本没有词性。
  实测一道 热带鱼 的题，四个选项 鸡／热带鱼／熊猫／大象 全在 动物 组内。
- ⚠️ **`id` 就是 `词语` 本身**：码头从来没有词 id（`store.done` 按文本索引，§5），
  而 arena 按 id 建索引——两者取同一个值，房间的 `wordIds` 与索引就天然对得上。

### 结伴出海：**withheld 一个版本，随教师后台一起上线**（见 §18x）
它是**教师主持**的那一种。上线 同伴挑战 的那一版**故意没有渲染这颗药丸**：当时
`teacher.html` 只开得了山上的房间，学生在这里输入任何房间号都会得到「找不到这个擂台码」
——**那是一扇打不开的门，不是一扇暂时没人的门**（§4.4）。
下一版给教师后台加了码头房之后，药丸随同一次改动放出来。
⚠️ **这个先后顺序本身是规矩**：一扇门只有在它背后真的有东西时才上线。
若哪天教师后台失去码头主持能力，**把这颗药丸收回去**，不要让它在加入页失败。

### 其他
- ⚠️ **`XH_index.html` 现在要加载 `arena.js` 与 `podium.js`**。podium 是跟着来的：
  arena 用它显示前三名，缺了的话一局结束就是一块空面板。
- ⚠️ **房间药丸是 hero 的兄弟节点，不是子节点**：`.xh-hero` 本身就是 `<button>`，
  按钮套按钮是 §14 的第一条陷阱。为此加了 `.xh-hero-wrap{position:relative}`，
  **并把 two-col 的高度上限从 `.xh-hero` 搬到了 wrapper**——留在原处就会去限制一个
  已经不在那个 flex 容器里的元素，海滩又会把空间全吃掉。
- ⚠️ **`store.pkMode` / `store.pkDur` 是独立的记忆位**，不并进 `quizMode`／`runQuizMode`：
  §18m 拆开那两个就是为了防「在这扇门选的题型偷偷改掉另一扇门」。
- ⚠️ arena 断网时的提示语改成按 `ctx.stream` 分家：码头上说「结伴出海和同伴挑战需要联网」，
  再印 结伴登峰 就是山上的词跑到码头的屏幕上。
- ⚠️ **房间数量太少不开局**：范围内可出题的词 < 4 直接拒绝并提示，
  不开一个永远开不了的空房间（§4.4）。

---

## 18x. 教师后台开得了码头房间 · 结伴出海 上线（2026-08-18）

§18w 把 结伴出海 押后了一个版本，因为 `teacher.html` 只开得了山上的房间。这一版补上，
药丸同时放出来。

### 归一化在教师端做，下游一条代码路径都不用改
码头的数据文件是**另一种形状**：148 行的扁平数组，字段是 词语／拼音／英文释义／图档／
组别／子类，**没有** levels→units→components。`fetchPierJson()` 把它整成后台早就在用的
`{units, byKey}`，所以范围选择器、词数统计、`generateRoom`、`tryCreate` 全都不知道有码头
这回事。
- ⚠️ **一个假的「年级」：出发码头；单元就是八个 组别**。老师开码头房时想的是
  动物／食物，不是 中三·单元四。把 组别 当 level、子类 当 unit 会得到一个多数分支
  只有一个孩子的两层手风琴。
- ⚠️ **`id` 就是 `词语` 本身**，和 `js/xh.js` 一致：码头从来没有词 id（`store.done`
  按文本索引，§5），而房间的 `wordIds` 必须是码头那边查得到的东西。
- 实测：8 组，词数 动物 28 · 日常用品 17 · 食物 37 · 学校 14 · 天气与自然 14 ·
  交通 10 · 地点 15 · 数字 13，与 §5 逐格相符。

### ⚠️ `ARENA_STREAMS`，不是往 `STREAMS` 里加
`STREAMS` 被 `loadWordMap()`（走 levels→units→components）和「处理记录」的 科目 下拉
（清零 `ws2_{stream}`）读着。码头**两样都没有**——加进去两处都会坏。
结伴出海 只需要它出现在**一个**地方，所以那一个地方自己一份列表。
⚠️ **码头排最后**：四座山是日常，扫一行时不该让特例站在最前面。

### ⚠️ 换科目时必须把 `_setup.mode` 拉回合法值
`ARENA_MODES_FOR` 一张表管三处：模式行、默认值、生成时的守卫。
选着 填空挑战 切到 码头，如果不纠正，模式行里**一个亮着的格子都没有**（那个模式没有瓦片），
看起来像没选，而 生成擂台码 会造出一个码头根本渲染不了的房间。
**和 arena.js 自己那两份模式清单是同一类 bug（§18w）。** 实测两个方向都会纠正。
- 码头可用的只有 **看图识词 · 英文选词**，理由与 §18w 相同（连线是一整盘、垂钓要键盘、
  句子玩法要场景底图）。⭐档 与 词雨选项 两行按**模式**判断显隐，码头没有那两个模式，
  所以天然不显示——**不要再加一层 stream 判断去「修」它**。

### ⚠️ 看图识词 的池子要滤掉没有图的词
`数字` 整组**刻意没有图档**（§5：画三个苹果教「三」教的是苹果）。放一个进去，
房间里就是一个空画框配四个选项——§4.4 最纯粹的静默失败。实测 148 → 135，丢掉的 13 个
**全部**来自 数字。

### ⚠️ 范围里那些东西叫什么，跟着家族走
山上选 单元，码头选 组别。在一排 组别 芯片上印「已选 3 个单元」，
和在码头屏幕上印 结伴登峰 是同一类错——那个词根本不是老师正在看的东西。
`scopeNoun()` 管三处文案。

---

## 18y. 房间：一场只有一个结束条件（owner 2026-08-18）

owner：「Room/peer mode should not concurrently be able to limit both duration and number
of questions, not logical, should be either or.」**确实如此**：`teacher.html` 同时写
`qCount` 与 `durationS`，而 arena.js 两个上限都认——**先到的那个**收场。
于是 20 题 · 5 分钟 的房间里，老师设的两个数字**总有一个是谎话**：快班在第 20 题提前收工，
慢班答到第 13 题被钟掐掉。（同伴挑战 两家一直只有时钟，本来就是对的。）

- **房间文档新增 `limitBy: "qty" | "time"`，只写生效的那一个上限**：
  按题数 的房间**不写 `durationS`**。一个躺在文档里不生效的字段，下一个读它的人会当真。
- ⚠️ **缺 `limitBy` 的旧房间一律当 `"time"`**，行为与从前一字不差。
  （题目答完仍然收场——那是 `renderQ` 的序列走完，不是第二个上限。）
- ⚠️ **按题数 的房间根本不建 `tick`，不是「把倒计时藏起来」**：留着它跑，
  题目没答完照样被掐，等于第二个上限换了个地方活着。HUD 上那颗 ⏱ 一并不渲染——
  剩多少就是卡片底部那行「第 X / Y 题」，同一屏上已经说清楚了。
- ⚠️ **按时长 的房间要发足词**（`ARENA_TIME_POOL = 80`，此前只发 `qCount` 个）：
  只发 20 个，快的学生第 20 题就收场，那就是把题数偷偷变回上限。
  40 是 同伴挑战 两家用的数（8 分钟也答不完），教师端给到 80。
- ⚠️ **词雨灵露 钉死在 按时长**：词是循环落下的，没有「一题」可数。
  教师端把 按题数 那格禁用并写明理由，`syncRight()` 顺手把 `_setup.limitBy` 拉回合法值——
  和 §18x 换科目时纠正 `_setup.mode` 是同一类：**一个选不中的格子看起来像没选**。
- ⚠️ **两处显示要跟着分家**：arena.js 的 `scopeLine()`（学生大厅）与教师端实况页的抬头，
  原来都印「N 题 · M 分钟」。现在按 `limitBy` 只印生效的那一个。
  `arenaRemaining()` 在 按题数 时返回 null，否则会印一个永远是 0s 的钟。
- 🐛 顺手补上 `scopeLine()` 的 `pic: "看图识词"`：码头房间的大厅一直把模式名印成 `pic`
  （§18w 加了渲染器，漏了这张表——**又一次「两处只接了一处」**）。
- 实测（stub 掉 Firebase 的 teacher.html 副本 + 无缓存 http server）：
  默认 按时长 → 写出 `{limitBy:"time", durationS:300, wordIds:80}`；
  切 按题数 · 30 题 → `{limitBy:"qty", qCount:30, wordIds:30}`，**没有 durationS**；
  切到 词雨灵露 → 自动回 按时长、按题数 变灰、说明出现；两种状态下 生成擂台码 的
  纵向位置**完全相同**（滑杆是替换不是叠加）。

### 屏幕上的话要与代码一致（owner 2026-08-18，同一批的第二问）

「不计历练值」这句话在两块**学生和老师真的会读到**的屏幕上活了四天：那是 2026-08-12 的规则，
owner **2026-08-14 就推翻了**（§12：房间照常发 历练值 + 灵露 + 掌握），代码从那天起一直在发。
§18w 修的是**注释**里的同一句，这次修的是**屏幕**上的。
⚠️ **说不发、账上却在发，比没有这句话更糟**：学生以为擂台白打，老师照着它跟全班这么说。

- **arena.js 结果页**：改成说出真的发生了什么，并且**按家族分家**（§4 水线）——
  山上「答对的新词已计入「已掌握」（海拔 +N 米）。历练值与灵露已照常发放。」，
  码头「…已算进航程（航程 +N 海里）。贝壳与航海值已照常发放。」。
  ⚠️ 印 海拔 在码头的结果页上，就是 §18w 那条「山上的词跑到码头的屏幕上」。
- 🐛 **那个 +N 一直是错的**：印的是 `correctIds.length`（答对几题），
  而**早就掌握过的词答对多少次海拔都不动**。现在 `conferMasteryFromRoom` 与码头的
  `conferMastery` 一样**返回新增数**，结果页印它。
  ⚠️ 全是复习词时印「这些词你本来就掌握了，海拔没有变」，**不印 +0**。
  ⚠️ 一个学生在 我的词山 上核对不到的数字，比不印数字糟。
- **加入房间那一屏**（`renderJoin`）原来写「🏔️ 结伴登峰 / 请老师在白板上写出擂台码」，
  而**这一屏是四种房间共用的入口**：老师开的（结伴登峰／结伴出海）与朋友开的（同伴挑战），
  山上码头各两种。⚠️ **进来之前无从知道是哪一种**（码就是六个字符，`pk` 要读到房间文档才知道），
  所以标题只说家族、副标把两条来路都说出来：「输入 6 位房间码加入：老师写在白板上的，
  或朋友给你的。」——原来那句话对着一个点了 加入朋友的房间 的学生，
  说的是一件不会发生的事。
- **教师后台**：出题设置的标题与实况页的抬头改成 `arenaName(stream)`——码头房叫 **结伴出海**
  （§18x 之后这块屏幕开得了两家的房，标题却一直写着山上那个名字）；
  说明改成 `arenaRewardHint(stream)`，两家各说各的货币，并写明「与平时同一套衰减／上限，
  所以房间不是捷径」——**这才是房间安全的理由，不是「发 0」**。
  ⚠️ 实况页抬头用 `room.stream || _setup.stream`：第一帧快照还没到时 `room` 是空对象，
  否则刚开的码头房会闪一下 结伴登峰。
- ⚠️ **教师后台的分页仍然叫 结伴登峰**，而它现在通往两家。改名是 owner 的决定，
  这次没动——记在这里，免得下一次又当成新发现。
- ⚠️ `guide.html` 已核过：「房间照常计分，所以它不是捷径」三处都是对的，不必改。

---

## 18z. 教师后台 学生档案卡 · 后台重建进度码 · 码头进度上云（owner 2026-08-19）

owner：「I need progress code for each student to be reflected in the teacher.html.
can it be redesigned such that when I click on a student, I can see their profile page?」

### 🐛 先修一件更根本的：**出发码头从来没有云端备份**
`xh.js` 只推 `dockScores`（一行窄榜单：昵称 + 航程 + 航海值），**`ws_xh` 一个字节都没上过云**。
两个后果都是真的：码头学生清了浏览器就全丢、**没有任何找回的路**；而 §18r 的进度码
在后台**根本拼不出 xh 那一段**，因为服务器上没有那份资料。
现在 `save()` 走 `scheduleCloudSync()`（2.5 秒防抖 + `pagehide` 冲刷，照抄 cs.js）。
- ⚠️ **水线没有被跨越（§4）**：承重的保证是「cs.js 从不读 `ws_xh`，xh.js 从不碰 `ws2_*`」，
  **两个引擎都没有新增一行**——这是 xh 把**自己的** store 写进**自己的** key（`progress.xh`），
  和 cs.js 写 `progress.{stream}` 完全同构。没有换算，不暗示汇率，cs.js 仍然没有路径读到它。
- ⚠️ **防抖是承重的，不是整洁**：连线 每连一对就 `save()` 一次，不防抖就是一场写入风暴。
- ⚠️ **旧资料不会追溯上云**：学生下次打开码头才会补上。后台因此要说出「这名学生的码头进度
  还没同步」，而不是让老师读成「码头进度丢了」（见下）。

### 学生档案卡（点任一行）
`班级视图` 是一堵 14 栏的墙：它回答「全班怎么样」很好，回答「**这一个**学生怎么样」几乎不回答——
四栏 掌握 是里面仅有的 per-student 数字，而 历练值 · 灵露 · 段位 · 徽章 · 个人纪录 ·
连续学习天数 · **整个出发码头**早就载进浏览器了，一个都没显示过。
- **五片陆地各一张卡**：山上印 海拔／历练值／本学期／灵露／最高连对／成就徽章／对战奖牌／
  连续学习 + 个人纪录；码头印 航程／航海值／贝壳／读过 N 句／航海徽章。
- ⚠️ **码头不是第五座山**：另一套词、另一套标签、**没有段位**，三个数字永不同行、永不相加（§4.1）。
- ⚠️ **没打开过的陆地整张卡不画**。四张「没有资料」正是这堵墙难读的原因，重复四次不是信息。
- ⚠️ **`Object.keys(badges).length` 不是 A 层徽章数**（§12）：对战奖牌 `b·` 在同一张表里，
  直接数会让每个在房间里拿过名次的学生凭空多出徽章。这里用的是 cs.js `achBadgeCount()` 那条规则。
- ⚠️ **卡片自己滚，overlay 不滚**：`.tov` 是 `align-items:center`，而 §18n 就是那道疤——
  居中的 flex 容器一旦内容更高就**向上下同时溢出，上半截永远滚不到**。给卡片
  `max-height + overflow` 之后 overlay 根本不会溢出，**陷阱是不存在，不是被绕开**。
- ⚠️ **档案 按钮对每一位老师都渲染，不只 HOD**：整行可点，但一个没有可见控件的点击区
  对不知道它存在的人就是不存在；而非 HOD 老师**原本连 操作 栏都没有**。
- ⚠️ **行内每颗按钮都要 `stopPropagation()`**，否则 复制／编辑 会**同时**在老师刚按下的
  那颗按钮后面弹出档案卡。
- ⚠️ **Esc 可关**：后台跑在 SSOE 笔电与 Chromebook 上，键盘用户关不掉的模态就是陷阱（同 §18v）。
- ⚠️ 头像按 `art/avatar/avatar_{id}.png` 拼（21 个全部符合，已核），但 `<img>` 带 `onerror`
  隐藏自己：将来一个不守约定的 id 退化成「没有图」，而不是学生名字旁边一个破图标。

### 后台重建进度码（VS3）
学生丢了设备就没有码可以贴——现在老师从后台读一段给他。
- ⚠️ **格式是 profile.js 的，逐字节相同（§18r）**。这是这套线格式的**第二个写入者**，
  所以**永远不能只改一边**：`_fnv1a` / `bitsToB64` / `utf8ToB64url` 是从 profile.js
  **刻意复制**过来的（teacher.html 不加载任何共享 JS，与它本来就抄了调色板同一种重复，§17）。
  **改一处要改两处**，漏了的症状是**学生自己的设备把这段码判成「已损坏」**。
- ⚠️ **位图按已发布词序定位**，所以 `loadWordMap()` 现在在同一趟遍历里记下 `_wordOrder`——
  **必须自上而下读文件**，绝不能用 `Object.keys(_wordMap)`（那是引擎的实作细节）。
- ⚠️ **没有 store 的陆地整段省略，绝不写成空的**：空段在另一台设备上会恢复成「零个已掌握」，
  而合并之所以安全全靠它是并集。
- ⚠️ **码里没有货币**，与 profile.js 同一条线：花 200 灵露买了头像，再恢复一段旧码，
  东西和钱都在（§18r）。
- ⚠️ **面板要说出「为什么没有码头」**：`xh` 段缺席时写明「还没同步到云端，学生下次打开码头
  会自动补上」。不写的话，码头学生的老师会读成「码头进度丢了」。
- ⚠️ **`CODE_LABEL` 另立一张表，不要并进 `STREAM_LABEL`**（§18r 同一个理由）：后者印在
  表头与擂台顶栏上，两行的长名字会把那一行撑坏；而 `xh` 那一行短表根本不该有。
  ⚠️ **`g2` 写的是「G2 普通学术华文」，与 profile.js 逐字一致——那是刻意的**，两个写入者
  必须对得上。⚠️ 但它与 §1 的「G2 华文」不符（profile.js 那一条已经过时），
  **要改就两处一起改**，别只改一处。
- 实测（离屏 WebKit + no-store server，§15）：**21 条断言全过**——teacher.html 出的码由
  **真的 profile.js** 解开，G3 的 36 个 id 与码头的 7 个词逐个相同、`readLines` 相同、
  纪录（最高连对 11 · 词雨 160 · 汉兜 4 · 组词 24 · 竞速 20）相同、昵称回得来、
  **改一个字符即被拒**、**按邮件宽度硬换行后照样解开**、没有 store 的档案**不出码**
  （而不是出一段等于零的码）。
  ⚠️ 顺带验证了一条**正确的**行为：词表里没有的词与句库里没有的 id **编不进码**
  （位图只覆盖已发布的位置）。测试里 8 → 7、3 → 2 的落差全部来自我自己编的假资料。

### 🐛 日期筛选的**结束日**在 SSOE 笔电上看不见，Mac mini 上正常
owner 报的是两台机器不一样——那正是线索。`.fdd` 宽 262px 写死，`.frange` 是一行 flex
装两个 `input[type=date]`，而 **Chrome/Windows 画的日期控件比 Safari 宽得多**
（mm/dd/yyyy + 上下箭头 + 日历钮），**flex 子项不会缩到自己的固有宽度以下**
（§14「grid/flex 子项不缩」）——第二个框被整个推出下拉框外并被裁掉。
- 改成**上下两行**（从 / 至 各占一行）+ `min-width:0`。只加 `min-width:0` 能治溢出，
  但会留下两个宽度**随平台变化**的窄框；两行在每台机器上都一样。
  ⚠️ 轨道写 `minmax(0,1fr)` 不写 `1fr`，同一条。
- 实测复现并证明：模拟 Chrome 那种 150px 固有宽度的控件，**旧的一行 flex 把结束日推出右边缘
  69px**（正是 owner 看到的），新的两行版两个框都是 216px、各留 1px 余量。
- 🐛 顺带补上**纵向**夹取：`最近活跃` 是最后一栏，它的 ▼ 在满表时靠近屏幕底部，
  下拉框原来会开到折线以下，**应用 按不到**。现在下方放不下就翻到按钮上方。

### ⚠️ 仍然待办 / 已知
- **`班级视图` 仍然是 14 栏横向滚动**。四栏 掌握 对绝大多数学生只有一栏非零，
  英文提示／提示趋势 对绝大多数学生是「—」或「数据不足」。**收窄没有做，等 owner 裁定**
  （见交接时给的三个选项）。
- **载入/刷新数据 仍然是手动的**，`（不自动刷新）` 是刻意的（248 份档案 = 248 次读）。
- **没有在真的 SSOE 笔电上验过**：日期那一条是在 WebKit 里**模拟** Chrome 的控件宽度证明的，
  修法是结构性的（不再依赖控件有多宽），但**owner 那台机器上要再看一眼**。

---

## 18aa. `segPy` 回填 · 组词成句的拼块拼音（owner 审毕 2026-08-19）

依据 `~/Downloads/segPy_implement_20260819/`（含 361 行合并源、两份交接文）。
owner：「I've vetted the 131 lines for the pier, all ok」——`改成` 一栏 202 条**全空**
＝ 采用全部建议值，已用脚本核对，**不是「还没填」**。

### 数据
- 合并源是 **`all_by_sentence_361.csv`**（每句 · 每块的展开表），**不是 202 条去重表**：
  `句子 id` + `第几块` 直接对上 `seg[i]`，不必按词块反查。
- ⚠️ **合并前先证明位置对得上，不是形式**：361 行的 `词块` 列逐块拼回去必须**等于该句的
  `seg` 数组本身**。实测 **73 句全部逐块相同、`第几块` 都是 1..n、361 == seg 块总数**。
- ⚠️ **17 条没有 `seg` 的句子不加 `segPy`**（7 条 display-only · 9 条含逗号 · 1 条 `老师来了！`），
  这是正常状态。实测「有 seg 无 segPy」「无 seg 却有 segPy」「长度不等」**三项都是 0**。
- 音节自检（§10）：361 块**音节数 == 汉字数，0 失败**；202 个不重复词块**没有一个出现两种读音**。
- diff 是 **507 增 0 删**（73×2 括号行 + 361 块行），所以文件其余部分一个字节都没被重排。

### 代码
- ⚠️ **`segPyOK(p)` 是独立的判断，没有并进 `segOK()`——交接文 §4 让并进去，这里刻意没照做。**
  `segOK()` 决定这句**能不能玩**；把拼音检查放进去，缺 `segPy` 的句子会**整句从玩法里消失**，
  静默缩小句库。而拼音在这里是**可选脚手架不是内容**（交接文 §5 自己这么写的），
  正确的失败是**降级不是丢弃**——那也正是交接文 §4 用文字描述的结果
  （「否则这一句整句不发拼音」），只是它把这条检查填错了地方。
  今天 73 条全过，所以两种写法**现在看不出差别**；差别会在有人加一条只有 `seg` 的句子那天出现，
  症状是那句话**再也不出现**，而且不报错。
- ⚠️ **整句闸门，绝不逐块判断**：半盘有拼音 ＝「有拼音的那几块是真的」。
  干扰块取自词表（查得到拼音），句子块是 `seg` 切出来的普通词语（多半查不到）——
  这个不对称正是这份数据必须手工审的理由（§18m），逐块兜底等于把它从另一头重建。
- ⚠️ **还有一道整盘扫描**：万一某个干扰块没拿到拼音，它就是整盘唯一没注解的那一块——
  同一个泄漏从另一端重建。现在只要有一块缺，**整盘一律不发拼音**。
  （实测 148 个词条**没有一个缺 `拼音`**，所以这条今天不会触发；它是为它触发的那天写的。）
- ⚠️ **走 `body.xh-py-on` 闸门，不用 `.xh-always`**：词语闪卡 是先学后测，拼音是内容；
  组词成句 是测验，拼音是可选脚手架，必须跟随学生自己的开关。
- ⚠️ **槽位改 `innerHTML` 并重新发出注解**（§14「textContent 抹掉注解」）：槽位每点一下就重写，
  用 `textContent` 会让**放进句子里的块掉拼音**，于是盘上有、句子里没有。
- ⚠️ **CSS 每一条都带 `body.xh-py-on` 前缀**（§18e）：闸门是 (0,2,1)，
  不带前缀的 `.xh-sort .xh-tile-w .xh-py` 是 (0,3,0)——**赢得过「关」那一半，输给「开」那一半**，
  覆盖静默失效。这个坑已经在 🔊 标签和 看句选词 英文上各踩过一次。
- ⚠️ **组字成词（`.xh-build`）不发拼音**：它的块是**单个字**，而它的答案就是一个词的读音——
  逐字注音等于把答案按顺序写出来（§18i 它答题前没有喇叭，同一个理由）。
  `renderBuild`／`buildTiles` 是另一条路径，这次一个字节都没动。

### 验收（真浏览器实测，不是只跑断言）
1. `segPy.length === seg.length`：**73 条全过**。
2. 音节数 == 汉字数：**361 块全过**。
3. **拼音关**：`.xh-py` 可见数 **0**；span 仍留在 DOM 里（CSS 闸门不重绘，§10）。
   ⚠️ 盘上仍有一行拉丁字母——`p.en` 那句英文题干，它是 `.xh-en .xh-always`，
   **是题目本身不是注解**（§18g 的既有豁免），跟着英文开关走，与拼音无关。
4. **拼音开**：一局 菜市场 · `妈妈要买两棵大白菜`，7 块**全部有拼音**
   （5 个句子块 + 2 个干扰块 咖喱／面包）——**认不出哪块是干扰项**，这一条才是整件事的验收点。
   放进槽位之后拼音仍在（妈妈/mā mā · 要/yào），拼音与汉字**居中偏移量实测 0**。

### ⚠️ 两条轻声读音留给 owner 定夺（没有擅自改）
owner 已审毕，所以**一个字都没动**，但这两条与词典的轻声形不同，且都出自
pypinyin 的「生成」那一批——与 §18d 修过两次的「地 dì」「得 dé」是**同一族**：
- **喜欢 `xǐ huān`**（词典 `xǐ huan`，**出现 6 次**）
- **妈妈 `mā mā`**（词典 `mā ma`，出现 2 次）
另外三个同类词块是对的：便宜 `pián yi` · 衣服 `yī fu` · 朋友 `péng you`。
⚠️ 真要改就**同时改 `data/xh_phrases.json` 与那张 CSV**，别只改一处。

---

## 18ab. 教师后台：学习支援 一栏取代 英文提示＋提示趋势（owner 2026-08-19）

owner 两句话推同一处：「can I also see if they've toggled pinyin on?」与
「I'm also not sure what is 提示趋势 … is that pertaining to wordle? if so we can remove that」。

### ⚠️ 提示趋势 **不是** 词语汉兜，而且它**测错了东西**
它是 **英文提示的淡出遥测**（这名学生是不是在慢慢不需要英文支援）。
owner 追问：「数据不足／观察中 is not helpful … will it stamp when did they turn on the
support and when they turned it off?」——**当时不会，一个日期都没有**：`enTel` 全是计数器
加一个 10 次的滚动布尔窗口。
- ⚠️ **`enTrend` / `enTrendVal` / `EN_TREND_DELTA` 已整段删除，不是改文案**：那个指标
  **要十个 session 才说得出话**，而且**把「第一天就关掉英文、再没开过」的学生评为「持平」**
  ——那是这个功能最好的结果，却被印成一个中性词。
- **改成报告事件**：现在是什么状态 · 从什么时候起 · 之前变过什么。**第一次翻转就答得出来**，
  所以「数据不足」这个问题是**消失了**，不是被换了个说法。
- ⚠️ **`last10Sessions` 与 `regressionAt` 仍在 cs.js 里采集**，只是这个读数没了。
  `regressionAt` 以「🔁 最近曾关掉后又开回来」保留在档案卡上——**对 2026-08-19 之前的老学生，
  它是唯一还看得见的信号**（他们的日志还是空的）。
  ⚠️ 它是 **session 序号不是日期**，**永远不要把它印成「N 月 N 日」**。

### `enTel.log` / `pyTel.log` —— 变化日志（cs.js）
`[{d:"YYYY-MM-DD", on:1|0}]`，**只在状态真的翻转时追加**，所以它是一部决定史，不是一部开页史。
- ⚠️ **拼音以前一点遥测都没有**。现在**各自一份日志，不合并**：这是学生两个独立的决定，
  一个数组装两种开关会让「他关掉的是哪一个」变成一道解析题。
- ⚠️ **日期从 2026-08-19 起记，永不回填**（§18v 同一条）：老学生的日志是空的，
  后台写「未记录」而不是编一个日期。**编出来的「开始使用」比承认不知道更糟。**
- ⚠️ **第一行永远不裁**：满 30 条时删的是**第二行**。「什么时候开始用的」是唯一没有别处可查的事实，
  近期翻转还能从尾巴读到；从头裁会悄悄抹掉每个长期学生的起点。
- ⚠️ **同日来回翻转会抵消**：当天关了又开，到那天结束什么都没变；两行同日期相反值
  在老师屏幕上读起来是自相矛盾。
- ⚠️ **云端合并按 (日期, 状态) 取并集，不是「取长的那一份」**：学生真的会在学校 Chromebook
  和家里 iPad 上各翻各的，取长的那份会**静默删掉另一台的历史**。并集可重复执行不变
  （与 §18r 进度码恢复安全的性质同一条）。合并后要**重新塌缩**，否则两台各自「开」会拼出
  一次根本没发生的翻转。
- ⚠️ **`AID_LOG_CAP` 声明在 `mergeAidLog` 上方**，而不是挨着 2,700 行之后的 `aidLogPush`：
  它现在只从云回调里被调用所以拿得到值，但这份代码库已经为同一个形状留过疤
  （xh.js `load()` 用字面量而不用 `ROUND_SIZES`／`OPT_TIERS`，因为 load 在模组初始化时就跑）。

### 后台怎么读
每片陆地各一块，**英文提示 与 拼音辅助 各一行**：开／关 · 从什么时候起（几天） ·
一句可执行的话 · 以及翻转轨迹 `开 → 8月9日 关 → 8月17日 开`。
五种判词：**一直开着**（还没试过关掉，可以鼓励他试一次）· **已经关掉**（≥14 天＝可以视为断奶了 /
刚关掉不久，留意会不会再开）· **关掉后又开回来**（值得看一眼，可能是更难的单元）·
**一直没开过** · **未记录**。
- 🐛 **`aidVerdict` 必须收 `name` 参数**：第一版把「不需要英文支援」写死，
  于是任何从没开过拼音的学生，**拼音那一行底下印着「不需要英文支援」**。
  **一个被两件事共用的判断，不能点名其中一件。**
- ⚠️ 「一直没开过 / 一直开着」后面印的是**「有记录以来」不是「起」**：那个日期是**开始记录**的日子，
  不是学生做决定的日子，当成起始日印出来就是宣称我们知道一件并不知道的事（§18v）。

### 拼音状态：原来**哪里都没有**
- 新的一栏 `学习支援`：`拼 开/关` + `EN 开/关` 两枚芯片，可筛选。
- ⚠️ **HCL 印「拼 — · EN —」，不是「关」**。**两个辅助在 HCL 上是刻意不发的**
  （§10；owner 2026-08-19 再次确认：「there kids are supposed to be the selected group
  better in chinese」）——`enAidAvailable()` 就是 `pyAidAvailable()`，两个一起没有。
  两个旗标仍被归一化成 false，直接读会给每个 HCL 学生印一个 `关`，
  读起来是**「他关掉了」而不是「这里根本没有可关的东西」**。
  ⚠️ **这次只是读旗标做报表，没有给 HCL 加任何辅助，也永远不要加。**
- ⚠️ **码头的键与默认值都不同**：`store.py`／`store.en`，且**两个默认开**（§10），
  而山上是 `pyAid`／`enAid` 默认关。用山上的键去读 `ws_xh` 会把每个码头学生报成 `关`。
- ⚠️ 表格一栏只放得下一个答案，所以取**主力陆地**（掌握／认得最多的那一片），
  tooltip 写明依据；**档案卡里逐片陆地列全**，因为两边默认值相反，一行摘要对某一侧一定是半真话。
- 顺带删掉因此没人调用的 `enStatus()`（§18p：留着不引用的存取器比没有更糟）。

---

## 18ac. 排行榜：自己那一行发光，不再挂一个「你」（owner 2026-08-19）

owner：「the student's own nickname has 你 behind it, that's weird … make the whole bar
stand out … I don't want 你 to be there」。

- **`· 你` 已从 `js/cs.js` 删除**。它**读起来像名字的一部分**，而那是整块榜单上
  **唯一一个学生自己没有选的名字**。⚠️ **出发码头从来没有加过**，所以这次是山上向码头对齐。
- **旧的高亮撑不住这个删除**：`outline:2px + 平涂 #FDF6E4` 与普通白卡在教室投影上几乎一样，
  那正是当初要往昵称后面焊一个字才找得到自己的原因。
- **现在四个信号，不是一个**：金色渐变底 · 实心金边 · 外发光 · **前缘一道金条**。
  ⚠️ **只靠颜色对色盲学生无效，对褪色的投影也无效**；前缘那道条与整体明度差在两种情况下都活着——
  **这才是删掉「你」之后仍然安全的依据**。实测灰阶下仍然一眼可辨（截图看过）。
- ⚠️ **刻意不做动画**：§7 把装饰排在行为后面；榜单每次切范围／切榜都会重绘，
  常驻动画会和自己的重绘打架；而 G1/G2 学生正是老 iPad 的用户。
- ⚠️ **用 `inset` 阴影不用 `outline`**：老 WebKit 的 outline **不跟随 border-radius**，
  旧规则在最该照顾的那批 iPad 上画的是一个方框套着 12px 圆角卡；它还在盒子外面，会和相邻行相撞。
- ⚠️ **`.me` 写在 `.gold/.silver/.bronze` 之后**（§14 同优先级后者赢）：第一名看自己的榜单时
  仍然拿到「自己」的样式。这个方向是对的——学生问的是「哪一行是我」，奖牌在名次栏里已经说了。
- ⚠️ **`.lb-uid` 在这一行要换成暖灰**：`#9FB3C0` 是给白卡调的（在白卡上本来就只有 2.17:1），
  压在金底上只有 **1.87:1**——偏偏这是它真正干活的那一行（区分两个同昵称的学生）。
  现在 **4.20:1**。昵称 8.61:1、数值 5.11:1。

---

## 18ad. 进度码去尾零：同一个格式，短一半到十四分之一（owner 2026-08-19）

owner：「is there a way for the student to enter a shorter code but still get their
progress restored?」

`bitsToB64` 给**该学段的每一个词**都发一个 bit——G3 是 1069 bit，不管学生掌握了 30 个还是
900 个。**最后一个已掌握词之后全是零，却要学生一个字一个字打出来。**
现在把**末尾整个为零的字节丢掉**。

| 学生 | 原来 | 现在 |
|---|---|---|
| G3 新生 · 30 词 | 179 | **12** |
| G3 学期中 · 200 词 | 179 | **42** |
| G3 强 · 600 词 | 179 | **108** |
| 一个只碰过 G3 的轻度学生（整段码） | 213 | **37** |

- ⚠️ **不必升版本号，理由很精确**：`b64ToKeys` 读的是 `bin.charCodeAt(i >> 3)`，
  越界返回 `NaN`，而 JS 里 `NaN & x === 0`。**短掉的 mask 解出来就是「这些词没掌握」**——
  正好就是被丢掉的那些零字节的意思。已在 WebKit 里拿**未修改的解码器**实测：
  30 / 200 / 600 三种掌握量下输出逐字相同。
  所以**学生邮箱里已有的码照常能用，这里新出的码老版本也读得懂**。
  ⚠️ **不要「整理」成把 mask 补回满长度。**
- ⚠️ **`n` 字段仍然是该陆地的完整词数**，它是用来校验 mask 长度、也是 `b64ToKeys` 的上限，
  **不是 mask 的长度**。
- ⚠️ **最坏情况不会变短，这是诚实的**：掌握了词表最后一个词的学生，尾部没有零可丢。
  测试里那个「重度」样本正是这种（296 → 286，只有码头段缩了）。
- 🐛 **两个写入者，我第一次只改了一个**：`profile.js` 与 `teacher.html` 各有一份
  `bitsToB64`（§18z 那种刻意重复）。只改前者时**码照样能解**（两份本来就可互换），
  **唯一的症状是老师发出去的码悄悄比学生自己生成的长一倍**。
  这正是那条「改一处要改两处」警告说的失效模式——它没有报错，只是安静地做错。

---

## 18ae. 恢复码 claims/{code} · 「restore everything」（owner 2026-08-19，**建设中**）

owner 三句话定了这件事：
1.「is there a way for the student to enter a shorter code but still get their progress restored?」
2.「**override VS3, let the students restore everything**」
3.「the pairing also prevents exploitation, the UID and progress code should match」

### ⚠️ VS3 的「码里没有货币」这条规则**已被 owner 推翻**
§4 与 §18r 写着「码里没有任何货币（灵露／贝壳／历练值／航海值）」，理由是
**花掉再恢复旧码就等于东西和钱都在**。owner 2026-08-19 裁定：设备送修／更换在学生里够常见，
**永久丢掉经济数据是真实的动机损失，而且投入最多的学生损失最大——激励方向是反的**。
- ⚠️ **那条理由本身没有被推翻，只是不再靠「不带货币」来兑现**。重复恢复必须仍然无害，
  靠的是 VS4 交接文的**全新账号闸门**（virgin gate）：
  **单调字段**（掌握／认得／纪录／拥有）照旧按并集／取大合并；
  **快照字段**（灵露／贝壳／items／equip／berth）**只写进一个全新的账号**。
  要再领一次，学生得先把账号清回全新——而那会**同时毁掉他买的东西**，净收益为零。
- ⚠️ **快照必须整批原子写入**。恢复钱包却把「拥有」按并集合并，头像就能活过那次清空，
  重复漏洞立刻回来。**这是这套设计里唯一承重的决定。**
- ⚠️ **`store.stats` 仍然不进码**（大、不是进度、只增不减对一个比率兑现不了）。

### 为什么是 `claims/{code}` 而不是「用 claimCode 查 users」
⚠️ **我一开始提的方案是错的，而且是不安全的**，记在这里免得下次又提：
**Firestore 规则看不到查询的 where 子句**。`allow read` 同时覆盖 get 与 list，
所以要让新设备跑 `users.where("claimCode","==",x)`，就必须放开 users 的 **list**——
那等于**让任何登录用户列出每一个学生的完整进度文档**。
**没有任何规则能表达「只有在按 claimCode 过滤时才允许」。**
→ 改成**用码本身当文件 ID**：读取变成 **get 一个你必须已经知道 ID 的文件**，
`list` 显式拒绝。`users/{uid}` 的规则**一个字都不用改**。
- ⚠️ **`allow list: if false;` 是刻意写出来的**，虽然不写也默认拒绝：
  将来谁图省事写成 `allow read`，就会**把这个码从一个秘密变成一个索引**，
  把每一份 payload 都暴露出去。**永远不要放宽成 `allow read`。**
- ⚠️ **拥有者的 uid 存在文件里面**，写入规则才检查得了：create 要求你盖自己的 uid，
  update 要求这份文件本来就是你的——没人能占用或覆盖别人的码。
- ⚠️ owner 要的「UID 与码要配对」**由结构本身满足**：一个码只存在于一份文件上，
  所以它**就是**那个配对，**28 位的 UID 根本不必打**。

### 码长什么样
- **10 个字符，字母表 `23456789ABCDEFGHJKMNPQRSTUVWXYZ`（31 个）**。
  ⚠️ **没有 0 O 1 I L**：这个码要从屏幕上读出来、由十一岁的孩子打进去，
  这五个才是真正会抄错的。31^10 ≈ 8.2×10^14。
- ⚠️ **用 `crypto.getRandomValues`，`Math.random` 只是兜底**：这串字符是陌生人与
  一个学生账号之间唯一的东西，而 `Math.random` 不是密码学随机源。
- ⚠️ **码是秘密**：不记 log、不上任何榜、**绝不由 uid 派生**——派生的话，
  排行榜已经印出来的 8 位 uid 前缀就能拿来伪造。

### 写入节奏
⚠️ **走 session 冲刷（pagehide），不走 2.5 秒的进度防抖**：把整份 store 在每次 save 时
再镜像一遍，会**让这个项目的 Firestore 写入量直接翻倍**（248 名学生已经接近免费额度）。
而对一个「设备丢了」的备份来说，**「你上一次学习结束时的状态」本来就是对的粒度**。

### ⚠️ 规则尚未发布 —— 每个入口都必须软失败
owner：「I'll publish rules block when ready」。§16 已经列着好几块规则没发布。
在这一块上线之前，`saveClaim`／`readClaim` 一律拿到 permission-denied，
**App 必须照常运作**，不能挡住一个只想玩的学生。
`readClaim` 因此把失败分成 `notfound` / `denied` / `error` 三种——
**「你打错了」和「规则还没发布」需要完全不同的话**。

### 进度状态：**全部落地**（2026-08-19b）
- ✅ `firestore.rules` 的 `claims/{code}` 区块（⚠️ **仍待 owner 发布**——发布前一切软失败）
- ✅ `firebase-init.js`：`makeClaimCode` / `saveClaim` / `readClaim` / `deleteClaim` / `saveProfileField`
- ✅ profile.js：铸码 · 组装 payload · **全新账号闸门** · 我的档案 上显示 + 复制 + 换一个
- ✅ nickname.js **与 cs.js**：昵称那一屏「换了设备？在这里找回进度」
- ✅ teacher.html：档案卡上显示学生的恢复码（只读）**并且也发 VS4**
  ⚠️ 教师端发的码若只发 VS3，就会**悄悄比学生自己生成的恢复得少**——
  这种不对称几周后才会以「老师给我的码把我的灵露弄丢了」的形式冒出来，
  那时已经分不清用的是哪一段码。实测：教师端出的 VS4 由学生端 profile.js 解开，
  灵露／贝壳／wins／船只所选全部落地（9 条断言）。
- ✅ VS4 长码：`wins` 带（owner 选 include）· `decoPos`／`berthPos` 不带（owner 选 omit）

### ⚠️ 一个输入框认两种码
学生手上拿的是哪一种码，他自己不见得分得清——**让他先分类就是要他懂一件他不懂的事**。
`恢复码` 是 8–24 个 `[0-9A-Z]` 且**不含点**；`进度码` 一定有点（VS1/2/3/4）。**按形状分，不问学生。**
⚠️ 打错的短码会落进恢复码那一支，于是错误讯息是「**找不到这个恢复码**」而不是
「进度码格式不正确」——**前者才是对他刚打的东西的真话**。

### ⚠️ 恢复成功之后 `location.reload()`
**不要**试着把恢复好的 store 交给正在跑的引擎：cs.js／xh.js 把 `store` 拿在内存里，
下一次 save 就会盖掉刚写进去的东西（§18r）；而恢复码换的是**整份 store**，
不是合并，根本没有一个 provider 钩子能表达它。恢复是一台设备一辈子一次的事，
重载是让每个引擎干净地重新读一遍的诚实做法。

### ⚠️ 新设备**不继承**旧的恢复码
`applyClaim` 会把 payload 里的 `claimCode` 删掉再写 profile。两台设备共用一份 claim 文件，
**每次 flush 都会互相覆盖 payload，后关的那台赢**。新设备自己铸一个。

### ⚠️ VS4：有经济数据才发 VS4，否则仍然发 VS3
没有任何经济数据的学生**照旧拿到 VS3 前缀**，码不会因为这次改动变长一个字节。
⚠️ **VS4 解码器必须收 VS3 码**（学生手上现在拿的就是 VS3）；VS3 码解出来就是没有 `eco` 区块。
⚠️ **eco 区块进校验和**：截断的 blob 会让**整段码被拒**，而不是恢复半个账号。

### 实测（真浏览器，两批共 45 条断言全过）
**VS4（25 条）**：全新设备恢复 → 灵露 340／贝壳 120／消耗品／装备／`wins`／海滩摆位／
头像与船只拥有 **全部回来**，`decoPos`／`berthPos` **确实没带**；
**赚→出码→花掉→再领 = 经济被跳过、灵露仍是 40、掌握照样合并**（重复漏洞已堵）；
没有经济数据的学生仍发 VS3；VS3 码照常解开。重度学生的 VS4 码 **465 字符**。
**恢复码（20 条）**：铸出的码 10 位、无 `0 O 1 I L`；**小写 + 中间带空格照样能恢复**；
昵称一起回来；新设备不继承旧码；非全新设备只合并单调字段；
打错说「找不到」、太短说「不完整」；换一个之后旧 claims 文件立刻删除。

⚠️ **长码不会被废除**：恢复码要联网，长码不用。§18ad 去尾零之后，
一个只碰过 G3 的轻度学生整段码只有 **37 个字符**，本来就已经能手打了。

---

## 18af. 我的词山 六处（owner 2026-08-22，第十八批）

owner 一次报了五件事加一句「the pinyin toggle also doesn't work here」。五件里有两件是
**同一个几何问题的两半**，所以这一节按病因分，不按报告顺序分。

### 🐛 ① 每一级的最后一个单元本来就画不出来
owner：「G1 doesn't display all the chapters eg sec 4 only has chapters 1-4 instead of 1-5」。
⚠️ **不是 中四 特有，四座山每一级都少一个**：单元 pin 画在**该单元最后一个词的海拔**上，
而 关卡 的海拔**就是同一个数**——30px 的旗子正好压在 19px 的圆点上。
藏起来的是 中一/中二/中三 单元六 与 中四单元五，**每座山四个**。
中四 之所以最显眼，是因为它只有 5 个单元（其余各 6 个），少一个就少两成。
- ⚠️ **修法是把 pin 推向路的旁边，绝不沿着路挪。** 沿路的坐标**就是海拔**，
  而营地正是靠它来读的（见 ②）——把 pin 往山上挪一点，画面就在说一句关于海拔的假话。
  单元圆点左右交替 ±9px，关卡 取 24px、落在它自己那个单元没占的一侧。
- ⚠️ **还有第二个碰撞，比第一个更普遍**：整串 pin 本来就比它们自己还密——
  23 个单元圆点摊在约 330px 的画好的山路上，中心间距约 12px，而圆点直径 19px。
  这一条在 owner 的报告里长得像 ③（营地挡住圆点），其实营地只是最大的那一个遮挡物。
- ⚠️ **`mtnNormalAt()` 要按屏幕像素算，不能按 sprite 分数算**：x 是宽度的分数、
  y 是高度的分数，不先乘长宽比就归一化，偏移会在高的 sprite 上被拉斜（hcl 是 1100×938）。

### 🐛 ①b 光靠公式修不完：`mtnDeconflict()` 是量出来的
⚠️ **手绘山路自己会折回来。** 上面那套偏移只解决**相邻**两个 pin 的碰撞——在 ≥900px 上
那就是全部（四座山实测 0 个点不到）。它解决不了**回头弯**：手机上（640×393 的舞台）
中一单元三 与 中一关卡 沿路相隔 30px，**在屏幕上只隔 12px**。
任何写在 `MTN_PATHS` 上的公式都不知道这件事，除非把版面重算一遍——所以这一遍**问浏览器**。
- ⚠️ **只把 pin 往外推，沿路坐标一格都不动**（理由同上）。有界：38px、3 轮。
- ⚠️ **两个方向都要试。** 回头弯上，一个 pin 的法线正对着弯道另一侧的邻居，
  「再往外一点」是**朝它走过去**。只试向外那一个符号，手机上会剩三个点埋着。
- ⚠️ **候选位置是「算」出来的，绝不是「量」出来的**：`.mtn2-pin` 带 `transition:transform .12s`，
  同一个 tick 里写 `--ox` 再读 `getBoundingClientRect` 拿回的是**旧位置**，
  于是搜索以为每个候选都一样、把十个候选全部否掉。**第一版就是这样静默失败的**，
  症状是「函数跑了、日志说有碰撞、一个 pin 都没动」。偏移是纯平移，算术本来就精确。
- ⚠️ **跑两次，第二次才是真的那次**：紧接 `innerHTML` 调用时舞台可能还量到 0×0
  （岛是按 vh/vw 加 `aspect-ratio` 定尺寸的），那一遍会「看不到任何碰撞」而什么都不做。
  这一遍是幂等的，所以下一帧再跑一次不花什么代价，而那一次手上才有真几何。
- ⚠️ **转屏是整块重画（`startMountain()`），不是只重跑这一遍**：只重跑它要去撤销一批
  **还在过渡中**的偏移，实测会留下一个埋着的点；而这块屏幕没有任何状态值得保。
- ⚠️ **窄屏改成横向平移，不是把地图压扁**（`.mtn2-scroll`，≤700px 给舞台 640px 下限）：
  390px 的手机上整条山路只有约 143px 却要装 27 个地标，间距约 5px——
  **怎么偏移都救不回来**。**能拖的地图是诚实的，有点不开的地标不是。**
  ⚠️ 滚动条挂在**包着舞台的那一层**，不是 `.mtn2-wrap`（会把 HUD／目标条／提示行一起拖走），
  更不是 `<body>`（§14）。打开时滚到中间：山路在每张 sprite 的中间三分之一，
  停在 scrollLeft 0 的手机学生只会看到一片海。
- 实测：四座山 × 1280×900／1024×768／390×844，**中心点点不到的 pin ＝ 0**；
  中四 五个单元全部在列。⚠️ **顶峰 要按环上而不是环心来验**：它是空心环，
  环心那个点交给环里的单元圆点是对的（那个圆点以前反而永远点不到）。

### ⚠️ ② 营地停在还没通过的 年度试炼 那里
owner：「I haven't passed the S1 final challenge so my campsite shouldn't be past that even
though my mastery percentage is beyond」。
- **只改了画出来的位置**。`buildMarks` 仍然把营地记在 alt 0，目标、海拔分区、`markDone`
  一个字节没动；HUD 上那个 `已掌握 N 米` 仍然是真实数字。**帐篷是「爬到哪」，不是「掌握多少」。**
- ⚠️ **这是 年度试炼 第一次真的挡住什么**：`badgeKeyL`（年级徽章）看的是「这一级的单元
  是否全部完成」，`store.gym[level]` 从前只解锁 神兽 头像。
- ⚠️ **闸门以上的地标照样点得开**（owner 裁定「单元仍可看」）：学生必须读得到他正在爬向的词。
  它们是**淡化，不是禁用**——那是关于**攀登**的陈述，不是关于地图的。
- ⚠️ **淡化用 `saturate()` 不用 `grayscale()`**：卡在 中一年度试炼 的学生完全可能已经拿到
  更上面的单元徽章，而那些 pin 是**金色**的；整个灰掉等于把他拿到的徽章画成丢掉的徽章。
- ⚠️ **被挡住时，目标条归闸门所有**：帐篷不动，却还在上面写「距 X 还差 N 词」，
  就是 App 在叫学生去做一件不会让它动的事。

### 🐛 ③ 营地那颗圆盘在吞点击
owner：「the campsite icon overlaps with a number of clickable circles so they are now
unclickable, this is also the same issue with the rest of the mountains」。
`.mtn2-pin.t-base` 是 **64px** 的按钮，外加一个 `::before{inset:-7px}` ——
**一颗 78px 的隐形点击盘**，压在一串间距约 12px 的圆点上：一次盖死五个地标。
- 缩到 **44px** · 帐篷字形与 `::before` 都改 `pointer-events:none` ·
  帐篷**让到路边**（法线方向 30px），`::before` 改成把 `--ox/--oy` 减回去的**锚点小圆**，
  「你在这里」仍然钉在山路上的准确海拔点。
- ⚠️ **`.t-unit / .t-level` 的 z-index 抬到营地之上**：帐篷是路上最大的东西，
  它落在哪里，底下的地标都必须照样画得出来、照样接得住自己那一下。
  ⚠️ **这条规则必须写在 `:hover{z-index:20}` 之前**：media query 与 hover 都不增加优先级，
  同优先级后者赢——写在后面，hover 的 pin 会掉回 9，标签被邻居压住。

### ④ 单元卡的词按 板块 分组
owner：「the vocab summary of each chapter should sort them into their respective
components e.g. 生活空间 核心 etc.」。
- 一整片 21 个词片**说不出**学生是在哪一块落后，而 板块 正是这个 App 其他地方已经在用的轴
  （①筛选 按它收窄、A层徽章一块一枚、板块驿站 卡用的是同一批徽章美术）。
- ⚠️ **所有分组共用一个滚动盒**（`.pop-groups`）：`.chip-wrap` 自带 38vh 的盒子，
  四个板块的单元会变成**四个各自独立的小滚动框**。

### ⑤ 知道了 → 返回 / 学习 / 挑战
owner：「the 知道了 button is odd too, I think it makes more sense if it's go back/learn/
challenge」。
- 知道了 是一句「我知道了」，而这张卡**不是通知**：它是全站唯一一处能看见「这个单元我还差
  哪几个词」的地方，所以合理的下一步是去练那个单元。
- ⚠️ **两颗按钮都不直接开玩法**：它们把 ①复习范围 设成那一个单元、打开对应的 ② 那一栏
  （学习→学习，挑战→闯关），玩法仍然由学生自己挑。**替他挑玩法正是 §4.4 那类静默失败的做法**
  ——这张卡不知道那个单元跑不跑得起哪些模式。
- ⚠️ **`store.compOff`（板块筛选）刻意不动**：那是学生在另一块屏幕上有意做的收窄。
  真被它筛成空的，① 自己那行 `共 0 词` 会说出来——这也正是它落在首页而不是落进某个玩法里的理由。
- ⚠️ 顺手把 `store.accLevel` 设成那个年级，否则改动看不见（那一级是收起的）。

### 🐛 ⑥ 这张卡上的拼音闸门从来没接过线
owner：「the pinyin toggle also doesn't work here」——**确实一次都没生效过**。
词片从上线第一天就带着 `data-py`（喂 `speak()` 用），**从来没有把它画出来**；
卡上的固定文字（营地／海拔／已掌握／按钮）也一个都没走过 `pyl()`／`enl()`。
- ⚠️ **词片只给拼音，不给英文**：§10 明写 `enl()` 不用在词汇资料上，而「这个词什么意思」
  是 我的词语表 的事。拼音直接取 `w.py`（手写资料，绝不在运行时生成，§8）。
- ⚠️ **`body.py-aid` 前缀是承重的**（§18e 同一个坑，这已经是第三次）：
  裸的 `.wchip .pylab` 是 (0,2,0)，赢得过 `display:none` 那一半、**输给**文件更后面的
  `body.py-aid .pylab` (0,2,0)——覆盖静默失效。
- ⚠️ **标签 + 数字要分开两个 span**（`popStat()`）：被注解的那个 span 里的中文必须**就是**
  PY_LAB 的 key（§10），而「海拔 264 米」当 key 意味着每一个海拔都要一条新条目。
- 实测：闸门开关只是 `<body>` 上翻一次 class，span **全部留在 DOM 里**（19 条拼音、
  10 条英文，关掉后 0 条可见）——没有重绘，所以也没有 §14 那个「重洗＝泄题」的风险。
  ⚠️ **HCL 整张卡一条注解都没有**，因为那里刻意两个辅助都不发（§10），这次没有给它加。

### 顺带：「凡事不愿＿＿的人」改成「完全不愿＿＿」
owner 2026-08-22 指着 G1 的填空题报的。同一句话**四个学段都有**（同一个词 吃苦：
`G1-0372` · `G2-0757` · `G3-0152` · `HCL-1362`），owner 裁定四个一起改——
只改 G1 会让同一个词在不同学段挂着两句不同的例句。
- ⚠️ **`clozePy` 跟着改**（`fán shì` → `wán quán`，音节数不变，仍是 15）。HCL 没有拼音字段，
  这是正常状态（schemaVersion 2 不覆盖 HCL）。
- ⚠️ **母表同步了**（§5「直接改 JSON 只允许作为同步对」）：四份
  `vocab-lists/*_词语总表.xlsx` 的 填空句 与 例句拼音 两列都改过，
  再跑 `generate_vocab_json.py --pinyin --verify` 逐词核对——**3741 个词条 0 差异**，
  手改的 JSON 与从母表重建的结果一致。
- `generate_search_index.py --write` 已跑：**索引一个字节没变**（它只收 词/拼音/英文/所属站，
  不收填空句），这是对的，不是漏跑。

---

## 18ag. 我的词山 只剩五个地标（owner 2026-08-22，同日第二批）

owner 看着 `20260822` 的成果说：「would it work better if only the 4 level final stages and
the ultimate challenge is preserved on the mountain images? still messy and missing chapters
persist. maybe it's not so important to have the small chapter circles? also for the campsite
hovering circle, maybe we can offset it something like a 'you are here' pin so that it
doesn't block the clickable stages?」（并附了一张 map pin 参考图。）

### ⚠️ 先核实：「单元不见了」这一次**不是 bug**
实测（HCL，她截图里的 已掌握 20 米）：**29 个 pin 全部在 DOM 里、全部点得开、0 个被埋**。
剩下的是**视觉上仍有 4 对圆点互相接触**——所以数不出来。
⚠️ **这条要记住**：`20260822` 那一批把「点不到」修干净了，却修不掉「读不出来」。
**能证明它可点，不等于它可读。** owner 报的仍然是同一句话，而她是对的。

### 五个地标
| 山上还有 | 搬到哪里去了 |
|---|---|
| ⛺ 你在这里（地图针） | — |
| 🚩 关卡 ×4 | **单元列表现在在它里面** |
| 🏯 顶峰 | — |
| ~~单元圆点 ×23~~ | 关卡 卡里的一行一行 |

- ⚠️ **单元卡一个字都没少**：`unitRowsHtml()` 从 `buildMarks()` 重建，交给 `openMark()`
  的**就是原来那个 mark 对象**——一条代码路径，一张卡。§18af 的 板块 分组、词片拼音、
  返回／学习／挑战 全部原样还在。
- ⚠️ **一行能说三件事，一个 19px 的圆点一件都说不了**：单元名 · 主题（新同学 新鲜事）·
  已掌握 N/M。圆点要一个一个 hover 才知道自己是谁。
- ⚠️ **三级返回**：海图 ← 词山 ← 关卡 ← 单元。`openMark(m, back)` 的 `back` 是**一个 mark，
  不是布尔值**；单元卡的返回键写着「‹ 中一 关卡」。把学生丢回地图，
  他就得重新找那面旗、重新点开，只为看下一个单元。
- ⚠️ **`markLabel()` 的单元现在带年级**（`中一 · 单元一`）：它剩下的调用点只有目标条和 pin 的
  title，而每个学段都有四个「单元一」——「距「单元一 营地」还差 55 词」指的是一个
  **学生定位不到**的里程碑。
- ⚠️ **关卡 放大到 38px**（原 30px）：它们现在是地图上**唯一**的导航，一面旗就是一整年单元的门，
  30px 是当年要挤进一条链子时挑的尺寸。顺带才第一次成为一个像样的触控目标。
- ⚠️ **闸门以上的淡化跟着放轻**（.62 → .78 不透明、`saturate(.5)` → `.72`）：
  29 个 pin 时淡掉一个只是人群里的一个；只剩五个时，三面淡掉的旗子是**三整年单元的唯一入口**。
  **一个地标只要是它内容的唯一通路，就永远不许难找。**

### ⚠️ 你在这里 地图针
- 气泡 `translate(-50%, -100% - 13px)`，`::after` 是一条 16px 的尾巴指回**准确的海拔点**。
  ⚠️ **两个数字是配套的**（`top:100%` 是 padding box，在 3px 边框以内），改一个要改另一个。
  实测尾尖与 pin 自己的 left/top 百分比点误差 **dx 0 / dy 0**。
- ⚠️ **`campOff` 是 0 而且必须保持 0，营地也不再带 `data-nx`**：`mtnDeconflict` 因此
  只把它当**障碍物**、永远不当**可移动的**。尾巴的全部意义就是「就是这个点」——
  把针挪到旁边，尾巴就指着空地。
- ⚠️ **`.mtn2-pin.t-base:hover` 要写成 (0,3,0)**，否则通用的 `.mtn2-pin:hover` (0,2,0)
  会在 hover 那一刻把它拽回「以路面为中心」的 transform，针会跳一下。
- ⚠️ **营地的标签挂在下面**（`.mtn2-name` 反转），不然会撞上自己的气泡。
- ⚠️ 被闸门卡住时，针正好指着那面 🔒 旗——**这是对的**，不是巧合：
  「你在这里，挡住你的就是它。」

### 仍然保留的两条
- **`mtnDeconflict()` 留着**（§18af）。五个地标几乎不会撞，但营地被闸门钉在关卡上时
  仍然可能，而它现在几乎零成本。**不要因为「现在不撞了」把它删掉**——它是那条
  「量出来，不要算出来」的保险。
- **窄屏 640px 横向平移留着**：g1 是最宽的 sprite，390px 手机上整条山路只有约 120px，
  四面 38px 的旗子照样叠在一起。

### 实测
四座山 × 1280×900 / 390×844：pin 数 **6**、点不到的 **0**、body 不横向滚动；
关卡 → 单元 → 返回关卡 的下钻在 g2/hcl 上逐个走通；
HCL 整张卡仍然一条注解都没有（§10）。

---

## 18ah. 术语改名 · 后端档名对齐（owner 2026-08-22，同日第三批）

依据 `~/Downloads/HANDOFF_术语改名_20260822.md`（owner 已批，§4 当场签核）。
起因：低起点学生读不出 攀山竞速 —— 攀·竞·速 三个低频字，最难的两个还落在谓语上。

### ⚠️ §0 命名规则（这一条以后要拿来挡新名字）
**一词一难 —— 一个标签只放一个生词，而且那个生词必须是名词。**
其余的字要留得住意思，让标签在生词还没解码时仍然猜得出来。推论四条：
1. **能直接读出动作的动宾结构，胜过文雅的复合词**：看图识词 零起点就能用，词海垂钓 不能。
2. **自造词问不到人**：学生可以回家问 攀 是什么意思，没有人帮得了他 垂钓 当游戏名，或者 汉兜。
3. **标签里避开多音字**：学生默读错了却毫无提示，比读不出来更糟。
4. **机制一样的玩法要取一样形状的名字**，迁移才免费。

### 改了什么
| 旧 | 新 | 为什么 |
|---|---|---|
| 攀山竞速 | **攀山快答** | 三个难字；快答 还说出了 竞速 藏起来的事实——这是唯一限时的模式 |
| 踏浪竞速 | **沙滩快跑** | 同一个 竞速 问题，而且落在**最低起点**那一层 |
| 词海垂钓 | **词海钓鱼** | 垂钓 文雅；钓鱼 每个小孩都会 |
| 组词成句 | **组词成句**（原 重整句子） | 重 是 chóng/zhòng 多音陷阱；现在与 组字成词 成一套：字→词、词→句 |
| 启航码头 | **出发码头** | 启·航 两个难字，偏偏印在这一层的门面上；出发 正是每局要按的那颗键 |
| 并肩航海 | **结伴出海** | 并肩 难，而山上同一个功能叫 结伴登峰；结伴X 现在两层对齐 |
| 学行 | **学习** | 原名语义不透明；码头本来就管这块货架叫 学习，学生从 XH 上到 G1 白学一个词 |
| 学海启航 | **学海起步** | 跟着 #5 走。⚠️ **不是 学海出发**：那会让 profile.js 渲染成「出发码头 · 学海出发」 |
| 组词挑战（山） | **组字成词** | owner 2026-08-22 当场签核。两边本来就是同一个玩法、同一句英文 Build the Word；改完 字→词 全平台一个名字，组词 也腾出来只表示「拿词当材料」 |

⚠️ **刻意没改**（记下来免得重开）：`历练值`（难，但永远挨着数字出现，重复本身在教它）·
`凌霄客`／`常胜擂主`／`凯旋号手`／`牵星板徽`（称号，罕见，难懂就是奖励）·`词语汉兜`。

### ⚠️ 这一批为什么是安全的
**只动中文字面量。** 模式 id（`sort`/`type`/`build`/`match`/`pic`/`listen`/`phrase`/
`enmcq`/`learn`/`surf`/`quiz`/`cards`）· 营地 key（`sprint`/`rain`/`assemble`/`handle`）·
榜单 key `sprint90` · store 字段 · stream key `xh` · Firestore 路径 · 美术档名
**一个字节都没动**——它们索引的是**学生记得的状态**，改一个就等于把每个回访学生的
记忆模式指向空值。

### ⚠️ §5.1 那个陷阱：注解表用中文当 key
`PY_LAB`／`EN_LAB`／`XH_PY` 全部**以屏幕上那串中文为 key**。改了渲染串没改 key，
**ruby 会静默消失**——不报错、不告警，而且对不需要拼音的大人来说看起来完全正常。
**丢掉拼音的正是这次改名要救的那批学生。**
⚠️ **做法**：整个仓库按中文字面量全局替换，key 与渲染串因此**天然同步**，
再回头修表里的**值**（拼音／英文），那是全局替换看不见的部分。

### 🐛 交接文没列到的一处：拼音**值**里也写着旧词
`用你在「学习」页选的复习范围…` 这条整句注解的 key 跟着改了，**值里仍然拼着
`「xiū xíng」`**。开着拼音的学生会被喂一个屏幕上已经不存在的词去拼读。
⚠️ **规则要扩一条：整句注解的 VALUE 里也可能嵌着那个词，不只 key。**

### 🐛 顺手修掉两个既有缺陷（已对 HEAD 核实，都不是这次改名造成的）
- **`"海拔"` 在 app.js 两张表里各声明了两次。** 同一个对象字面量里两个同名 key
  **后者静默覆盖前者**，早的那一行是死代码。今天两边的值恰好一样所以看不出来，
  但下一个改错那一份的人不会得到任何反馈。（这是 2026-08-22 我自己加 单元卡 时留下的。）
- **`xhPy("船只 · 想开哪一艘都可以")` 没有表行**，所以 我的词语表 上那个小标题
  从上线起就没有过拼音。⚠️ `船只` 的 只 是 **zhī**（船的量词），不是 zhǐ。

### 🐛 交接文 §5.5 说错了一件事
它写「`teacher.html` 与 `XH_index.html` 是独立档、不带 cache-bust 查询串」。
**两个都带**：`XH_index.html` 有 **9** 处 `?v=`，`teacher.html` 有 `ASSET_V`。
照它做就会漏掉七处仪式里的两处，而那正是「新 CSS 配旧 JS」的成因（§3）。

### 后端档名：海＝xh，山＝cs
owner：「all the sea focused things called xh and mountain focused things called cs -
right now the backend naming is a mix of two languages」。清点下来**混用的只有两个档**：
- `js/app.js` → **`js/cs.js`**、`css/app.css` → **`css/cs.css`**（词山）
- `js/xh.js` / `css/xh.css` 本来就对（学海）
- ⚠️ **共享档案不站队，也不要改**：`profile.js`（身份，§4 第 1 条）· `nickname.js` ·
  `search.js`（五站只读）· `tts.js` · `firebase-init.js` · `podium.js` ·
  **`arena.js`（2026-08-18 起两家共用，§18w）**。给它们挑一边就是在说一句假话。
- 真正要改的 `<script>`／`<link>` **只有 9 个**，其余 290 处是注释与文档里的提法。
- ⚠️ **`?v=` 机制不受影响**：三个档案读 `document.currentScript.src` 但**只取查询串**，
  不取目录（§2）。
- ⚠️ **改名当天要挑没有课的时段部署**：GitHub Pages 给 HTML 也是 `max-age=600`，
  手上握着旧 HTML 的学生会去要一个已经不存在的 `js/app.js`（404 = 白屏），
  最长十分钟。这次是周末部署，owner 裁定直接换、不留过渡副本。

### ⚠️ 不要碰的东西（owner 2026-08-22 明确：有危险就不做）
这三样也可以叫「后端命名」，但它们**不是名字，是索引**：
- **`ws2_{stream}` / `ws_xh` localStorage key**——改名而迁移不完美＝学生进度成孤儿；
  更糟的是拿着旧 HTML 的那台设备**还在往旧 key 写**，两边静默分叉。
- **Firestore 集合**（`users`／`scores`／`dockScores`／`rooms`／`claims`）——要同时
  重发规则**并**迁移既有文件，而后者浏览器端根本做不到；已发出去的恢复码会全部解析不了。
- **进度码里的段标签**（VS3/VS4 的 `g1:`／`xh:`）——那是**线格式**。
  学生邮箱里躺着的每一段长码会读不出来，而 `teacher.html` 是同一格式的第二个写入者。
⚠️ 这三样**换来的只是我和 owner 才看得见的整洁**，代价是学生的进度。**不要动。**

---

## 18ai. 顶栏没有面板了 · 地标名字不再画两次（owner 2026-08-22，同日第四批）

owner：「for all the liquid glass bars on top, maybe we can do away with them to increase
the feeling of openness and game? like the seamap doesn't have a top bar. we just have to
ensure that the labels stay visible (G1/2/3/hcl etc)」。

### 拿掉的是**面板**，不是内容
海图从来就没有顶栏，但它**有控件**：一颗 `返回首页` 药丸、两颗辅助药丸，以及**挂在每座岛上的
名牌**。照这个做：`.topbar` / `.xh-top` 现在只是一排布局，背景、边框、投影、`backdrop-filter`
全部去掉，每个子元素自己带玻璃。
- ⚠️ **学段名必须活下来**（owner 唯一的条件），所以它拿到**自己的名牌**（`.tb-id` / `.xh-id`）。
  **不是用带阴影的裸字**：这行字直接压在 `applyAmbience()` 的照片上，而 §7 明写
  亮色画面上的答案是**深墨压玻璃**，不是浅色字压画。名牌只包住文字，所以读起来是一枚浮标，不是一条栏。
- ⚠️ **海图能把名字挂在岛上，一局测验不能**：顶栏在**每一屏**都在，而内屏没有任何美术可以承载身份。
  这就是名牌不能省的原因。
- ⚠️ **整排 `pointer-events:none`，控件各自 `auto`**：它是 sticky 且 z-50，
  一条透明的整宽条**照样吃掉**落在控件之间空隙上的每一次点击——包括点在它底下滚过的卡片上。
- ⚠️ **两颗辅助药丸的 alpha 从 .55 抬到 .72**：它们本来背后有顶栏的玻璃，现在直接坐在照片上。
- ⚠️ **`@supports not (backdrop-filter)` 里那两行 `.topbar` / `.xh-top` 底色必须删掉**，
  改成给名牌。留着的话，没有模糊的旧 iPad 会把那块板**原样画回来**——而那正是这个回退块伺候的机型。

### 🐛 滚动时会撞：所以有一层**只在滚动时**出现的柔光
面板一去掉，sticky 的性质就暴露了：卡片会从控件**底下**滑上来，实测 1024×700 时
复习范围 那张卡直接穿过名牌。
- `.topbar::before` 是一层**底边淡到全透明**的渐变，`.scrolled` 时才 opacity 1。
  ⚠️ **整条渐变里不许有任何硬边**——加一条 `border-bottom` 或 `box-shadow` 就等于把栏装回去。
- ⚠️ **静止时（scrollY 0）chrome 是零**。那是第一眼，也是 owner 当时在看的那一屏。
- 一个 `passive` 的 scroll 监听翻一次 class，**整页寿命只挂一次**
  （`setTopbar` 每屏都跑，按次挂会一次导航攒一个）。挂上时先跑一次 `apply()`，
  这样从 bfcache 还原到半截位置也是对的。
- ⚠️ **没有用 IntersectionObserver 哨兵**（那是这类需求的标准写法）：iOS 12.2 才支持，
  而 G1/G2 的机队正是更旧的 iPad。
- ⚠️ **`window.scrollTo()` 在自动化环境里不派发 scroll 事件**，自己写的监听同样收不到——
  查这个 class 没生效时别怀疑代码，先 `dispatchEvent(new Event("scroll"))` 复核。

### 🐛 地标 hover 时名字画两次
owner：「somehow the name appears twice at times when hovered - I can't really replicate
that though」。**每次都会重现，但要停留约一秒**：pin 同时带着原生 `title=""` 和
`.mtn2-name` 名牌。名牌 `:hover` 立刻出现，浏览器自己的 tooltip 延迟出现、而且落在另一个位置——
「有时候」正是因为它取决于鼠标停了多久。
- `title` 是 2026-08-15 特意加的（**「for slow-hover and screen readers」**），
  然后 2026-08-15 又在它上面加了名牌，**没有把它退役**。
- 改成 `aria-label`：**读屏那一半留着，重复的那张图没了。**
- ⚠️ **规矩**：一个元素只要自带可见的 hover 标签，就**不许**再挂 `title`。

### 年级峰 → 关卡（同批，owner 当场定名）
owner：「年级峰 is a misnomer as there aren't sub mountains on each mountain, it should be
more like a game stage」。**这座山只有一个峰，就是 顶峰**；那四个标记原来的名字在描述
**画面里根本没有的地形**——而 §18ag 刚把它们变成地图上仅有的四个导航点，名字错就更显眼。
- ⚠️ **选 `关卡` 不是因为它最短，是因为它最省**：两个字都常用，是名词（符合 §18ah §0
  的一词一难），而且 ② 那一栏已经叫 **闯关**——`关` = 一关 这个词学生会**碰到两次**，
  第二次是免费的。落选的：`大关`（略文）· `站`（没有「要过关」的意思，闸门感弱）·
  `里程碑`（🎯目标 里的「第 N 米里程碑」已经占用，同名会混）。
- ⚠️ **`顶峰` 不动**：那是这座山真正的山顶，名字是准确的。
- ⚠️ 注解表两行的**值**跟着改：`"关卡": "guān kǎ"` / `"Stage"`。
  只改 key 不改值，正是 §18ah 记的那个坑。

### 没有动的两块
- **`teacher.html`**：教职员的资料工具，它要的是密度不是开阔。
- **`guide.html`**：它是一份文档，而且 §18o 把顶栏高度实测进 `--gd-topbar-h` 驱动粘住的目录条；
  拆栏要连那套一起重做，换不到任何「游戏感」。

---

## 18aj. 交通 10 → 18 · 码头精灵的宽度上限（HANDOFF_交通扩充_20260822，落地 2026-08-22）

一张 2×4 的 ChatGPT 精灵表进来，八个词出去：**水上** 船／渔船／帆船，**陆路** 摩托车／罗厘／
救护车／消防车／警车。管线是 `local-admin/process_transport_art.py`，日志同名 `.log`。

### 交接文里有三条是错的，别照着抄
- ⚠️ **「底色是 `#FF00FF`」——不是**。实测众数是 **(246, 5, 249)**。§6 早就写过
  「没有一张的洋红是 `#FF00FF`」，这张也一样。**逐张实测，永不假定**，脚本自己量。
- ⚠️ **「data + assets only，不动 JS」——不成立**。新的 `子类` 必须同时进
  `XH_PY` 与 `XH_GROUP_EN`，否则 `checkGroupLabels()` 报警、章节小标题裸奔（§第 899 行的合同）。
- ⚠️ **「XH_index.html 不需要 `?v=` 」——不成立**。它每一个 `<script>`／`<link>` 都带 `?v=`，
  而且 `ASSET_V` 是**从 xh.js 自己的 `?v=` 反查出来的**，`data/xh_v3.json` 的 fetch 也吃这个值。
  改了 xh.js 或词表就必须推**七处**（§3）。这一批推到 `20260822g`。
  ⚠️ §3 那行「当前」当时停在 `20260822e`，而文件已经在 `f` ——**发版后顺手改那一行**。

### 抠图：`spill > 55` 这条老规矩在这张表上会打洞
`process_run_art.py` 的 `burnt = spill > 55` 建立在一句前提上——「这批美术的暖色都是 R>G>B，
所以 `min(R,B)` 是蓝、signature 为负」。**这张表推翻了它**：摩托车油箱与消防车车身是
(204,3,91) / (235,3,101) 这种 G 近乎 0 的高饱和红，`min(R,B)` 取到的是**蓝**，signature 读到 +88。
裸规则会把 **166 个车身内部像素**当底色切掉，也就是往红漆上打洞。
- 加两个条件才安全，两个都实测过：**`|R-B| < 60`**（底色 R≈B≈247，混色必然左右平衡；
  真红差 113）＋ **距底色 ≤2px**（毛边按定义贴着底色，内部像素因此永远碰不到）。
- ⚠️ **渔船的网查过了**。§6 记着晾网架「magenta 的不只是网眼，是网绳本身」，这张表正好也有一张网，
  所以是同一个陷阱的复刻。实测这张网是青绿的，**那一格内部高 spill 为 0**，不需要预先改色。
  **要检查的是这件事本身，不是这次的结论——下一张表照查。**
- ⚠️ **缩放前必须先把美术颜色向外泼一圈**（`colour_extend`）。透明像素照样带着 RGB，
  Lanczos 会把 (246,5,249) 重新混进每一条边，alpha 挡不住——不做就是八圈洋红光晕。

### ⚠️ 交付 320，不是交接文写的 256
`.xh-sprite img` 是**按高**排版的（`clamp(130px,24vh,210px)`），这批全是又宽又扁的裁切；
钉死宽 256 会让高只剩 85–172px，**卡片反过来放大它们**，渔船要放到 2.5 倍——
先赔掉分辨率再在 `image-rendering:pixelated` 下放大回去，等于踩两次 §14。
320 不是新数：现存三台**宽**的交通工具就是这个数（`xh_bicycle` 320x180 ·
`xh_doubledecker` 320x232 · `xh_aeroplane` 320x210），256 是留给高瘦件的（`xh_mrt` 192x256）。
实测 59–95 KB，落在现存区间内（bus 52 · bicycle 73 · doubledecker 98）；原尺寸要 131–180 KB，
比 `art/xh/` 里任何一张都大，所以没有原样交付。

### 🐛 顺带修掉一个一直都在的溢出：精灵没有宽度上限
高度 clamp 不设宽度天花板，于是**宽图有多宽画多宽，直接走出屏幕**。375px 实测：
渔船 **588px**、船头船尾各切掉一截、木牌框也断了。
- ⚠️ **这不是新美术带来的**：`xh_bus` / `xh_car` / `xh_airport` / `xh_bicycle` 早就把页面
  横向撑到 **510px**，新船只是把它顶到 730px、顶到没法视而不见。
- 修法两行：`.xh-sprite img` 与 `.xh-pic img` 加 `max-width:100%;object-fit:contain`，
  `.xh-pic` 加 `min-width:0`。⚠️ **`object-fit` 不能省**——`max-width` 一旦生效，
  那条写死的 height 会把图**压扁**而不是缩小。⚠️ `min-width:0` 是因为格子是 grid 子项，
  默认 `min-width:auto` **不肯缩**（§14「grid/flex 子项不缩」），宽图会把整列撑开。
- 实测修后：十八张交通精灵全部 fits，页面 `scrollWidth == clientWidth == 375`，选项格 149px。
- ⚠️ 同一条订正 `.xh-sic img` 早就做过（它那句「max-width/max-height，**不是** height:62px」）。

### ⚠️ 十八个词**整组**都有 子类 了（owner 裁定）
交接文只想给新的八个打标签、老的十个留平 `交通`，理由是「反正没有 UI 显示 子类」——
**这句是错的**：`logPages()` 正是按 `子类` 给 我的词语表 分节，且 `secs.length > 1` 就出小标题。
半套标签会在**章节 交通 里出现一个叫 交通 的小节**。owner 选了整组重标：
**陆路交通 11 · 水上交通 3 · 交通设施 3（车站／机场／路）· 空中交通 1（飞机）**。
- ⚠️ **空中交通 只有一个词是故意的**：`startRound` 的 `big` 过滤器**本来就不会**拿一个
  凑不满一局的 子类 当主题（那段注释讲的是 饮料 只有 4 个词），所以如实命名不花任何代价。
- ⚠️ **干扰项照旧只看 `组别`，不看 `子类`**（PATCH_category_hierarchy）。
  抽了 13,500 局验过：40,500 个干扰项全部同 `组别`，其中 2,600 个跨 `子类`——**跨子类是对的**，
  收窄到 子类 反而会把「猫 vs 鲨鱼」这种真题变成分类题。
- ⚠️ 新词一律 **append 到数组末尾**，老的 148 行一个字节没动（landmine：词序是载荷）。
- ⚠️ **拼音在这张表里是逐音节空格分开的**（`shuāng céng bā shì`）。交接文写的是连写
  （`yúchuán`），148 行现存数据没有一行是连写。

### 顺带对齐的数字
词数 **149 → 156**，但 ⚠️ **原来那个 149 本来就多了一个**：文件里一直是 148，
`guide.html` 的**中文**简介（「148 个词」）一直是对的，是**英文**那句和 `js/xh.js` 的两处注释在错。
`guide.html` 的 `STATS` 同时改了 查词索引 2,142 → **2,150**（`generate_search_index.py --write`
重跑，改前先 `--verify` 确认基线一致）。

## 18ak. ② 是两扇门 · 名牌 · 图标按视口缩放（owner 2026-08-23，第十九批）

owner：「the games button, they expect to click and be directed to choose game mode, but
the game mode is the section below and they miss it … they should be presented with 2 doors
on a new clean page with the current background persisting, and the two doors are
flashcards and quiz」。**两边一起改**（owner 当场裁定：码头 ②③ 是同一个形状）。

### 病因：一个看起来会翻页、其实只重画折线以下的控件
② 是一对标签页：按 闯关，**只把下面那一段换掉**，而那一段在 1024×768 的学校 Chromebook 上
就在折线以下、在手机上更远。学生按下去、画面看起来没变，于是以为自己点错了。
⚠️ **这不是「学生不会用」，是控件在说谎**：金色的选中态 + 立刻重画 = 一个 tab；
而 tab 的合同是「我换的东西就在旁边」。它换的东西不在旁边。

### 现在
| | 以前 | 现在 |
|---|---|---|
| ② | 两个 tab，有选中态 | 两扇门，有 `›`，**没有选中态** |
| ③ | 首页折线以下的一排卡 | **门后面一整页**，只有那一边的活动卡 |
| 设置 | 卡片再往里一层 | **一格没动，还在那里** |

- ⚠️ **门后面永远只能是活动卡。** 码头 2026-08-16 把 ② 做成导航只活了一天就被推翻（§18m），
  原因**不是**「② 不该导航」，而是那个页面同时背「做什么」与「多难」：两下点击之后，
  零起点学生第一眼看到的是一排滑杆。这一版把那一步留在原处。
  **不要把 每次题数／难度／题型 搬到门后那一页。**
- ⚠️ **范围跟着进来**（owner 选的「一行摘要 + 改范围 ›」）：①复习范围 留在首页，
  但门后那一页顶上写着 `已选 23 个单元 · 共 426 词`（**与首页那一行逐字相同**，
  两处说法不一致比不说更糟），旁边一颗 改范围 › 回上一页。
  一扇门灰掉的理由（这组没有图片／没有多字词／没有句子）**全都是范围造成的**，
  而范围在上一页——这就是它必须跟着进来的原因。
  ⚠️ 码头那颗 改范围 会先把 `store.scopeOpen` 设为 1 再回首页：手机上范围盒默认收起，
  否则学生落在首页却找不到刚才要改的东西。
- ⚠️ **门后那一页不编号**（§7）：它只有一个决定。首页仍然是 ①②。
- ⚠️ **三级返回，标签说出去处**：海图 ← 首页 ← 门后的活动页 ← 设定页 ← 一局。
  山上在门里说 `‹ 回到「学习」`／`回到「闯关」`，门外仍是 `回营地`／`回到营地`。
  ⚠️ 名字加「」不是装饰：学习／闯关 单看是动词，「回到学习」会被读成一句话。
- ⚠️ **`_path` 记的是「人在哪扇门里」，不是「上次选了哪扇」**（后者是 `store.homeTab`／
  `store.tab`，仍然照旧存）。`renderHome()`／`renderMenu()` 把它清零，
  所以**从右栏进去的每一块屏幕**（成就墙 · 风云榜 · 我的词语表 · 营地 · 我的词山 ·
  走进社区 · 航海徽章 · 我的海滩）不必自己记得清，照旧回首页。
- ⚠️ 山上 `setTopbar` 现在**也接受一个函数**当返回去处。老调用点一个都没改：
  传字符串的落到 `backToHub()`，它在门外回首页、在门里回那扇门。
  **只有 `renderPath` 自己传函数**——它传字符串就会把自己重画一遍，一颗按不动的返回键。
- ⚠️ 我的词山 单元卡上的 学习／挑战 现在**落在那扇门的活动页上**（§18af 时落在首页）：
  这两颗按钮的名字就是那两扇门的名字。仍然**不替学生挑玩法**。
- 实测（离屏 WebKit，§15）：四座山与码头各走一遍
  首页 → 门 → 设定页 → 返回 → 门 → 返回 → 首页；成就墙／徽章墙进出仍回首页；
  1440×900 / 1280×900 / 1024×768 / 390×844 四个尺寸截图看过，`body` 不横向滚动。

### 🐛 顶栏名牌在手机上是一根竖条 —— **既有缺陷，不是这次改出来的**
owner 看着我贴的手机截图报的。**在 `main` 上复现过**：390px 下 `.tb-id` 被压到
**32px 宽、370px 高**，五个字一个字一行竖着排下来，顶栏 **394px**——比内容还高。
成因是 §14 那条 flex 老陷阱：顶栏是一行 flex，右边四颗控件 `flex:none`，
名牌是**唯一可缩的那一个**，而它的内容可以换行，于是它缩到一个字宽。
- 修法两层：**① 名字与注解一律 `nowrap` + 省略号**（兜底，且防止下一个被加进顶栏的控件
  再抢一次宽度时又变成竖条）；**② 手机上让位**——副标先让，然后是 查词 的词标签
  与几处内距。**一颗按钮都没有被藏起来。**
- ⚠️ **试过「让注解换行」，那是错的**：一旦可以换行，名牌的 min-content 宽度就掉到一个字，
  flex 立刻把它压成 22px 宽 118px 高——同一根竖条，只是这次由我自己造出来。
- ⚠️ 码头腾地方的办法是**返回键在手机上交出它的两行注解**（`← 海图` 四个字加箭头已经说得完）。
  那条覆盖**必须写在闸门规则之后**：优先级相同，后者赢（§14）。
  我第一次写进文件前段那一档 media query，量出来一点变化都没有。
- 顶栏高度：山上 394 → **58px**，码头 130 → **73px**。

### 名牌上的字（owner 同批）
- **码头**：`词山学海 Vocab Summit · 学海起步` 那一整条 `.lv` 删掉（连它的拼音与英文）。
  owner：「this part is too cluttered, we can remove everything under the first
  'the beginner's pier'」——那块名牌上 **The Beginner's Pier 出现了两次**。
  现在是 出发码头 / chū fā mǎ tóu / The Beginner's Pier 三行。
- **四座山**：副标不再印「词山学海 Vocab Summit ·」，**只留 CPDD 学段标签**
  （owner：「keep the G1/2/3/HCL label」）。平台名在每一块屏幕的同一个角落重复一次，
  而那是学生唯一不需要被提醒的事——他已经在里面了。
- **四座山的名字第一次有了英文**（owner 定名，星 → 将 → 王 → 圣 这条阶梯在英文里也走完）：
  | 中文 | 拼音 | 英文 |
  |---|---|---|
  | 词星大冒险 | cí xīng dà mào xiǎn | **Adventure of the Vocab Star** |
  | 词将竞技场 | cí jiàng jìng jì chǎng | **Arena of the Vocab Champ** |
  | 词王淬炼坊 | cí wáng cuì liàn fáng | **Forge of the Vocab King** |
  | 词圣鸿文苑 | cí shèng hóng wén yuàn | **Court of the Vocab Sage** |
  ⚠️ 拼音**与 `index.html` 落地卡上那四串逐字相同**（那是已经审过的）：改一处要改两处。
  ⚠️ **HCL 的名牌上看不到这两行**，因为高级华文刻意不发拼音与英文（§10）。
  这是正确行为，不是漏了；要给它加，得先推翻 §10。
  ⚠️ 手机上（≤620px）名牌只剩**中文名 + 学段标签**：学段标签是 owner 明说要留的那一条，
  而拼音与英文在一块**身份**牌上是装饰——它们该在的地方是按钮与题目，那里一个都没关。

### 图标按视口缩放（owner 同批）
owner：「the emojis can be much bigger to illustrate its function and also have the panes
respond dynamically to screen size」。门与活动卡的图标、内距、字号一律改成 `clamp()`：
写死 46px 的图标在 1440 的笔电上和在 390 的手机上一样大，于是笔电上是一整片空地配一颗小图标。
- ⚠️ **下限保护手机，上限保护它不会在投影仪上变成一张海报。**
- ⚠️ **宽高必须同一个 `clamp()`**：只写宽度，`place-items` 会把图标格子拉成椭圆。
- ⚠️ 门后那一页在**又宽又高**的视口上（≥900×700）**垂直居中**；手机与矮横屏不开，
  那里 `min-height` 会把内容推到折线以下。
- ⚠️ 居中之后 `.section-label` 那颗药丸会被拉成一整条横杠（column flex 的默认
  `align-items:stretch`，`inline-block` 挡不住），要 `align-self:flex-start`——
  guide.html 的 `.num` 药丸踩过同一条。
- ⚠️ 码头**只在 `.xh-path` 里放大**：同一个 `.xh-mode` 在设定页上是一排七个小题型卡，
  跟着一起长就会挤成一堵墙。

### 两边的门长得一样 · 一律两列（owner 2026-08-23）
owner：「the pier's games/learn doors should look the same as mountains. Now they are
aesthetically different. Note make them all a grid of 2 columns」。
- 码头的门原来是**居中、图标与名字同一行、注解在下面**；山上的是**图标格子在左、
  名字与注解左对齐、箭头在右**。现在两边**逐个节点相同**（`.xh-hd-*` ↔ `.hd-*`），
  ⚠️ 类名各自一套是因为两张样式表本来就刻意重复（§17）——**改一处要改两处**。
- ⚠️ **`.xh-tab.on` 整条删除**：门没有选中态。它此前只剩这一对元素在用。
- ⚠️ **码头的门用固定小尺寸，不跟视口放大**：它坐在一条 `clamp(320px,30%,400px)` 的
  左栏里，两列之后每扇只有约 157px。照抄山上那组 clamp，文字栏只剩 **27px**，
  「学习」会被省略号切成「学…」（实测）。**结构一样，尺寸更紧**，这是刻意的。
- ⚠️ **名字 nowrap，注解可以换行**。两件事缺一不可：名字换行就是 学／习 竖着排；
  而注解 nowrap 会在 135px 的栏里把 `chuǎng guān` 切掉一半。名字那一行是 nowrap，
  它就是文字栏的 min-content 下限，所以放开注解**不会**让整扇门塌成一根竖条。
- **活动卡一律两列**（`repeat(2,minmax(0,1fr))`，≤560px 单列）。
  ⚠️ auto-fit 会按宽度装：闯关 在 1440 上排成三列一行，而 学习 只有两张卡——
  同一条动线的同一层，两个版面。⚠️ 三个玩法的学段（G1/G2）因此是 **2 + 1**，
  最后一张落在左边：这是 owner 选的取舍，不是溢出。

### 码头控件的白从 60% 提到 92%（owner 同批）
owner：「the opacity of liquid glass on the pier can be higher to match the mountains」。
**面板本来就一样**（`.xh-board` 与 cs.css 的 `.card` 逐字相同：74→56→60% 的渐变）；
差的是**控件**：码头的 `.xh-mode`／`.xh-tab` 一直是 `--glass` **60%** 平涂，
而山上同一类东西（`.camp`／`.unit`／新的 `.hdoor`）是 `--card` **92%**。
- xh.css 因此第一次有了 `--card` 与 `--card-line`（与 cs.css 同值，§17 的刻意重复）。
- ⚠️ **面板一个字都没动**：§18a 明写场景底图上的面板**必须**看得见后面的摊位。
  抬的是控件，不是面板。
- ⚠️ 山上新的 `.hdoor` 第一版是一层 74% 的玻璃渐变，**已改成与 `.camp` 同一种材质**：
  同一条动线上两级长得不一样，比两级都朴素更糟。

### 顺带
- **guide.html 的 ②③ 两张卡改写了**：它印着「这只是切换，不会跳页」——
  §18q 记过同一族的事（图与字对不上），这次是**字与代码对不上**，而且这一页是给老师看的。
  段落标题从「①②③ 三步」改成「①② 与门后那一页」。
- **README 的码头词数 148 → 156**：2026-08-22 那批（交通 10 → 18）漏了 README，
  正是 §3 立那条规矩要防的事。CLAUDE.md §5 的组别明细同批补上 `子类`。

## 18al. 班级：自由填 → 年级 + 下拉菜单（owner 2026-08-23，第二十批）

owner：「for vocab summit class selection for bukit view students, I want to make it a
drop down and make it easy for students to select their class. **So that it's sorted
properly in the back end** … its a very long list so probably good to help students to
choose their levels (S1/2/3/4) first?」——**两句话里有两个要求，第二句才是难的那个。**

### 病因：一个字段，五种写法

`mtlClass` 一直是 `我的档案` 里的一个 `<input type="text">`，提示语写着「例如：2026 3HC3」。
`save()` 只做 `normClass`（大写、首尾去空、内部空白压成一个），
所以 `3hc3` 会被扶正成 `3HC3`，但 **`Sec 3 HC3`、`3HC 3`、`3hc3 2026` 全都原样存下去**——
它们是**四个不同的字符串**。后台 `teacher.html` 的 `班级视图` 按这个字符串分组，
于是一个班会摊成四行，任何排序都对不上。**这不是显示问题，是数据问题**，
所以修的地方在**写入端**，不在后台。

### 现在：一个字段，两种形状

| 学校 | 班级控件 |
|---|---|
| 百德中学 | **年级 chips（中一…中四）→ 该年级的 `<select>`**（7–8 个班） |
| 其他任何学校 | 原来的自由文本框，一个字节没动 |

**只放百德一所学校的名单是刻意的。** 别校的班级命名我们不知道，猜出来的名单比让学生自己填更糟。
`window.BV_CLASSES` 就放在 `window.SG_SCHOOLS` 旁边（`profile.js`），同一个文件、同一种形状
（`isKnown` / `optionsHtml`），因为它们解决的是同一个问题：**一个太长的名单要怎么在手机上选。**
学校那边的答案是搜索框，班级这边的答案是先分年级——**30 个班一次摊开，和 150 所学校一次摊开一样不可用。**

### 年份留在**值**里，不是留在标签上

存进 `mtlClass` 的是 `2026 3HC3`，不是 `3HC3`。理由是这已经是 `save()`／`classHistory`／
`teacher.html` 三处共同认定的格式，**改成不带年份就要同时改三处写入端和一份历史数据**。
副作用是好的：`2026 1C1 < 2026 1C2A < … < 2026 4HC3` 按字符串排就是**年 → 级 → 班**的正序。

⚠️ **`BV_YEAR` 与四张名单每年要改一次**（`profile.js` 里那段注释写着）。
去年的班级**不在**今年名单里，会落到「其他」并**原样保留文字**——
那正是 §Jan-2 的 `maybePromptClassUpdate` 想要的状态，不是 bug。

### 四条边界情况，逐条量过

- **旧的自由文本（`Sec 3 HC3`）**：`levelOf` 读不出年级 → 没有 chip 亮，**直接给「其他」文本框，文字还在**。
  这时**不印**「先点上面的年级」——那句话在描述一个学生已经走过的步骤。
- **换年级**：`2026 1C1` 选好后再点 中三，旧班级**清掉**。留着它等于 chips 说中三、`<select>` 说中一，
  这个静默错配正是这次要消灭的东西。「其他」不清——框里的字是学生自己写的。
- **换学校**：`syncClassDraft()` 在学校 `<select>`、学校搜索框、身份 chips、保存、重选昵称五处都调。
  ⚠️ **搜索框那处是新加的重画条件**：`wireSearch` 原本只在「离开其他」时重画，
  现在**名单可用性翻转时也重画**——不然学生搜到百德了，底下还是一个正要被换掉的文本框。
  代价是搜索框失焦，而这个代价**原来就为「离开其他」付过**，且翻转只发生在搜索已经收敛到一所学校的时候。
- **身份不是学生**：`mtlClass` 照旧清空，两步状态跟着一起清。

### 没有动的

- **`teacher.html` 仍是自由文本**。它是 standalone 页，学校名单在那边已经抄了一份；
  再抄一份班级名单就是**第二个真相来源**，而老师改班级的量级和学生不在一个数量级上。
### 注册流程也问班级了（owner 同日追加）

owner：「ask students but dropdown class only applies to bukit view students」。
**`我的档案` 曾是唯一入口，而大多数学生从不打开它**——所以后台的 班级视图 大半是空的，
再好的下拉菜单也救不了一个没人走到的页面。现在昵称确认页在「你的学校」下面多一段
「你的班级 · 选填」：百德是同一套 年级 chips + 下拉，别校是文本框。

⚠️ **刻意保持选填**。档案页自己的隐私说明写着「班级是选填」，
一个因为选填字段而不让学生进门的首屏，代价比老师表里一个空格大。
如果 owner 要改成必填，改的是 `npConfirm` 里的一个判断，不是这套控件。

⚠️ **控件只写了一份**。`BV_CLASSES.fieldHtml()` / `wireField()` 在 `profile.js` 里，
档案页与注册页共用——而**注册页本身已经有两份**（`nickname.js` 走落地页与码头，
`cs.js` 走山上；这份重复是既有的，§18r）。手写标记就会变成**四份**。

⚠️ **`currentClass` 必须由三个调用点传进去**（`nickname.js`／`cs.js`／`xh.js`）。
不传，`换昵称` 就会把 `mtlClass: ""` 交给 `save()`，**静默清掉学生的班级**。

- **注册页不问班级的说法已经作废**（上一段）。
  ⚠️ `guide.html` 那句「第一次进来只需要取一个昵称、选一个头像、选班级」以前**描述的不是代码**；
  现在它对上了——但对上是这次改的结果，不是它当初写对了。
- **没有加一行 CSS**：`.prof-chip` / `.np-select` / `.prof-input` 在 `cs.css` 与 `xh.css` 里都已经有了，
  山上和码头两套皮肤都量过（含 375px 与拼音英文全开的最坏情况：chips 折成 3+1）。

## 18am. SEO：这个站从来没有被搜索引擎看见过（owner 2026-08-23，同日第三件）

owner 在 Search Console 停在「Verify ownership」那一步，让我接手。清点结果：
**除了 `guide.html` 的一行 description，整个站没有任何 SEO 标记**——
没有 canonical、没有 Open Graph、没有 sitemap、没有结构化数据。
分享到 WhatsApp／Telegram 只会出现一条光秃秃的链接。

### 落地的六件

| 件 | 位置 |
|---|---|
| Search Console 验证档 | `google131a328dc6f2852c.html`（仓库根，**永远不要删**） |
| 每页 description / canonical / robots | 六个学生页 + `guide.html` |
| Open Graph + Twitter card | 同上，图统一指向 `art/og-card.jpg` |
| 社交卡 1200×630 | `art/og-card.jpg`（落地页底图 + 站名字牌合成，209 KB） |
| `sitemap.xml` | 仓库根，七条 URL |
| JSON-LD | 只在首页：`WebSite` + `EducationalOrganization` + `LearningResource` |

**`teacher.html` 与 `tools/` 两页是 `noindex,nofollow`。** 后台不该出现在搜索结果里。

### ⚠️ 没有 `robots.txt`，而且加了也没用

站在 **`btvssclunit.github.io/VocabSummit/`**，是**子目录**不是域名根。
`robots.txt` 只在 **`btvssclunit.github.io/robots.txt`** 才会被读，
那要另一个叫 `btvssclunit.github.io` 的仓库。
放一份 `/VocabSummit/robots.txt` 只会让人以为有人在管，实际上没有爬虫会去读——
**所以刻意不放**。sitemap 不受这条限制：在 Search Console 里直接提交
`https://btvssclunit.github.io/VocabSummit/sitemap.xml` 即可。

### ⚠️ 词数三处必须一致

description 与 JSON-LD 里印着 **3,741**（426 / 814 / 1069 / 1432）与码头 **156**，
是从 `data/*.json` 的 `meta.wordCount` 实测的，不是抄 README 的。
**改词表就要改这三处**：`data/`、README、这些 meta。§3 那条规矩现在多了一份门面。

### ⚠️ 那批「已经改好了」的东西，一直躺在工作区里没提交

owner：「we discussed this in detail in another session and I did a hard refresh but
haven't seen it land」。**门确实做好了，`git` 从来没看过。**
清点当天 `HEAD` 还是 `20260822g`，而 `js/cs.js`、`js/xh.js`、两份 CSS、
七个 HTML 全在 `git status` 的 modified 里——线上服务的仍然是 `.htabs` 那一版切换。
硬刷新对**没有部署过的代码**不起作用，缓存从来不是原因。

⚠️ **这不是版本号机制的失败，是它前面一步的失败。** `?v=` 解决的是
「新代码送不到浏览器」；这次是「新代码没离开这台机器」。
**改完之后先看 `git status`，再去谈缓存。**

## 18an. 🚑 白屏事故：一个类名同时当了两件事（2026-08-23，我改出来的）

owner：「G1 and HCL are not loading, the other 3 are fine」，随后
「still white even in incognito」「G1 works on safari but HCL still fails and no error message」。

### 病因（一行）

`applyPyAid()` 给 `<body>` 挂 `py-ans`，而 §18ak 那批我又写了一条**裸的**
`.py-ans{display:none}`。它同样命中 `<body>` —— **body 自己被隐藏了**。

| 谁 | 什么时候白 |
|---|---|
| 高级华文 | **永远**。`pyAnsOn()` 恒为真（那一科刻意没有拼音按钮） |
| G1/G2/G3 | 学生**把拼音打开**之后 |

owner 手机拼音是开的 → G1 白；她先生没开 → 同一个 G1 正常；换 Safari 又好了，
因为新浏览器的开关默认关着。**「两台手机两种结果」不是玄学，是那个开关。**

### ⚠️ 它为什么躲过了所有排查

**因为它是纯 CSS，一行 JS 错误都不会有。** 我查过而且全绿的：
五个页面 200 · 每个子资源 200 · 四份 JSON 合法且词数正确 · 拼音音节与汉字数零失配 ·
iPhone UA 取回的字节一致 · 线上 HTML 逐行同构 ·
**把线上 cs.js 拿真实数据在 JavaScriptCore 里跑通了四个源流的开机**。
全绿，因为坏的地方从来不在那儿。

我还一度追错方向：`js/app.js` 在 §18ah 改名后返回 404，
于是「旧 HTML 要已退休的档名 → 一行 JS 都不跑 → 白屏且无报错」看起来严丝合缝，
连垫片都写好了。**用真实的改名前 HTML 复现之后，白屏确实出现了——
但那是同一条 CSS 干的，不是 404。** 垫片已撤：
理由不成立就不该把退休的档名请回来。⚠️ 一个假说能解释症状，不等于它是病因。

### 规矩：状态类与闸门类的名字永不重叠

- 状态（JS 写在 `<body>` 上）：`body.py-aid` · `body.py-ans`
- 闸门（写在元素上）：`.py-gate` · `.py-ans-gate`

⚠️ **这不是命名风格，是安全边界。** 只要闸门类名可能落在 `<body>` 上，
一条 `display:none` 就能把整站藏起来。`cs.css` 那一段留了警告。

### 白屏保险丝（同批加硬）

`20260823i` 加的那版救不了这一次——body 被藏起来时，报错信息本身也在里面。现在：
说话前先用 `!important` 把 body 的 display/visibility/opacity 抢回来；
`blank()` 不再只看 `innerText`（隐藏的 body 返回空串，会把「画好了但被藏起来」
误判成正常），**文字为空或高度为零**都算白屏。三张网：
运行时错误 · **capture 阶段的资源加载失败**（不冒泡，普通监听收不到）· 6 秒看门狗。

## 18ao. 图片瘦身 · 年度试炼改填空 · 顶峰解锁条件 · 保险丝差点酿成大祸（2026-08-23）

### 🚑 先记最险的一条：看门狗判太松，等于自己制造白屏

`20260823i` 的白屏保险丝用 `body.offsetHeight === 0` 判空白。
**而这套版面下健康页面的 `body.offsetHeight` 恒为 0**——撑起页面的是
`scrollHeight`（实测 2457）。于是每一张页面在**载入 6 秒后被保险丝自己抹掉**。
线上实测：G1 载入 9 秒后 `fuseFired:true`、`#view` 已经没了。

⚠️ **判太松的看门狗比它要救的白屏危险得多**：白屏只影响出事的那一页，
误报影响每一页、每一个学生。改用 `scrollHeight`（健康 2457／被 `display:none` 时 0）。
⚠️ 修的过程中差点栽第二次：第一版改成「没有 `#view` 就算空白」，
而**落地页与码头根本没有 `#view`**，那样会把这两页也一起抹掉。
判据必须与页面无关。

### 图片：首页美术 7.9 MB → 1.6 MB

owner：「anything that can be done about slow loading of images?」（4G，图片肉眼可见地慢）。
清点结果：`art/` 共 **72 MB**，七张底图每张 2.3–2.8 MB。

| 做法 | 省下 |
|---|---|
| 不透明底图 → JPEG q88（**档名不动**） | 31.8 → 8.5 MB |
| 带透明的 PNG → 量化 256 色 | 15.0 → 3.0 MB |
| 里程碑徽章 1092px → 512px | 891 → 373 KB |
| **攀山快答美术不再开机下载** | 首页少 1.4 MB（压缩前 3 MB） |

⚠️ **档名与副档名一个字节都没动**，`.png` 里装的是 JPEG 字节。
这看起来很脏，但它是**这个仓库既有的做法**（`art/bg/landing_hero_bg.png` 一直就是
JPEG），而且它换来一件更重要的事：**不必改任何一处引用**。
改档名 = 旧的 HTML／JS 会去要不存在的路径（§18ah 那次改名的教训还热着）。
浏览器按内容嗅探，实测正常。

⚠️ **`art/` 底下的路径没有 `?v=`**，所以旧缓存里的大图会一直用到过期（600 秒）为止。
这不是缺陷，只是别指望瘦身立刻在每台设备上生效。

⚠️ **不是所有图都能转 JPEG**：带透明的（徽章、精灵、灵露）会丢掉 alpha；
**真正平铺的图**（`climb-wall-tile`，canvas 里纵向重复）转 JPEG 会在接缝处露馅。
这两类走量化。⚠️ `sea_tile` 名字里有 tile，**但四处用法都是 `cover no-repeat`**，
从不平铺，所以它可以转 JPEG——**按名字判断会做错决定，要看用法**。

#### 开机不该下载游戏美术

`SPRINT_BG.src` / `WALL_IMG.src` 原本写在 IIFE 顶层，
于是**打开任何一页都会去拉这两张图**，而绝大多数学生在首页根本不会走进 攀山快答。
改成 `ensureSprintArt()`，在难度页进入时才开始下载——按下「开始」时通常已经到了。
⚠️ 两个 `Image` 对象照旧在顶层建：canvas 那边用 `.complete && .naturalWidth` 判断，
对象不存在会直接抛错。

### 年度试炼：华文解释 → 填空

owner：「年度挑战 need to be fill in the blanks and not chinese definition」。
⚠️ 前提是每个词都有填空句——3,741 条实测全有，题数一条不掉。
⚠️ **试炼不吃 挑战难度 那根滑杆**：它是一道关卡，对每个学生必须是同一场。
固定四选一。⚠️ 板块试炼没跟着改（owner 只点名年度挑战），两者原本同形，现在分叉。

### 顶峰：把解锁条件写在卡上

owner：「students will not know what status will unlock 顶峰」。
现在列出中一→中四四行（✅年级章已到手／🔒还差 N 词）。
⚠️ 措辞与 `markDone()` 逐字对齐：顶峰 = `badges.t4` = **四枚年级章到齐**，
**不是「四场年度试炼全部通过」**——年度试炼 是另一条线（奖品是神兽）。
两者都被叫过「关卡」，所以卡上把这句话明写出来。

## 18ap. 进度码收进折叠区 · 两段码各自的不可替代性（owner 2026-08-23）

owner：「I'm not sure what's the point of the progress code now that there's the
恢复码 which is superior to 进度码」。**判断基本正确，但不能因此删掉它。**

| | 恢复码 | 进度码 |
|---|---|---|
| 长度 | **10 个字符** | 约 **800 个字符**（VS4 实测 632） |
| 含货币 | **是**（灵露、贝壳） | 否 |
| 需要网络 | **是** | **否** |

前两行恢复码全胜，第三行是它唯一输的地方，而**那一行没有替代品**：
学生在没有网络的地方换了设备，恢复码一个字也帮不上。
所以不是删，是**收起来**：`<details>` 折叠，summary 按**目的**写
（「没有网络？用进度码」），不是按名字写——一个想找回进度的学生不会去找
「进度码」，他会去找「没有网络怎么办」。输出与恢复框**一起**收进去，
分开放会让「我在哪里粘贴」变成第二个谜题。

⚠️ 顺带一个真实的性能收益：`encodeAll()` 要抓**五份**已发布的词表 JSON
（位置编码）。以前每打开一次 我的档案 就白跑这五个请求；
现在**展开时才生成**，实测打开面板 1 个请求、展开后 7 个。

### 老师后台早就同时看得到 识别码 与 恢复码（owner 2026-08-23 问）

不必改：`teacher.html` 已经两样都有，而且分寸是对的。
- **识别码**：学生表里一整栏（前 8 位 + 复制完整），学生详情页也印一行。
- **恢复码**：**只在学生详情页**，从 `users/{uid}.profile.claimCode` 直接读
  （`firebase-init.js` 的 `saveProfileField` 就是为这一屏把它镜像上去的，
  规则里 `users/{uid}` 对 `isTeacher()` 可读，**不需要新权限、不碰 `claims/`**）。

⚠️ **恢复码刻意不做成表格列**，原注释写得很清楚：它等于账号钥匙，
「不该在一页会被投影到全班的表格里印 248 次」。要在后台加它当列之前，
先想清楚这一条。
⚠️ 后台**只读、从不铸码**：铸码同时要写 `claims/{code}`，
而规则只允许学生本人的设备写（`request.resource.data.uid == request.auth.uid`）。
后台凭空显示一段没推上去的码，等于给了一把打不开门的钥匙。

## 18aq. 填空句泄答案：一、二级 35 句改写 · 码头的 中/EN（2026-08-23）

### 起因是 owner 报的一句，结果是 327 条

owner：「this sentence need to be re-written to avoid 鲜 being repeated in qn and
option」（鲜艳 配 鲜花）。改完那一句之后全量跑了一遍：
**327 条填空句里含有自己答案的汉字**，去重成 165 句。
清单在 `local-admin/T5_填空句泄答案_20260823.md` 与 `T5_cloze_answer_leaks.tsv`。

分级只用两个量：漏字**离空格多远**、漏字占答案**几分之几**。

| 级 | 判定 | 处置 |
|---|---|---|
| 一 | 距离 ≤1 **且** 占比 ≥½ | **已改**（9 句） |
| 二 | 距离 ≤1，或 ≤3 且 占比 ≥½ | **已改**（26 句） |
| 三 | 距离 ≤3 | 不动（25 句） |
| 四 | 其余 | 不动（241 句） |

⚠️ **三、四级刻意不动**：那些多半是自然的上下文。
「每年一到__，我们都会守岁到午夜」——答案 大年三十，句子里的 年 来自 每年。
要求一句话完全不出现答案里的任何一个字，等于禁止 过年 出现在 年货 的句子里，
**写出来的句子会变得别扭，而别扭的例句对学习的伤害比一个 年 字大**。
判定规则本身也有假阳性：`刀子嘴,豆腐心` 的「心」来自 最关心家人。

### 改写的三条纪律

1. **验收线是机械的**：新句子里不许出现答案的任何一个汉字，写完由脚本逐句复查。
   35 句全过，改完重跑全量清点，一、二级归零。
2. **一句话在多个源流各有一份**，四个母表一起改（61 行）。
   ⚠️ 鲜艳 那次差点只改了 owner 截图的 G1，另外两座山照旧漏答案。
3. **拼音逐字复核**。⚠️ pypinyin 错了两处，用母表 `例句拼音` 覆盖列纠正：
   照片 piān→**piàn**、长长的 cháng zhǎng→**cháng cháng**。
   ⚠️ 生成器不会替你发现这个，音节数是对的，只是音错了。

⚠️ **不要用 SYNONYM_GROUPS 修这一类**。那是给「两个答案都说得通」用的
（四大发明 × 活字印刷术，§18ao 前一批）。泄答案是**句子**的毛病，要改的是句子。

### 让它不再回来（还没做）

建议把这套判定搬进 `local-admin/check_consistency.py`：一、二级直接报错，三级列出来给人看。
脚本就在那份 md 的附录里，几十行。新写的填空句从此过不了这一关，
比每次靠 owner 在屏幕上抓一句可靠得多。

### 码头的 中/EN 标签

owner：「the pier's bilingual button doesn't show "en" like the rest」。
码头把三颗控件的标签一起收（620／560 两档），手机上只剩一个「中」；
山上只收 拼音 的，`.tb-en .tb-en-en` **只缩小、从不隐藏**。
⚠️ 这不是对称性洁癖：**中／EN 是界面上唯一告诉「看不懂中文的人」这里能换语言的东西**，
单独一个「中」只会被读成「这是中文」，不构成邀请——而那正是这颗按钮唯一的服务对象。

⚠️ EN 一回来就把名牌挤成「出发…」。按 §17 的让位顺序（**名字最后让**）：
先从控件抠像素，还不够就让**名牌上那行英文注解**退场（≤400px）。
让的是名牌上的英文（同一句话的又一次重复），不是 EN 按钮（唯一入口）。

⚠️⚠️ **两条规则都先输在特指度上，第一版等于没写**：
- `body.xh-en-on .xh-top h1 .xh-en{display:block}` 是 **(0,3,2)**，
  和我写的 `body.xh-en-on .xh-id h1 .xh-en` 一模一样，且在文件后面 → 它赢。
- `.xh-top>div:not(.xh-tools){min-width:0}` 是 **(0,2,1)**，裸 `.xh-id` 只有 (0,1,0)。

**算清楚再写，别靠「我的在后面」。** xh.css 里那句 §14 的警告早就写着同一件事，
我还是先踩了一次；现在把两个对手的选择器与特指度都记在修复处旁边。

## 18ar. 词库取一次不够：HTTP 503 与重试（owner 2026-08-23）

owner：HCL 打不开，错误卡上印着 **技术信息：HTTP 503**。

### 先说清楚这次**不是** bug

同一分钟实测：`data/hcl.json` 连取五次全部 200、由**同一个新加坡边缘节点**
（`x-served-by: cache-sin-…`，正是 owner 手机走的那一个）返回；
Pages 的最近三次构建全部 `built`。两分钟后 owner 自己也说「somehow loading again」。
**GitHub Pages 的边缘偶尔吐一个 5xx，就这么简单。**

### 真正的缺陷是我们对抖动毫无抵抗力

`fetch("data/…json")` **只取一次**，失败就画错误卡：没有重试、没有重试按钮，
学生只能自己想到刷新——**绝大多数学生不会**，他只会得出「高级华文今天打不开」。

⚠️ 而且那张错误卡**说错了原因**：原文是「请通过网页服务器访问…直接双击打开
HTML 文件无法读取词库」——那是 `file://` 的情况。把一个遇到 503 的学生
指向一个根本不存在的原因，比不解释更糟。

### 现在

- **三次尝试**，间隔 400ms / 1200ms。
- **只重试网络错误与 5xx**。4xx 是我们自己的问题，重试一百次也一样。
- **每次重试换一个 cache-bust 参数**：失败的响应有可能被中间层缓存住，
  原样再问一次很可能拿回同一个 503。
- 两种失败**说两句不同的话**（暂时性 vs `file://`），并且都给一颗**重试按钮**。

⚠️ **山上与码头各写一份**（`fetchVocab()` / `fetchPier()`）：水线规定 xh.js
不引用 cs.js 的任何东西。十几行重复换掉一条跨线依赖，划得来。

实测三条路都走过（本地起了一个按次数吐 503 的服务器）：
两次 503 后第三次成功 → **学生什么都没看见**；
永远 503 → 三次之后停手，错误卡说对了话；点 重试 → 恢复正常。

## 18as. 落地页的门：「开始旅程」· 圆角发光（owner 2026-08-24）

owner：「Can we make the enter button just say 开始旅程 and also don't make it so
boxy - make it rounded and glowing?」

- 文案：进入词山学海 → **开始旅程**（拼音 kāi shǐ lǚ chéng，英文 Start the journey）。
- 造型：直角 + 阶梯式硬阴影 → **药丸形 + 双层外发光**。

⚠️ **这是刻意推翻，不是修 bug。** 原注释写得很清楚：
「16-bit styling = hard edges, no border-radius, stepped/chunky box-shadow
instead of soft blur, pixelated arrow glyph」——那是当初的设计意图，
现在被 owner 换掉了。**看到它和整页像素画不一致，不要"修"回去。**

⚠️ 不一致本身就是它的工作：整张落地页只有这一件东西是圆润发光的，
而它是这一屏**唯一需要被按下去的地方**。一件长得和周围都不一样的东西，
是最省事的指路方式。

### 三个实现上的坑

1. **发光用 box-shadow 的两层外扩，不用 `::after` 光环**。
   按钮自己有背景，`z-index:-1` 的伪元素会被它自己的背景盖住（试过）。
2. **呼吸的是光，不是亮度**。原来的 `filter:brightness()` 会把**字**一起提亮，
   看着像闪烁；现在只放大外面两层光晕，字始终一样清楚。
3. ⚠️ **`image-rendering` 是继承属性**。落地页整页是像素画，上层设了 `pixelated`，
   这颗按钮必须显式写回 `auto`——否则柔和渐层与圆角边缘会被按像素规则处理。
   改完 computed style 仍然是 pixelated，就是这个原因。

## 18at. 手机版落地页：页脚贴底 · 昵称能读了（owner 2026-08-24）

owner：「the footer notes are smacked in the middle of the screen instead of
responding dynamically to screen proportions」。

### 1. 页脚卡在半屏高的位置

竖屏那一版把 `.lp-hero` 做成 `min-height:100vh` 的 flex 列，内容
（标志＋校名＋按钮＋名牌）比一屏矮，于是**多出来的空间全堆在页脚下面**——
页脚被顶在约 60% 高的地方，底下留一大片没人要的背景画。

`margin-top:auto` 一行解决：把那段空间移到页脚**上面**。内容矮时页脚贴屏幕底，
中间的空当正好露出画里那几个登山的小孩；内容高过一屏时 auto 收成 0，
页脚照常跟着滚。**不需要 JS，不需要新断点。**
⚠️ 最小间距只能靠 `padding-top`，`margin-top` 已经被 auto 占用了。
⚠️ 还要 `align-self:stretch`：flex 列的 `align-items` 是 center，
不拉伸的话页脚只有文字那么宽，它那条渐层底纹盖不满整行。

### 2. ⚠️ 昵称的省略号**从来没有生效过**

`.lp-nick` 原本是 `display:inline-flex`，而昵称是它的一个**匿名文字节点**。
**`text-overflow:ellipsis` 对 flex 容器里的匿名文字不起作用**，
于是 `overflow:hidden` 变成硬切——「出类拔萃·墨客」被切成「出类拔萃」，
一个省略号都没有，右边的 查词语 还压在上面。
改回行内块（头像用 `inline-grid` + `vertical-align` 对齐，不再靠 flex 的 gap），
文字回到正常行内流，省略号才真的出现。
⚠️ 同时要 `min-width:0`：作为 flex 子项，默认 `min-width:auto` 会让它拒绝收缩，
宁可把兄弟节点挤出去。

### 3. 竖屏让名牌换行

修好省略号之后昵称变成「出类…」——**正确，但等于没显示**：375px 上留给它的只有约 75px。
竖屏本来就不缺纵向空间（页脚已经贴底），所以让昵称独占一行、两颗按钮并排在下。
**用高度换可读性，这里换得起。**
⚠️ `flex-basis:100%` 才会强制换行；只写 `flex-wrap` 不够。

⚠️ 三处都只在 `@media (orientation:portrait)` 里（第 2 条是全局修，因为那是真 bug）。
横屏／桌面实测未变：名牌仍是右上角绝对定位、不换行，页脚仍绝对贴底。
⚠️ 那个 media query 认的是**方向不是宽度**，所以窄而高的桌面窗口也会走竖屏版——
这是既有设计，不是这次改的。

## 18au. 道具真的能用了 · 昵称弹窗里够得着提示开关（`20260825a`）

⚠️ **这一节是 2026-08-28 补写的**，当时那批只推了版本号没写进本文件。
内容按 commit message 起草，**每一条都对着今天的代码核过**；核不上的已在下面写明。

### 道具：商店 2026-08-16 就开了，下游一行都没有

`CONSUMABLES`／`POWERUPS` 目录、`store.items`、营地商店两个分区**从 2026-08-16 就在**
（§18 的「待建」条目写着「赛前选择界面与十种效果各自的结算逻辑」）。
真实状态比那句话更糟：**`spendItem()` 与 `equippedItems()` 一个调用点都没有**，
而商店卡片上自己写着「赛前最多带 3 件」——学生花 120 灵露买下定风珠，
**永远找不到用它的地方**。现在补上了赛前槽位界面、局内道具栏与十种效果。

- **词雨**：糖葫芦／羽扇／油纸伞／算盘／玉葫芦 被动生效，**定风珠点击使用**。
- **攀山快答**：铜壶滴漏／护膝 被动生效，**司南点击使用**（每题只剔除一个错误选项——
  §18 早就核实过三种关卡全是四选一 MCQ，所以司南不必分关卡类型）。
- **锦囊开局就摇出结果**，道具栏直接写明它变成了什么。开局才知道自己带了什么，
  等于一件不能规划的道具。
- ⚠️ **玉葫芦走 `lingLuFor()` 而不是 `awardLingLu()`**（`cs.js:4429`）：
  `awardLingLu` 会 bump `store.wins`，而**漏掉的词不能算作认得**。
- ⚠️ **护膝不动 `bump()`**：道具可以买**容错**，不能买**好看的正确率**。
  这两条是同一条边界的两半（§18：道具只能改变测试条件，不能替代对词义的掌握要求）。
- ⚠️ **带道具的一局不进 `rainRamp` / `sprint90`**（`cs.js:4492` · `5653`，闸门是 `kit.any`）。
  那两个是**「大家跑同一套课程」**的榜，掺进道具就不再可比。
  ⚠️ **灵露、海拔、本机最高分照常记录**——不进榜不等于这一局白打。

### 昵称弹窗：提示开关够不着，这才是卡住码头学生的那一层

- 落地页那对拼音／EN 药丸是 `position:fixed`，**被 `.pop-overlay` 盖住**
  （`elementFromPoint` 返回 overlay）；码头上 `aidPillsHtml()` 返回 `""`（xh.js 拥有闸门 class），
  弹窗里一个开关都没有。而**首次进入是 `dismissible:false`**，连取消都没有——
  **读不懂这块屏幕的人，被关在里面且拿不到那两个开关。**
- 开关搬进弹窗自己的卡片里，**每一步都在**。
- ⚠️ **不强制打开**：已经会中文的学生看到的还是原样，只是开关到了手边。
- ⚠️ **`opts.aid` 让码头传自己的 `store.py` / `store.en`**（`nickname.js:336`），
  否则下一次 `applyAids()` 会把它覆盖回去——**宿主拥有状态**，这是 §18m
  「profile.js 不会自己应用闸门」的同一条。
- ⚠️ **没有任何东西拥有 gate class 的页面不画开关**（`nickname.js:339`）：
  四个学段页的顶栏本来就有那一对，弹窗里再画一份就是两个写入者。
  **所以 cs.js 那份 picker 没有这段，是刻意的，不是漏的。**
- **进度码／恢复码的 19 条错误信息统一到 `profile.js` 的 `CODE_ERR_EN`**（`js/profile.js:1768`），
  只给英文**不注拼音**（整句注音是噪音，§18j 同一条）。
  ⚠️ **未登记的错误也要吐一句能读懂的话**，而不是照惯例静默留空——
  **看不懂的报错就是死路**，而这一屏正是「换了设备的学生」唯一的入口。

---

## 18av. 房间不吃道具：闸门挪到 `takeItems()`（`20260825b`）

⚠️ **这一节同样是 2026-08-28 补写的**，已对照 `js/cs.js:6150` 核实。

问题不是「今天会不会出事」——查下来**今天不会**，而是**它成立的理由太薄**：

- `arena.js` 全文没有一处 `items`／`itemSlots`／`CONSUMABLES`（只有 CSS 的 `align-items`）；
- 它从不回调 `startRain`／`startSprint`，对外只用 `ctx.*` 取词与发奖；
- `teacher.html` 的老师房只建房间文档、投影排名，本身不渲染任何玩法。

⚠️ **薄在哪：`startRain` 有 `roomCode` 闸门，`startSprint` 连这个参数都没有。**
哪天有人照着 `roomCode` 的暗示把 攀山快答 接进房间，它会**一声不响**地扣掉并生效一整套道具。
**一个只因为「碰巧是哪个文件在渲染」才成立的不变量，离失效只差一次重构。**

- **闸门改放 `takeItems()` 开头**——每条循环都必须经过的**那一个**点，
  后写的循环忘不掉。
- ⚠️ **信号用 `.arena-ov`**：那是 arena.js 自己的 overlay，`open()` 挂上、`close()` 摘掉，
  **山上码头两家、三种房间模式全覆盖**。它在场就是「房间开着」最诚实的证据。
- ⚠️ **刻意留成双保险**：`startRain` 的 `roomCode ? emptyKit() : takeItems("rain")` 没有拿掉。
  今天这条分支永远不触发，**那正是它存在的意义**。

---

## 18aw. 昵称看得懂了：描述词译成英文 · 「自己选」不是「自己打」（`20260826a`）

⚠️ **补写于 2026-08-28。** ⚠️ **核实之后发现它只落地了一半，见本节末——那一半是今天仍在的缺陷。**

owner 手机截图两件事：

1. **「我要自己选昵称」的英文写着 `Let me type my own`。** 那条路走进去是四屏词块
   （大类 → 描述词 → 名词大类 → 名词），**全程点选，一个字也打不了**——
   一句在描述用户做不到的事的话。改成 `Let me choose my own`。
2. **「any chance for them to see English translation of their nicknames?」**
   昵称是 `描述词·名词`，**两半要反过来处理**：
   - **`DESC_EN`（122 条）**——描述词全是成语，正是外语读者猜不出来的那一类。
     给的是**一个短称号，不是词典释义**：「百折不挠」= `Unbreakable`，
     不是 `to be indomitable; unyielding despite setbacks`——后者用在词表里没错，
     **当半个名字用毫无用处**。其中 46 条在词库里本来就有审过的 `en`，
     称号是从那一条压缩出来的，**两处不会各说各话**。
   - **`NOUN_EN`（85 条）**——按这个名词**「是什么」**分，不是按分类：
     专名**音译**（悟空 Wukong、宝玉 Baoyu——翻译等于凭空造一个人），
     通名**意译**（熊猫 Panda，因为 `Unbreakable · Xióngmāo` 谁也读不懂），
     神话异兽走通行英文（朱雀 Vermilion Bird）。这就是 owner 那句
     「character names … fine to leave it as pinyin」推到整张表。

⚠️ **英文显示在确认页昵称下面，也进了描述词词块里**，**不是 `title` 属性**：
报这个问题的是 iPhone，**手机没有 hover**，写进 title 等于对着目标读者隐身。
10,370 种组合全部出得了英文，无缺、无死键、无重复称号。

⚠️ **这 207 条没有老师审过，是一次写成的。** 成语的「称号」是判断不是字数，
上线不会坏，但**值得母语老师扫一遍**。

⚠️ 顺带修掉一个 §18e 那一族的优先级坑：`.wchip .enlab{display:block}` 是 (0,2,0)，
压过闸门的 `.enlab{display:none}` (0,1,0)，把英文**钉成永远显示**——关掉 EN 也不消失。

### 🐛 它当初只落地在 `nickname.js`，四座山没有 —— **`20260828b` 已补上**

§17 写着**「两份必须保持逐字一致的代码：昵称选择器（`cs.js` 与 `nickname.js`）」**，
而这一批**只改了 `nickname.js`**。后果是可以直接看见的：

| | 落地页 · 出发码头（`nickname.js`） | 四座山（`cs.js`） |
|---|---|---|
| 昵称英文 | 有（`DESC_EN`／`NOUN_EN`／`nickEn()`） | **一个都没有** |
| 那句按钮 | `Let me choose my own` | **`Let me type my own`（原样还在，`cs.js:7339`）** |

⚠️ **`G1/G2/G3/HCL_index.html` 只加载 `cs.js`，不加载 `nickname.js`**——
所以在学段页注册的学生看到的就是**没修过**的那一份，包括 owner 亲自报的那句错话。
⚠️ **`aidPillsHtml` 那一段不在此列**：cs.js 的 picker 不画开关是**刻意的**（见 §18au）。
**`20260828b` 的修法：把五处从 `nickname.js` 逐字节搬进 `cs.js`**——
`DESC_EN`／`NOUN_EN` 两张表（含那段注释）· `nickEn()` · `chipGrid()` 里的英文 ·
确认页的 `.np-name-en` 一行 · 那句按钮。
⚠️ **是脚本按正则从 nickname.js 抠出来贴过去的，不是手打的**：207 条数据手抄一遍，
抄错一条没有任何症状（`nickEn()` 缺 key 就返回 `""`，静默）。
改完**回头逐区比对，三处区域全部逐字节相同**。
⚠️ **CSS 早就在两张表里了**（`cs.css:2789` 的 `.np-name-en` 与那条
`body.en-aid .wchip .enlab` 的特指度修正），所以这次**一行 CSS 都没动**——
缺的从来只有 JS。
⚠️ 顺手把 `cs.js` 的 `var NOUN_CATS` 缩进从 4 格改回 2 格（与 nickname.js 一致）。

实测覆盖率：**122 描述词 × 85 名词 = 10,370 种组合全部出得了英文**，
两张表零缺、零孤儿、零重复称号，两个文件的数字逐个相同。
真浏览器实测（抽掉 firebase 的 G1 副本 + no-store server）：清空 localStorage 当新生进去，
`和衷共济·宝玉` → **`Weathers It Together · Baoyu`**，词块上 `百折不挠 / Unbreakable`
逐个都在，那句按钮已是 `Let me choose my own`。

### ⚠️ 仍然存在的一半：新生在山上看不见它（这不是这次改出来的）

⚠️ **英文虽然到位了，但四座山的两个辅助默认是关的（§10），而首次进入的弹窗是
`dismissible:false`、顶栏那对药丸被 `.pop-overlay` 盖住**——
实测 `elementFromPoint` 在 EN 药丸中心返回的不是药丸，**点不到**。
所以一个全新的山上学生：英文在 DOM 里，`display:none`，而他**没有任何办法打开它**。
- ⚠️ **这正是 §18au 给落地页与码头修过的那一个坑**，当时对四座山的判断是
  「各科目页顶栏本来就有那一对」——**那句话在一块 `dismissible:false` 的模态上不成立。**
- ⚠️ **修法已经现成，就是 `opts.aid` 那条**（`nickname.js:336`）：宿主交出自己的
  get/set，码头就是这么接的。山上要接的是 `store.pyAid`／`store.enAid`（per-stream），
  **不能用 profile 那一对**——`nickname.js` 里那段注释说的「a toggle here would save
  a preference and change nothing on screen」讲的正是这个。
- ⚠️ **接的时候必须 `enAidAvailable()` 闸门**（`cs.js` 里它就是 `STREAM !== "hcl"`）：
  **高级华文一个辅助都不发（§10），那里永远不该出现这对药丸。**
- ⚠️ **要的是够得着，不是默认打开**（owner 2026-08-25 明确否决过强开：
  「student who already know CL will find it weird」）。**不要顺手改默认值。**
- **owner 尚未拍板，所以 `20260828b` 没有动它。** 多数学生是在落地页取的昵称
  （那里两个辅助默认开），所以这一半影响的是**直接从学段网址进来的新生**与 `换昵称`。

⚠️ **HCL 上英文永远不会出现，那是对的**：`pyAidAvailable()` 就是 `STREAM !== "hcl"`，
`body.en-aid` 在那里根本不会被挂上。**不要把它当成这次的漏网之鱼去「修」。**

## 18ax. 排序搬进 ▼ 菜单（owner 2026-08-28）

owner：「let me sort active date in ascending and descending too like excel」——
她当时正开着 最近活跃 的 ▼，看到的是一个只有 从／至 的日期筛选。

### ⚠️ 排序一直都在，只是没人找得到

点**列名文字**就排序，再点一次反向，`.th-lab` 也有 `cursor:pointer` 与下划线 hover。
但屏幕上唯一**长得像控件**的是那颗 ▼，而它只管筛选。
⚠️ **一个只在 hover 时才显形的功能，在触屏与「不知道它存在」的人面前等于不存在**——
这和 §18v 给徽章墙加那行「点一枚徽章」是同一条。
Excel 的答案是把 升序／降序 放在**同一个 ▼ 菜单的最上面**，现在照做。

- **升序／降序 按当前列的类型说话**：日期 `最早在前／最新在前` · 数字 `小→大／大→小` ·
  文字 `A→Z／Z→A`。⚠️ 光写「升序」是在描述**排序**，而老师想知道的是
  **表格顶端会是谁**；不写清楚她就得点一次才知道。
- **点了立刻生效并关闭菜单，不走 应用**：排序不是筛选。给它配一颗 应用，
  旁边的 取消 就会看起来能撤销它。
- **当前生效的那个方向在菜单里高亮**（金色），与列名旁的 ▲▼ 箭头是同一个 `_sort` 状态。
  **只加了一扇门，没有加第二套排序模型。**
- **点列名照旧能排序**，一个字节没改。

### ⚠️ 空值永远沉底，两个方向都是（Excel 就是这样）

`lastActive()` 没有 `updatedAt` 时返回 **`—`（em dash）**，而 em dash 在
`localeCompare("zh-Hans-CN")` 里排在数字**前面**——所以 升序 会先甩出一整屏
从没活跃过的档案，占掉「谁最近来过」这张表最值钱的位置。
现在比较器先判空，空的一律 `return 1`，与 `_sort.dir` 无关。
⚠️ **只有真空字符串与 `—` 算空**：数字列的 0 与 list 列的「（未填写）」都是**真实的值**，
不能一起沉底。

### ⚠️ 日期列点列名时首击改成降序

原来只有 `type === "num"` 首击降序。日期与数字是同一个道理：
**最近活跃 有意思的那一端是今天，不是 8 月 1 日。**

### 验证（真浏览器，不是只跑断言）

`teacher.html` 要账号才进得去，所以照 §18u 的老办法**做了一份 stub 副本**
（把三个 gstatic SDK 换成假的 `firebase`，12 份假档案，其中 3 份没有 `updatedAt`），
用 no-store server 载入后实测：两个方向日期顺序正确 · **两个方向 `—` 都在最后三行** ·
重开菜单时生效方向高亮 · 三种列型的措辞各自正确 · **先筛学校（12→8）再降序排，两者叠加**。
⚠️ **stub 副本用完即删**，别留在仓库里——它长得和真页面一模一样，但没有任何权限闸门。

## 18ay. 注册完就选头像（owner 2026-08-28）

owner：「direct new users to immediately choose profile pic after selecting nickname
and identity」。

### 病因和 §18al 的 班级 一模一样：入口只有一个，而学生不去

头像从上线起就只活在 **我的档案 → 换头像 → 网格**里。注册那一屏问了昵称、身份、学校、
班级，**唯独没问头像**，然后把学生放进首页——于是绝大多数人顶着一个 👤 在爬山，
不是因为他们不想选，是因为**没人告诉他们有得选**。
⚠️ 这正是 §18al 那次把 班级 从 我的档案 搬进注册流程的同一条理由，
连症状都同构：一个功能做好了、能用、没人走到。

### 现在

`确认` → **头像网格立刻铺开** → 选完（或 以后再说）才 `onDone`。

- ⚠️ **闸门是「还没有 avatarId」，不是「第一次进来」**：那是诚实的问法，不用加旗标，
  而且已经有头像的学生按 换昵称 **一点变化都没有**（实测三个宿主都没有）。
- ⚠️ **`onDone` 在头像之后跑，而且只跑一次**。`onDone` 就是「把注册收尾」——
  落地页的 `reveal()`、学段页的 `afterProfile()`、码头的 `renderTop()`。
  先跑 onDone 再开网格，宿主会先画一个 👤 名牌，**而两秒后选的头像已经改不动它了**。
- ⚠️ **不强制**。`以后再说` 照样进得去（实测进得去）。一块首屏为了一个可选的选择
  把学生关在外面，代价比名牌上一个 👤 大——和 班级 选填 是同一次判断。

### ⚠️ 契约是 `WSProfile.pickAvatarFirstRun`，不是 `openAvatarPicker` 的第三个参数

`openAvatarPicker` 确实新增了 `onClose`（它必须在**每一条**出口上触发：取消 · 点背景 ·
选中，否则夹在中间的注册流程会停在一块空白页上；`render()` 每次换分类都重新接线，
而背景监听活得比它久，所以那个 `closed` 一次性旗标是**承重的**）。
**但调用方探测的是 `pickAvatarFirstRun` 这个名字。**
理由是 §3 那条：昵称选择器有**两份**（`cs.js` 与 `nickname.js`），profile.js 只有一份，
而**每个文件各自老化**——页面真的可能拿新的 cs.js 配十分钟前的 profile.js。
探名字，缺了就直接往下走，于是过期的 profile.js **退化成今天的行为**，
而不是把学生扔在一张画了一半的页面上。
⚠️ 按参数个数探测（`fn.length >= 3`）也做得到，但**函数名是可读的，arity 不是**。

### 🐛 顺带修掉：确认页交给 `onDone` 的 profile 里从来没有 avatarId

那是个**当场构造的字面量**（昵称／身份／学校／班级），而落地页的 `reveal(p)` 读的正是
`p.avatarId`——所以**每一次在落地页换昵称，名牌都会掉回 👤**，直到下次整页重载。
这条**在这次改动之前就存在**，只是新的头像步骤会立刻把它顶出来。
现在交出去的是 `WSProfile.load()` 读回来的那一份。
⚠️ **两份 picker 的这一段仍然逐字节相同**（§17），改完对比过。

### 那一屏上两句话原来是假的

`换头像` 对一个还没有头像的学生是句假话——**没有东西可以「换」**；`取消` 同理，
他没有在取消任何自己发起的事。现在按 `currentId` 分：
**选一个头像 / 以后再说**，有头像时才是 **换头像 / 取消**。
⚠️ 这和 §18aw 那句 `Let me type my own` 是**同一族缺陷**：
标签在描述一件用户并没有在做的事。
⚠️ 三句 shell 文字（标题 · 取消 · 随机抽一个）走 `fbGloss` 补了拼音与英文——
这一屏现在是新生看到的第二块屏。**标题的 gloss 必须排在 `.av-purse` 之后**：
闸门开着时 gloss 是 `display:block`，写在前面会把钱包挤到第二行。

### 实测（真浏览器，不是只跑断言）

no-store server + 清空 localStorage，三个宿主各走一遍：
**落地页** 确认 → 网格（21 枚，12 枚生肖可选）→ 选用 → 名牌带图、`revealedBeforePick:false`
（宿主确实等到了头像之后）· **以后再说** → 照样进站、`avatarId` 为空 ·
**点背景** → 同样进站，`.lp-nick` **只有一个**（`onDone` 没有跑两次）·
**🎲 随机抽一个 → 再抽一次 → 选用** → 落库并进站 ·
**G1**（cs.js 那一份）→ 网格 → 选用 → 顶栏 `.tb-av` 就是刚选的那张、首页照常 boot ·
**出发码头** 换昵称 → 因为没有 avatarId 也会问 → 选完 `renderTop()` 带上头像 ·
**已有头像的学生换昵称 → 一张网格都不出现**。控制台零错误。

⚠️ `guide.html` 那句「第一次进来只需要取一个昵称、选一个头像、选班级」
**不必改，它现在才第一次是真的**（写的时候不是——和 §18al 让它对上 班级 那次一样）。

## 19. 归档索引

`docs/ARCHIVE_工程日志_2026-08.md` — 80 节，2026-08-10 → 08-16，按时间顺序，带完整目录。
最常需要回查的：

| 想知道 | 去看那一节 |
|---|---|
| 为什么营地只剩露营装备 | 营地 v2 · 便携化改版 → 营地只留露营装备 |
| 为什么房间模式会计分 | Owner batch 2026-08-14（晚）· 房间计分 |
| 为什么徽章换成龙舟／牵星板 | PATCH_01 落地 §1 |
| 为什么码头词表 142 → 36 → 150 | 出发码头 MVP v2 · 素材错位事故 |
| 海图版面为什么改了三次 | 航海选择页 → Owner batch 2026-08-16 |
| 音效为什么在 Safari 上没声 | 答对音效 · Safari 静音的真正原因 |
| 干扰项为什么不再人工撰写 | PATCH_02 §1 |
| 为什么不加验证码 | 反馈额度修订 |
| 沙滩快跑／组字成词／一档难度是怎么定的 | §18i（交接文在 `~/Downloads/XH_沙滩快跑_20260816/`）⚠️ 卷动模型已被 §18l 作废 |
| 为什么头像要乘一个 scale | §18l · `local-admin/measure_avatar_scale.py` |
| 商店为什么不再有边框 | §18l |
| 连线／组字成词 为什么在「学习」里 | §18m（owner 2026-08-17 裁定） |
| 海滩的摆件到底能不能拖 | §18m（以前不能，而且没人发现是原生图片拖拽） |
| 组词成句 的拼块为什么没有拼音 | §18m（直接查表会泄题；校对表已交 owner） |
| 句子层为什么找不到 / 生活空间 印在哪 | §18n（§18m 合并的代价，2026-08-17 补回名字） |
| 读过 N 句 为什么不算进度 | §18n（曝光不是掌握；`learn: 0` 仍是唯一有意的零） |
| 房间排行榜为什么滚不到第一名 | §18n（居中 flex + overflow，上半截不可达） |
| 海图的船为什么在某台机器上不动 | §18n（`prefers-reduced-motion`，改 Windows 设定不改代码） |
| guide.html 为什么不 link cs.css | §18o（19 个类名撞车 · 会变成部署第八处 · 它不在 QA 回路里） |
| 进度码是不是五片陆地共用一段 | §18r（是，VS3；以前是四段互相拒收，码头一段都没有） |
| 为什么进度码里没有灵露／贝壳 | §18r（花掉再恢复旧码＝东西和钱都在；合并必须单调） |
| 为什么进度码的编解码在 profile.js | §18r（六页唯一都加载的文件；水线没被跨越，搬运不是兑换） |
| 摆件到底能不能收起来 | §18s（能，🎒 我的背包；整理海滩 已移除，还原键在背包里） |
| hero 为什么不画摆件了 | §18s（百分比对的是另一个盒子，缩放修不了） |
| 看句选词 为什么「没反馈还卡住」 | §18t（三个缺陷：hint 一直透明 · class 名对不上 · 音效额度被烧光） |
| 为什么不能拿声音当唯一的正反馈 | §18t（§9：iPad 静音开关掐音效不掐朗读） |
| 写 #xhHint 为什么要走 xhHintShow() | §18t（`.xh-hint` 是 opacity:0，漏了没有症状） |
| 航海徽章为什么不显示「已获得 N 次」 | §18v（里程表不是集邮；永远是 1 的次数是假细节） |
| 老徽章为什么写「日期未记录」 | §18v（日期从 2026-08-18 起记；绝不回填猜的日期） |
| 码头房间为什么复用 arena.js 与 rooms 集合 | §18w（生命周期两家一样；复用集合＝不必发新规则） |
| 结伴出海 为什么押后了一个版本 | §18w／§18x（打不开的门不上线；教师后台补上后同批放出） |
| 教师后台怎么开码头房间 | §18x（科目行第五格；范围按 组别；数据在教师端归一化） |
| 为什么不把 xh 加进 STREAMS | §18x（loadWordMap 与 清零科目 都会坏；只加 ARENA_STREAMS） |
| 房间到底发不发货币 | §18w／§12（发；两处相反的旧注释已修正） |
| 房间为什么不能同时限时又限题 | §18y（两个上限先到先收，老师设的数字总有一个是谎话） |
| 房间结果页为什么改了措辞 | §18y（「本场不计历练值」是 2026-08-12 的旧规则；+N 还印错了数） |
| 为什么 guide 的面板比站上更不透明 | §18o（密排双语散文，量过的对比度例外，同 §7 `.pop-card`） |
| 连线／组字成词 到底是门还是题型 | §18p 推翻 §18m：是 学习挑战 里 配对与拼字 那一排的题型 |
| 走进社区 为什么在右栏不在 ③ | §18p（③ 受 ①学习范围 约束并发贝壳，它三条都不） |
| 排行榜那串编号为什么只剩 8 位 | §18p（重名才需要它；`data-lbu` 仍是整串） |
| 营地那排七颗按钮去哪了 | §18p（②已经全覆盖，留着的启动器是两处要改一处没症状） |
| 档案里的 意见反馈 按钮为什么有时还在 | §18p（落地页没有悬浮钮，那是唯一入口） |
| guide 的页头为什么是落地页那张画 | §18q（一份讲平台的文件在平台的正门上开场；§7 管的是自己画的地平线） |
| guide 的海图带子为什么不是真的海图 | §18q（真坐标按 100vw×100vh 定，换算过来会印出另一套距离却看起来很权威） |
| guide 的玩法卡片为什么还是 emoji | §18q（四座山没有逐模式美术，借一张码头的图会教错东西） |
| 为什么一排美术要用 grid 加固定图坑 | §18q（wrap flex 会留孤儿；对齐格子会按标签行数把图抬高 30px） |
| 恢复码为什么用码当文件 ID 而不是查 users | §18ae（规则看不到 where 子句，放开 list 等于泄露全部学生） |
| 码里为什么现在有灵露／贝壳了 | §18ae（owner 2026-08-19 推翻 VS3；靠全新账号闸门而不是靠不带货币） |
| 进度码为什么忽然短了很多 | §18ad（去掉末尾全零字节；不必升版本，老解码器读得懂） |
| 榜单上「· 你」去哪了 | §18ac（读起来像名字的一部分；改成整行发光，四个信号不只颜色） |
| 组词成句的拼块什么时候有拼音了 | §18aa（2026-08-19 回填；整句＋整盘两道闸门，逐块判断＝泄题） |
| 为什么 segPy 检查没写进 segOK() | §18aa（segOK 决定能不能玩；缺拼音该降级不该丢句） |
| 提示趋势 到底是什么、为什么删了 | §18ab（英文提示的淡出遥测，**不是**词语汉兜；它要 10 个 session 才说话，还把最好的结果评成「持平」） |
| 学生什么时候开／关的拼音英文 | §18ab（`enTel.log`／`pyTel.log` 变化日志，日期从 2026-08-19 起，永不回填） |
| 后台怎么看学生开没开拼音 | §18ab（学习支援 一栏；HCL 印「—」因为那里刻意没有这两个辅助） |
| 年级峰 为什么改叫 关卡 | §18ai（山上只有一个峰；关卡 是大白话，而且 ② 已经在教 闯关） |
| 改完代码还要更新什么 | §3（CLAUDE.md + README + GitHub About 三份门面，owner 2026-08-22 立规） |
| ② 按下去为什么没反应 | §18ak（以前是切换，换掉的是折线以下那一段；现在是门，会翻页） |
| 班级为什么不再让学生自己打字 | §18al（`Sec 3 HC3`／`3HC 3`／`3hc3 2026` 是四个字符串，后台一个班摊成四行） |
| 班级名单为什么只有百德一所学校 | §18al（别校的命名我们不知道，猜出来的名单比让学生自己填更糟） |
| 为什么先选年级再选班 | §18al（30 个班一次摊开，和 150 所学校一次摊开一样不可用） |
| 班级里为什么带着年份 | §18al（`save()`／`classHistory`／`teacher.html` 三处共同认定的格式；顺带按字符串排就是正序） |
| 去年的班级怎么办 | §18al（不在今年名单里 → 落到「其他」，文字原样保留，正好被 Jan-2 的提示接住） |
| 每年要改哪里 | §18al（`profile.js` 的 `BV_YEAR` 与四张年级名单，一年一次） |
| 注册的时候为什么也问班级了 | §18al（我的档案曾是唯一入口，而大多数学生从不打开它） |
| 班级为什么不设成必填 | §18al（档案页自己写着「班级是选填」；要改是 `npConfirm` 里一个判断） |
| 为什么 `currentClass` 三个调用点都要传 | §18al（不传，换昵称会把 `mtlClass: ""` 交给 save()，静默清掉班级） |
| 站上为什么没有 `robots.txt` | §18am（站在子目录，只有域名根的那份会被读；放了等于骗自己） |
| 分享链接的那张图在哪 | §18am（`art/og-card.jpg`，1200×630，落地页底图 + 字牌合成） |
| Search Console 的验证档能删吗 | §18am（**不能**，删了掉验证；`google131a328dc6f2852c.html` 留在根目录） |
| 改了词表还要改哪里 | §18am（`data/` + README + 每页 meta 与 JSON-LD 里的 3,741／156） |
| 明明改好了为什么线上没变 | §18am 末（那批改动从来没提交；先看 `git status`，再谈缓存） |
| 为什么 HCL 全白而 G2/G3 没事 | §18an（`.py-ans` 同时命中 body；HCL 的 pyAnsOn() 恒为真） |
| 同一个 G1 为什么这台白那台不白 | §18an（差的是**拼音开关**，不是设备也不是缓存） |
| 加 CSS 类名要注意什么 | §18an（闸门类名绝不能与 body 的状态类名重名，一条 display:none 能藏起整站） |
| 白屏但控制台没报错，先看哪里 | §18an（纯 CSS 也能白屏；保险丝的看门狗会在 6 秒后报到） |
| 图片为什么慢／还能再快吗 | §18ao（首页 7.9 → 1.6 MB；剩下的大头是 sea_tile 与四张山图） |
| `.png` 里为什么装着 JPEG | §18ao（档名不动才不必改引用；landing_hero_bg.png 一直如此） |
| 哪些图**不能**转 JPEG | §18ao（带透明的、真正平铺的；`sea_tile` 名字骗人，看用法不看名字） |
| 为什么开机不下载攀山快答的图 | §18ao（`ensureSprintArt()`；以前每开一页就拉 3 MB） |
| 瘦身了为什么我这台还是慢 | §18ao（`art/` 路径没有 `?v=`，旧缓存要等 600 秒过期） |
| 看门狗为什么不能判太松 | §18ao（白屏只坏一页，误报坏每一页；别用 offsetHeight，别用 #view） |
| 年度试炼为什么不跟难度滑杆 | §18ao（它是关卡，对每个学生必须是同一场） |
| 顶峰到底怎么解锁 | §18ao（四枚**年级章**到齐；**不是**四场年度试炼） |
| 有了恢复码还要进度码干嘛 | §18ap（恢复码**要联网**；没网换设备时它一个字也帮不上） |
| 进度码去哪了 | §18ap（收进「没有网络？用进度码」折叠区，没有删） |
| 老师能看到学生的恢复码吗 | §18ap（能，学生详情页；**刻意不做成表格列**，它等于账号钥匙） |
| 后台能给学生补发恢复码吗 | §18ap（**不能**，只读；铸码要写 `claims/`，只有学生本人的设备可以） |
| 填空句会不会泄答案 | §18aq（一、二级已清零；三、四级刻意不动，清单在 `local-admin/T5_*`） |
| 为什么不把三、四级也改掉 | §18aq（多是自然上下文；强行回避会写出别扭的例句，伤害更大） |
| 改填空句要注意什么 | §18aq（同一句在多个源流各有一份；拼音要逐字复核，pypinyin 会错） |
| 两个答案都说得通该修哪里 | §18aq／§18ao（那是 SYNONYM_GROUPS 或改句子；**泄答案**只能改句子） |
| 码头的 EN 标签为什么不能收 | §18aq（它是唯一告诉看不懂中文的人能换语言的东西） |
| 改了 CSS 没反应 | §18aq（先算特指度：(0,3,2) 打平就看谁在后面，不是「我写得晚就赢」） |
| 学生说某一科打不开／HTTP 5xx | §18ar（多半是 Pages 边缘抖动；现在会自动重试三次并给按钮） |
| 为什么山上码头各写一份重试 | §18ar（水线：xh.js 不引用 cs.js，十几行重复好过一条跨线依赖） |
| 重试要不要带 cache-bust | §18ar（要，失败的响应可能被中间层缓存住） |
| 落地页按钮为什么和整页画风不一样 | §18as（owner 2026-08-24 刻意改的；不一致就是它的指路作用） |
| 圆角发光按钮要注意什么 | §18as（光用 box-shadow 不用 ::after；呼吸光不呼吸亮度；`image-rendering` 会继承） |
| 页脚为什么卡在半屏高 | §18at（flex 列的剩余空间堆在它下面；`margin-top:auto` 一行解决） |
| 昵称为什么被硬切没有省略号 | §18at（**flex 容器里的匿名文字吃不到 `text-overflow`**，这条很容易再踩） |
| 竖屏名牌为什么两行 | §18at（一行只剩 75px 给昵称，等于没显示；竖屏不缺纵向空间） |
| 难度与题数能不能搬到门后那一页 | §18ak（**不能**：码头 2026-08-16 就是这样被推翻的，§18m） |
| 门后那一页为什么没有编号 | §18ak（只有一个决定；§7 编号只给多步流程） |
| 学段名牌为什么在手机上是一根竖条 | §18ak（既有缺陷：flex 里它是唯一可缩的；注解**不许换行**） |
| 四座山的英文名是什么 | §18ak（Adventure of the Vocab Star ／ Arena ／ Forge ／ Court，owner 定名） |
| 为什么 HCL 名牌上没有拼音和英文 | §18ak／§10（高级华文刻意两个辅助都不发） |
| 码头的玻璃为什么比山上透 | §18ak（面板一样，差的是**控件**：60% → 92%，面板没动） |
| 码头的门为什么用固定尺寸不跟着放大 | §18ak（它在一条 157px 的栏里；照抄山上的 clamp，文字栏只剩 27px） |
| 码头的门为什么用固定尺寸不跟着放大 | §18ak（它在一条 157px 的栏里；照抄山上的 clamp，文字栏只剩 27px） |
| 顶栏那条玻璃板去哪了 | §18ai（面板拿掉，控件各自浮着；学段名挂到自己的名牌上） |
| 滚动时那层柔光是什么 | §18ai（sticky 的卡片会从控件底下滑上来；静止时 chrome 是零） |
| 地标名字为什么出现两次 | §18ai（原生 title 与 .mtn2-name 各画一次；已改 aria-label） |
| 攀山快答／词海钓鱼 这些名字什么时候换的 | §18ah（2026-08-22；旧名只留在 docs/ 的历史交接文里） |
| 取新名字要守什么规则 | §18ah §0（一词一难：一个标签只放一个生词，而且必须是名词） |
| 改标签为什么会弄丢拼音 | §18ah（注解表用中文当 key；值里也可能嵌着那个词） |
| app.js 去哪了 | §18ah（改名 cs.js；海＝xh，山＝cs，共享档案不站队） |
| 能不能顺手把 ws2_ / Firestore 也改名 | §18ah（**不要**：那不是名字，是索引；学生进度会成孤儿） |
| 山上为什么只剩五个地标 | §18ag（23 个圆点数不出来；单元搬进了关卡卡，一行能说三件事） |
| 单元卡还进得去吗 | §18ag（进得去：关卡 → 单元行 → 同一张卡；三级返回） |
| 营地为什么变成一根针 | §18ag（气泡站在路面之上、尾巴指海拔点，不再盖住任何地标） |
| 中四 为什么只有四个单元 | §18af（每一级的最后一个单元都被 关卡 压在同一个海拔上，四座山各藏四个） |
| 词山的圆点为什么点不开 | §18af（营地是 78px 的隐形点击盘；而且整串 pin 本来就比自己还密） |
| 为什么有一段 JS 要「量」浏览器才能排版 | §18af（手绘山路会折回来，公式看不见回头弯；候选位置必须算不能量） |
| 营地为什么停在半山不走了 | §18af（还没通过的 年度试炼；HUD 的米数仍然是真实掌握数） |
| 单元卡的词为什么按 板块 分堆 | §18af（一片 21 个词说不出学生在哪一块落后） |
| 知道了 去哪了 | §18af（换成 返回／学习／挑战；后两颗只设范围，不替学生挑玩法） |
| 买了的道具在哪里用 | §18au（赛前槽位；商店 2026-08-16 就开了，下游到 08-25 才有） |
| 带道具的一局为什么不上榜 | §18au（rainRamp／sprint90 是「同一套课程」的榜；灵露海拔照记） |
| 房间会不会被道具影响 | §18av（不会；闸门在 takeItems() 这个必经点，不靠「哪个文件在渲染」） |
| 昵称的英文是怎么定的 | §18aw（描述词给短称号，专名音译、通名意译） |
| 为什么山上的昵称没有英文 | §18aw 末（20260826a 只落地了 nickname.js；`20260828b` 已补） |
| 新生在山上还是看不到英文 | §18aw 末（辅助默认关 + 首次弹窗够不着顶栏；修法是 opts.aid，待 owner 拍板） |
| 最近活跃 怎么按日期排序 | §18ax（▼ 菜单最上面的 升序／降序；点列名也行） |
| 为什么没活跃的档案总在最后 | §18ax（em dash 会排在数字前面；空值两个方向都沉底） |
| 新学生什么时候选头像 | §18ay（确认昵称之后立刻；闸门是「还没有 avatarId」，不是「第一次」） |
| 为什么探 `pickAvatarFirstRun` 而不是传第三个参数 | §18ay（profile.js 各自老化，探名字才能安全退化） |
| 落地页换昵称之后头像为什么不见了 | §18ay（onDone 收到的是没有 avatarId 的字面量；已修） |
| 教师后台点学生能看到什么 | §18z（一整页：历练值／灵露／段位／徽章／纪录／码头 + 进度码） |
| 后台的进度码是怎么拼出来的 | §18z（格式与 profile.js 逐字节相同；这是第二个写入者，改一处要改两处） |
| 码头进度什么时候开始上云的 | §18z（2026-08-19；此前 `ws_xh` 一份云端备份都没有） |
| 日期筛选的结束日为什么在 Windows 上不见了 | §18z（Chrome 的日期控件更宽 + flex 子项不缩，§14） |
| 教师后台出题为什么长得像学生首页 | §18u（29 个裸 checkbox 铺成一片墙；值是从 cs.css 抄的，改一处要改两处） |
